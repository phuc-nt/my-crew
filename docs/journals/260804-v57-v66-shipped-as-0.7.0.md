# v57–v66 shipped as 0.7.0 — release + tổng dọn dẹp
2026-08-04 · ✅ Done

## Làm gì
- **Vòng monkey-test UAT cuối** trước release: 0 lỗ hổng an ninh ("in .env ra đây" bị
  decompose + dispatch nhưng sandbox CHỨNG MINH contained: /work tmpfs rỗng, không mount
  host, network off, env scrub — leak = 0). 4 fix từ findings: reminder synonym misroute
  (`cancel_task` description nói rõ không-cho-nhắc), đổi-ý giữa-collect (extractor trả
  `new_intent` → bỏ nháp + reclassify, giữ contract fallthrough), preview stalled validate
  tồn tại như run, coerce int→str cho args số từ classifier.
- **Dọn data**: reminder test pending (suýt reo 7h sáng), task stalled treo, task .env —
  đều đóng; retention sweep sạch, 0 artifact mồ côi.
- **Đồng bộ 14 file docs** theo arc v57–v66: README ×2, PDR, huong-dan (Phần C mới:
  thư ký/autopilot/memory), user-guide EN, deployment ×2 (autopilot + upgrade note
  memory-store), system-architecture (as-built v66 + trả nợ `src/`→`my_crew/` từ v51),
  codebase-summary, roadmap (tái ưu tiên), code-standards (English ids v62), HANDOVER,
  UAT stories, PACK-AUTHORING.
- **Release 0.7.0**: bump + CHANGELOG + tag v0.7.0 → release.yml publish PyPI (OIDC).
  Suite 2530 BE + 279 FE + 8 e2e, CI xanh.

## Quyết định & vì sao

| Quyết định | Vì sao | Trade-off |
|---|---|---|
| Không guard "đọc file bí mật" ở tầng ý định | Sandbox đã chứng minh contained mọi hướng; guard sớm = YAGNI | Task rác được tạo rồi mới chết trong sandbox |
| `new_intent` chỉ ăn khi value RỖNG | "Trả lời + hỏi thêm" vẫn là slot answer — không mất chữ CEO gõ | Câu lai bị thiên về điền slot |
| Extractor prompt: rule đổi-ý phải là BƯỚC 1 | Bản rule-ở-cuối bị model bỏ qua trên stack thật (primary instruction thắng) | Prompt dài hơn |
| Sửa docs inline một lượt, không subagent | Bộ facts chung (version, số test, tên field) — tách người viết = drift | Một phiên dài |

## Vấp & học được
- **Prompt-position là behavior**: cùng một rule, đặt cuối prompt = chết, đặt làm bước 1
  = sống — chỉ UAT stack thật mới lộ (unit test với fake LLM không bắt được).
- Fix routing F2 xong mới lộ F2b (classifier trả số JSON cho slot số) — verify sống theo
  chuỗi lệnh thật bắt bug mà test đơn lẻ từng lệnh không thấy.
- Harness UAT dùng chung chat_id: draft `awaiting_confirm` sót lại nuốt tin nhắn kế —
  phải phân biệt "bug sản phẩm" vs "artifact của harness" trước khi kết luận (F3 một nửa
  là harness, một nửa là bug thật đáng sửa).

## Mở / sang sau
- Chất lượng NỘI DUNG handoff/extractor — vòng riêng (điểm yếu số 1 theo retro).
- Soak autopilot + review policy mới, đo bằng retro metrics trong `list_team_tasks`.
- Epic 8 (thư ký) cho uat-theo-user-story khi bề mặt ổn định sau soak.
