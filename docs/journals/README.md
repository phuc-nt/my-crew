# Dev Journal — my-project-manager

Dòng thời gian phát triển kiến trúc + tính năng (repo vừa-làm-vừa-học). Đọc bảng dưới để thấy cả hành trình; mở từng file cho chi tiết.

**Quy ước:** 1 file / mốc, **tiền tố ngày** `YYMMDD-<slug>.md` (vd `260622-phase-1-slice-2-confluence-report.md`) → sắp xếp theo thời gian, mỗi mốc tự ghi ngày. Súc tích theo template — chỉ ghi cái verify được, không bịa, không kể lể. Ghi ở mốc (phase/slice xong hoặc sự kiện đáng nhớ).

## Dòng thời gian

| Ngày | Mốc | Trạng thái | Tóm tắt |
|---|---|---|---|
| 2026-06-21 | [Phase 0 — Scaffold](260621-phase-0-scaffold.md) | ✅ Done | Hello-agent (LangGraph) + guardrail core. Chốt: tool qua MCP+CLI; allowlist + Lớp A hard-deny (2 vòng review). E2E OpenRouter OK. |
| 2026-06-21 | [Phase 1 Slice 1 — Reporting](260621-phase-1-slice-1-reporting.md) | ✅ Done | Jira+GitHub→Slack qua gateway. Agent SPAWN MCP subprocess (stdio-only). E2E post Slack thật. |
| 2026-06-22 | [Phase 1 Slice 2 — Confluence](260622-phase-1-slice-2-confluence-report.md) | ✅ Done | Report detail→Confluence + short+link→Slack. E2E thật cả 2. State chỉ primitive (fix checkpoint). |
| 2026-06-22 | [Phase 1 Slice 3 — Daily/Weekly + Cron](260622-phase-1-slice-3-daily-weekly-cron.md) | ✅ Done | `report --daily\|--weekly` (weekly + sprint data) + cron launchd. Phase 1 HOÀN TẤT. |
| 2026-06-22 | [Phase 2 — Guardrail Hardening](260622-phase-2-guardrail-hardening.md) | ✅ Done | Dedup bền + audit query + Lớp B interrupt (queue/approve). Reviewed, red-line verified. |
| 2026-06-22 | [Phase 3 — OKR Tracking](260622-phase-3-okr-tracking.md) | ✅ Done | OKR (Confluence table) → map epic → rollup có trọng số → report --okr + nhúng weekly. E2E + write thật. Fix bug duedate ở Jira MCP (overdue im lặng). |
| 2026-06-22 | [Phase 4 — Resource + Cost](260622-phase-4-resource-cost.md) | ✅ Done | Workload theo assignee (overload tương đối mean) + cost (LLM budget thật + labor ước lượng) → report --resource + nhúng weekly + cron. Review fix C1 (tên assignee inject Slack mrkdwn). |
| 2026-06-22 | [Phase 5 — Audience-split](260622-phase-5-audience-split.md) | ✅ Done | `--audience internal\|external` cả 4 report; external → giọng business + Lớp B duyệt. Review fix C1 (link Confluence rò PII). E2E lộ+vá gap Phase 2: approve giờ post thật. Service/bot/multi-user hoãn. |
| 2026-06-23 | [v2 M1-P1 — Config-injection](260623-v2-m1-p1-config-injection.md) | ✅ Done | Giết 2 config singleton (grep src/ = 0 hit). `from_dict` thuần + `from_env` wrapper; config inject làm tham số qua cả call graph. Mở đường P2 per-agent (`profile.yaml→dict→from_dict`). 282 test xanh. |
| 2026-06-24 | [v2 M1-P2 — Profile system](260624-v2-m1-p2-profile-system.md) | ✅ Done | Agent = thư mục `profiles/<id>/` (4 file: profile.yaml + SOUL/PROJECT/MEMORY.md). Loader env-fallback 3 tầng → `default` == v1. Inject persona/project/memory vào prompt; external KHÔNG lấy gì từ profile (guardrail PII). `--profile` ở cli/cron. 317 test xanh + E2E thật. |
| 2026-06-24 | [v2 M1-P3 — Registry + worker](260624-v2-m1-p3-registry-worker.md) | ✅ Done | N agent cô lập: `registry.yaml` + worker subprocess (1 process/agent, `.data/agents/<id>/`) + service daemon (cron `schedule:` qua croniter, cap 4, timeout 600s). Cô lập rơi ra từ 1 `data_dir`; auto-migrate `.data/` v1. thread_id `<id>:<kind>:<audience>`. 383 test + E2E spawn thật. |
| 2026-06-24 | [v2 M1-P4 — Multi-agent CLI](260624-v2-m1-p4-multi-agent-cli.md) 🏁 | ✅ Done | `mpm agent list/register/run/approvals/approve/reject/audit`. Surface multi-agent trên primitive P3 (additive, cli/cron giữ legacy). `run` spawn worker (lock-step argv với service); approvals/audit per-agent = GAP-CLOSER (đọc `.data/agents/<id>/` mà cli global không thấy sau migrate). 414 test + E2E thật. **🏁 ĐÓNG MILESTONE 1** (P1→P2→P3→P4). |
| 2026-06-24 | [🏁 v2 Milestone 1 COMPLETE](260624-v2-m1-milestone-complete.md) | ✅ Done | **Tổng kết milestone**: 1 agent (v1) → N agent cô lập hoàn toàn qua CLI/worker + scheduler, guardrail per-agent. 17 commit, 414 test, E2E thật. Retrospective + bài học xuyên P1–P4. Sang M2 (web dashboard + Postgres + streaming). |
| 2026-06-24 | [v2 M2-P5 — Graph interrupt](260624-v2-m2-p5-graph-interrupts.md) | ✅ Done | Mốc đầu M2. Lớp B → LangGraph `interrupt()` thật: node `approval_gate` (3 graph) pause external trước deliver, checkpoint-serialize, resume `Command(resume=...)`. `execute_approved()` post LIVE (fix C1: trước đó re-queue lần 2). `worker --resume` + `mpm agent resume`, exit 3 + run-event `interrupted`. AUGMENT (queue giữ, replace ở P8). 443 test + E2E thật (post Slack + reject sạch). |
| 2026-06-25 | [v2 M2-P6 — FastAPI + SSE](260625-v2-m2-p6-fastapi-sse.md) | ✅ Done | Web surface M2: FastAPI localhost (list/status/trigger/stream). Trigger chạy graph in-process (sync `stream` trong thread vì sync SqliteSaver), SSE phát node-progress LIVE + terminal `interrupted`. `summarize_node` = PII firewall allowlist. Cap 4, single-drain (409). E2E bắt 2 bug fake che mất (astream-vs-sync-saver + empty-box-on-resume) → vá + test thật. P5↔P6: `mpm agent resume` đọc checkpoint server tạo → post thật. 490 test. |
| 2026-06-25 | [v2 M2-P8 — Postgres + Store + memory](260625-v2-m2-p8-postgres-store.md) | ✅ Done | 2 nửa opt-in: Postgres checkpointer (SQLite default) + LangGraph Store + **cross-thread agent memory** (extractor LLM → Store content-hash + mirror MEMORY.md section, đọc lại qua inject P2 internal-only; KHÔNG qua gateway, KHÔNG tới external). Postgres wired + selection-test, chưa chạy PG thật. Review bắt 2 CRITICAL (conn-leak `__enter__`, MEMORY.md marker-doubling) → vá + regression test. 518 test. |
| 2026-06-26 | [v2 M2-P7 — Web dashboard](260626-v2-m2-p7-web-dashboard.md) | ✅ Done | Mảnh cuối M2: dashboard HTMX+Jinja2 trên app P6 (additive, 4 route cũ byte-stable). 6 surface: list/status/cost · **approve/reject trên UI** (đúng đường post thật CLI) · audit · config view+EDIT (validate→atomic-replace, MEMORY read-only) · trigger+SSE live. Extract dispatcher trùng (cli+mpm) → src/actions. Review verify: không bypass guardrail, edit sai giữ file gốc byte-identical. 545 test. |
| 2026-06-26 | [🏁 v2 Milestone 2 COMPLETE](260625-v2-m2-milestone-complete.md) | ✅ Done | **Tổng kết milestone**: platform v2 đủ TOÀN BỘ — interrupt (P5) + streaming SSE (P6) + dashboard (P7) + Postgres/Store/memory (P8). 16 commit, 545 test. **E2E thật TOÀN pattern** (Jira/Slack/Confluence + Postgres throwaway thật). Bài học: E2E + review adversarial bắt nhiều CRITICAL mock che mất. M2 không còn mảnh hoãn → sang M3. |
| 2026-06-26 | [v2 M3-P10 — Skill system](260626-v2-m3-p10-skill-system.md) | ✅ Done | 5 skill PM bundled (`skills/*.md`, instruction-only — KHÔNG tool authority) + LLM selector inject được (fake offline, lỗi→`[]`) tự chọn skill → chèn body vào prompt INTERNAL của cả 3 builder. Lằn ranh đỏ: external KHÔNG lấy gì từ skill (phòng thủ 2 lớp, mutation-proven). No-skills byte-identical + allocation-free (pool rỗng → không dựng LlmClient). Wire qua 3 entry point (server thừa hưởng). 592 test, review DONE/slice. |
| 2026-06-26 | [v2 M3-P9 — Cross-agent memory (A3)](260626-v2-m3-p9-cross-agent-memory.md) | ✅ Done | 2 agent cùng `project:` đọc-chéo fact của nhau (RO sibling, WO self qua `_assert_self_namespace` fail-loud). Đọc Store namespace `(sibling_id,"memory")` namespace-scoped (InMemory+Postgres) + LLM ranker inject được (lỗi→`[]`, lọc chống bịa) → chèn block INTERNAL của cả 3 builder. Lằn ranh đỏ: external KHÔNG lấy gì (phòng thủ 2 lớp). No-project byte-identical + allocation-free. Wire qua 3 entry point (1 store dùng chung read+write). Review S1 bắt 1 HIGH (sibling YAML hỏng crash reader→vá). 628 test. Lưu ý: đọc-chéo thật cần `store: postgres`. |
| 2026-06-27 | [v2 M3-P11 — Integrations + multi-channel](260627-v2-m3-p11-integrations-multichannel.md) | ✅ Done | C3+D2: MCP registry config-driven (`extra_servers` + `integrations:` block, token env-only) → Linear stdio (read + gated `linear_createComment` Lớp B; huỷ diệt→Lớp A) · Email/SMTP = action type mới `email_send` qua gateway, **ALL email = Lớp B**, password env-only, stdlib smtplib STARTTLS (zero dep). Channel registry wire 3 graph đồng nhất, email INTERNAL-ONLY (external KHÔNG email). Mọi write mới sau Action Gateway (allowlist default-DENY + Lớp A + Lớp B); no integrations/smtp ⇒ byte-identical. 704 test, review DONE/slice. Live-key E2E hoãn. |
| 2026-06-27 | [v2 M3-P12 — Automation + observability](260627-v2-m3-p12-automation-observability.md) | ✅ Done | B4+B3+D3: **B4** LangSmith tracing opt-in (DEFAULT OFF = byte-identical, gated flag+env, lazy import) · **B3** run replay từ checkpoint đóng băng (no re-fetch live data, safe-replay guard: chỉ deliver/approval_gate/terminal) · **D3** workflow automation = READ-only data + analyze LLM + PROPOSE action thông Action Gateway (Lớp B enqueue, KHÔNG auto-exec). Lằn ranh đỏ giữ: tracing observability-only, replay+automation đều qua gateway. Backward-compat: OFF + no-automation ⇒ byte-identical. 775 test. **M3 HOÀN TẤT** (P10+P9+P11+P12 xong). |
| 2026-06-27 | [🏁 v2 Final live E2E](260627-v2-final-live-e2e.md) | ✅ Done | **E2E live toàn surface** (agent throwaway, Postgres + post THẬT). Verify: Jira read 21 issue · Confluence page thật (2064385) · approve→Slack post thật (ts 1782532805) · Postgres checkpointer+store · 20 fact memory thật · B4 tracing toggle · B3 replay (dedup + refuse unsafe) · D3 propose Lớp B/skipped. Bắt 1 bug offline che mất (`LlmResult.text`→`.content`) → vá + regression test. Dọn sạch (DSN không vào file tracked). 776 test. **ĐÓNG v2.** |
| 2026-06-27 | [🏁 v2 COMPLETE — M1+M2+M3](260627-v2-complete-milestone.md) | ✅ Done | **Tổng kết v2 toàn bộ**: 3 milestone (core + platform + extensibility) = 776 test, 28 commit, live E2E. M1: multi-agent CLI + worker/service + isolated stores (282→414 test). M2: interrupt + streaming + dashboard + Postgres opt-in (414→545 test). M3: skills + cross-agent memory + integrations/multi-channel + automation/observability (545→776 test). Bài học xuyên v2: E2E live bắt CRITICAL mà mock che; review adversarial + repeated-write = lưới thứ 2; guardrail per-agent KHÔNG rewrite chỉ inject; lằn ranh đỏ = test+observe không chỉ comment. Deferred: live-key integration (Linear/SMTP/LangSmith real), advanced workflow (when, schedule), replay re-fetch. Sang sau = mở rộng domain hoặc multi-user. |
| 2026-06-28 | [v2 M4 — React visualization dashboard](260628-v2-m4-react-dashboard.md) | ✅ Done | React SPA (Vite+TS) replaces HTMX dashboard; 5 slices (JSON API + React shell + visual views + ops surfaces + wiring). Read-only JSON layer (`routes_visualize` + `routes_ops_json`) over existing data; approve/reject via same gateway path. Memory/automation internal-only. Guardrail untouched. 785 pytest green, vitest 11, ruff clean. Live-key E2E deferred; auth/remote deferred. |
| 2026-06-30 | [v3 M5 — Domain-pack abstraction](260630-v3-m5-domain-pack-abstraction.md) | ✅ Done | Mốc đầu v3. Lõi PM-cứng → multi-domain: domain = pack cắm vào (`domain-packs/<domain>-pack/`), thêm domain = thêm pack KHÔNG sửa lõi. PM → `pm-pack`, byte-identical. 6 slice: scaffold+`domain:` field · report-kind dispatch (PackRegistry) · ToolProvider (transport-agnostic, đọc qua `pack.tools`) · allowlist config-driven + **RED LINE gate** · prompt+skill → pack asset · generic `Task`/`Event` + `Issue↔Task` lossless. Lằn ranh đỏ VERIFIED adversarial: Lớp A markers GIỮ lõi check-trước, pack KHÔNG override; default-DENY giữ; PM==core default. Scope: dispatch giữ lõi (shared primitive), PM analyzer giữ Issue (round-trip = proof). 816 test, ruff clean, review 0 CRITICAL/HIGH. |
| 2026-07-01 | [v3 M6 — hr-pack (ép abstraction)](260701-v3-m6-hr-pack-proof.md) | ✅ Done | Pack thứ 2 (HR) chạy THẬT trên cùng lõi. Domain mới = folder mới; HR đọc **Google Sheet qua gws CLI** (transport lõi chưa từng biết, spawn như gh) + Confluence reuse → headcount → Confluence+Slack qua guardrail cũ. GATE: hr-pack `git diff src/`=**RỖNG**, nhưng thêm HR lộ 3 M5 seam thiếu → vá 3 commit generic RIÊNG (discovery filesystem · pack-as-importable-package · kind-validation union) — 0 HR logic. Bài học: literal "src/ rỗng" không đạt với pack self-contained đầu, intent (0 domain logic) ĐẠT. Red-line: HR external strip link như PM, PII-safe by design (headcount = số tổng, không tên). E2E live: gws đọc sheet 10 người → Confluence 3211265 + Slack ts=1782916805 (Active 7, Eng 4). 839 test, review DONE_WITH_CONCERNS→4 finding vá (H1 external red-line, H2 pack isolation, M1/M2/M3 fail-loud). |
| 2026-07-02 | [v3 M7 — UI low-tech (create + lifecycle)](260702-v3-m7-low-tech-ui.md) | ✅ Done | Low-tech user tạo + quản lý vòng đời agent 100% qua web: wizard 5 bước (domain→identity→reports/schedule→bindings→review), pause/resume/delete, panel sức khỏe kết nối. Định vị chốt: setup kỹ thuật 1 lần / vận hành low-tech hằng ngày. UI và CLI tạo agent CÙNG scaffold path (`registry_edit` dùng chung); mọi write config validate-before-replace + atomic (không bao giờ corrupt registry); web KHÔNG cầm secret (health = presence bool; token box = tên biến .env copy-paste). Review vá hết: H1 append corrupt-persist, M2 effective_enabled (registry∧profile), M1 wizard stale-pack-state. 863 pytest + 30 vitest xanh; E2E live create→pause→delete round-trip, registry byte-identical. Kèm: đánh giá lại plan v3/v4 theo mục tiêu "đội agent thay human hành chính" → thêm M11 ask-agent Slack inbox (planned). |

| 2026-07-02 | [v4 M9 — model fallback chain](260702-v4-m9-model-fallback-chain.md) | ✅ Done | Agent không chết vì 1 model lỗi: `model_chain` → 402/429/5xx/timeout/empty tự chuyển model kế, log lớn tiếng (`FALLBACK:` + `LlmResult.fallback_from`); 401/403/Budget KHÔNG fallback (key-level/cap tối thượng, re-check trước mỗi attempt). As-built gọn hơn plan: chain trong `LlmClient.complete` → 0/9 call site sửa, không khai báo = byte-identical. 885 test; E2E live: primary invalid → fallback minimax → report delivered. Review vá M1 fail-loud config, M2 warn chain-override-model. |

| 2026-07-02 | [v3 M11 — ask-agent Slack inbox](260702-v3-m11-ask-agent-inbox.md) | ✅ Done | Agent thành "đồng nghiệp" hỏi được: mention `@<agent-id>` trong channel internal → poll (search MCP, watermark, bootstrap không dội backlog, cap 3/poll) → QA pipeline ground trên `pack.tools.read` (pm-pack thêm `read()`, 0 core change) → reply thread qua Action Gateway (dedup theo mention ts). Internal-only chặt hơn plan (external channel fail lúc load). Review bắt H1 đắt giá: self-loop nếu LLM echo "@id" — vá structural `sanitize_reply`, không tin prompt. 909 test; E2E live: hỏi PR mở → trả lời đúng 2 PR stale thật trong thread, re-poll không double. |

| 2026-07-02 | [v3 M8 — admin-pack (fleet watch)](260702-v3-m8-admin-pack-fleet-watch.md) | ✅ Done | Domain thứ 3, nhìn VÀO TRONG: giám sát cả đội (cost-rollup/guardrail-health/audit-digest) qua accessor generic read-only (`agent_state_reader`, sqlite mode=ro) + team alerts banner (budget≥80%, approval treo 24h, deny spike). GATE PASS: src/ chỉ 2 bổ sung generic dự liệu. Review bắt H1 systemic: pack allowlist chưa wire vào runtime gateway (hr-pack cũng dính từ M6) — vá cả hai + test runtime-path; bài học: test classifier riêng = phantom guarantee. 922 test; E2E live: agent admin TẠO QUA WIZARD M7 → post Slack số liệu đội thật. |

| 2026-07-02 | [v5 M12 — chat-command qua Lớp B](260702-v5-m12-chat-command-lop-b.md) | ✅ Done | Bậc 3 thang trách nhiệm: "đồng nghiệp nhờ được". Mention yêu cầu tiếng Việt → LLM chỉ PHÂN LOẠI theo catalog pack khai (validate lúc load: tool cấm không khai nổi) → args validate bằng code → `enqueue_for_approval` (primitive gateway mới: Lớp A trước, refuse-not-queue) → người duyệt → jira dispatch mới thực thi. Chat không bao giờ chạy thẳng. Reviewer xác nhận invariant, 0 HIGH; vá M1 infra-error≠question (lần 2 pattern này trong ngày). 940 test; E2E live: mention → #23 chờ duyệt (Jira untouched) → approve → SCRUM-23 thật. |
| 2026-07-03 | [v6 M13 — danh tính Telegram per agent](260703-v6-m13-telegram-identity.md) | ✅ Done | Mở v6 ("công ty 1 CEO"). Mỗi agent 1 Telegram bot riêng (tên/avatar qua BotFather) — nhận Q&A (M11) + lệnh Lớp B (M12) + report (channel registry) qua transport HTTP thuần, không MCP mới. Action type `telegram_send` native gateway; `chat_ids` allowlist 2 tầng (đọc bỏ qua + ghi refuse chat lạ). Không khai telegram → byte-identical. Review 0 CRITICAL; vá H1 privacy-mode docs, M1 secret-regex token Telegram, M2 bootstrap offset=-1, M4 per-chat isolation. 969 test; E2E LIVE 3 bot (PM/HR/Admin) mô phỏng công ty: Q&A data thật · lệnh→#24→duyệt→SCRUM-24 thật · lệnh nguy hiểm bị chặn (0 mutation) · report tự push vào Telegram. Bài học: Bot API không trả tin bot tự gửi ⇒ self-loop bất khả thi (M11 phải defuse, Telegram cho free); bot trả lời qua service poll ~60s, không realtime. |
| 2026-07-03 | [v6 M14a — CEO chat-ops](260703-v6-m14a-ceo-chat-ops.md) | ✅ Done | "CEO không cần terminal": DM admin bot tiếng Việt → tạo agent (slot-filling nhiều lượt), bật/tắt, hỏi trạng thái/chi phí. M12 áp vào mặt phẳng quản trị: LLM chỉ classify+extract, CODE chạy catalog CORE-fixed (không lệnh xóa). Multi-turn state machine + conversation store SQLite (TTL); confirm 2 bước thay Lớp B cho config-writes (M7: không qua gateway). Operator gate trên `mention['user']` bất biến (agent=admin + telegram operator). Không operator → M11/M12 byte-identical. Review 0 CRITICAL; vá 2 MEDIUM (guard test domain-choices theo pack; confirm word-membership + cancel-thắng fail-safe). 998 test; E2E LIVE bắt 3 bug UX thật (extract "quản lý dự án"→pm, lowercase "DAILY]"→daily, validate reject) → tạo agent sales-pm 100% qua hội thoại. Phạm vi M14a; web chat box + dashboard Việt hóa → M14b. Bài học: E2E thật là bộ test UX không thay thế được — đối thoại người dùng luôn bẩn hơn fixture. |
| 2026-07-03 | [v6 M14b — Web chat box](260703-v6-m14b-web-chat-ops.md) | ✅ Done | Mặt web của chat-ops: 1 endpoint `/api/ops/chat` + view Chat.tsx (route /chat, nav "Trợ lý"). TÁI DÙNG nguyên engine M14a — conversation_key = operator_id (không phải transport) → web + Telegram DÙNG CHUNG draft (bắt đầu ở web, xác nhận qua Telegram OK). Không SSE (ops reply 1 lượt). Posture localhost no-auth: "web IS operator by construction" (cùng trust các route admin write sẵn có); không mở thêm quyền, mọi write vẫn confirm 2 bước; M16 phủ auth. Dashboard Việt hóa CẮT (chủ dự án chốt không cần). 1003 pytest + 33 vitest; live E2E: POST tiếng Việt → engine chung trả trạng thái fleet thật. Bài học: engine transport-agnostic (M14a) khiến mặt web chỉ là adapter mỏng. |
| 2026-07-04 | [v6 M15b — report/qa task + board](260704-v6-m15b-report-qa-task-board.md) | ✅ Done | Đóng M15: thêm report-task (chạy report kind định kỳ qua ĐƯỜNG GRAPH CŨ, external→Lớp B) + qa-task (câu hỏi lặp qua Q&A M11) — recurring tới deadline 14d (CODE) hoặc cancel; runner tách `_run_watch`/`_run_recurring`. Web board `/tasks` (GET list mọi agent + POST cancel idempotent, giao việc vẫn qua chat) + nav "Việc đã giao". INVARIANT giữ: mọi post qua gateway đúng phân loại. Review 0 CRITICAL → vá 5: hash()→sha256 (dedup ổn định qua restart), M1 per-day gate (report-task đốt token 24×/ngày→1×), M2 synthetic mention post top-level (không thread_ts giả vào Slack), L1 board per-agent isolation, L2/L3. 1037 py + 36 ts. E2E live: giao report+qa qua chat → board hiển 3 loại → cancel. Bài học: `hash()` không bền qua process (PYTHONHASHSEED); dedup-ở-deliver không cứu compute-ở-compose (gate trước body đắt); synthetic input phải khai cờ để nhánh giả-định-input-thật rẽ đúng. |
| 2026-07-04 | [v6 M16 — auth + go-live](260704-v6-m16-auth-golive.md) 🏁 | ✅ Done | **ĐÓNG v6**: production hardening. Single-user session auth (`auth.py`: bcrypt hash + itsdangerous SessionMiddleware; AuthMiddleware chặn mọi route trừ health/login/me/assets; rate-limit 5/phút/IP; auth OFF khi chưa đặt hash → byte-identical dev). Auth = bảo vệ nút Duyệt (Lớp B) trên web. Fail-loud guards: bind non-loopback+auth-off refuse; auth-on+secret-yếu refuse ở create_app (cả đường uvicorn app:app). Serve SPA 1 process từ FastAPI + /health. deploy/install.sh (uv sync+build+preflight+launchd) + com.mpm.web.plist + backup/restore (loại .env R4). `mpm web hash-password/gen-secret`. UAT checklist tiếng Việt 8 mục + deployment-production.md. 1053 py + 39 ts. E2E live: auth ON → mọi /api/* 401 pre-login → login → SPA serve FastAPI → bind guard live. Review 0 CRITICAL, ship-blocker none → vá weak-secret HIGH (tự bắt) + M1 (guard ở create_app) + H1 (SSE test đúng route) + M2 (restore --no-absolute-names). Bài học: fallback secret là bẫy im lặng nguy hiểm nhất (quên-set=auth-giả, forge cookie được) → fail-loud khi auth ON + secret công khai; guard phải fail lúc START không lúc dùng; /api/me public nhưng chỉ lộ authenticated+username. 🏁 v6 HOÀN TẤT — 4 bậc trách nhiệm, 3 domain, danh tính Telegram, chat-ops tiếng Việt, production-ready. |
| 2026-07-04 | [v7 M17 — setup wizard](260704-v7-m17-setup-wizard.md) | ✅ Done | Mở v7 (zero-friction, qua red-team plan trước cook). Cửa vào: máy sạch → install.sh (clone/build 3 MCP server + open browser) → **First-run Setup Wizard** nhập key qua form (mỗi bước có Test) + đặt mật khẩu → CEO KHÔNG mở text editor. `env_writer` atomic merge .env + **whitelist key-name** (chống env-injection PATH/LD_PRELOAD→RCE); `routes_setup` guard: khóa BỀN bằng marker `.setup-complete` (rotate hash không mở lại — red-team MAJOR-2), localhost-only (không tin X-Forwarded-For), write-only (status bool không lộ value). Ghi .env KHÔNG hot-reload (load_dotenv không override + secret bind-once) → finish RESTART web (red-team MAJOR-1). 1072 py + 42 ts. Review 1 CRITICAL C1: gộp "show-wizard?" (UX) với "lock-writes?" (security) vào 1 hàm → wizard tự khóa sau step0 + dashboard hở auth → TÁCH `wizard_locked`/`wizard_should_show` + test real-flow. H1/M1/M2 vá. E2E: ghi nhiều key liên tiếp không 410 → finish → 410; xóa-hash-marker-vẫn-khóa. Bài học: 1 predicate cho UX + security = bug (tiến hóa đối lập); fix vội regression đẻ CRITICAL — vá phải test đúng đường thật; .env ghi ≠ hiệu lực (lặp lần 3, phải restart). |
| 2026-07-04 | [v7 M18a — agent studio](260704-v7-m18a-agent-studio-telegram.md) | ✅ Done | Lấp gap wizard M7 (tạo agent xong CHƯA chạy được, dừng ở copy .env tay). Giờ: bind bot Telegram từ web (`routes_agent_knowledge.bind_telegram`: getMe validate → ghi `<AGENT>_TELEGRAM_BOT_TOKEN` vào .env whitelist + telegram block profile → override-load → chạy NGAY không restart vì token đọc per-call, khác session-secret M17). Trang agent hợp nhất `/agents/:id` (header + tab Hoạt động gom status/cost/runs + tab Kênh Telegram bind panel). Team click→trang agent; wizard tạo xong→redirect. 1080 py + 44 ts. Review 1 CRITICAL C1 (partial write: bind không chat_id → token vào .env rồi builder raise → nửa vời; cũng là deadlock UI getUpdates-cần-token/bind-cần-chat) → vá require ≥1 chat_id 400 SỚM trước mọi ghi + recent_chats POST token-pasted phá deadlock. H1 rebind-ghi-đè-block (mất ops_operator_id) → merge. M1 test-stub-che-validate → stub gọi build_telegram thật. E2E live: bind-no-chat 400 no-partial-write → bind-with-chat @phucnt_my_pm_bot → agent chạy được. Bài học: ghi 2 nơi phải validate-trước-write (all-or-nothing); UI cho phép cái backend cấm = bug chờ nổ; stub che validate = phantom coverage. |
| 2026-07-07 | [v9 P4 — responsive mobile](260707-v9-p4-responsive-mobile.md) 🏁 | ✅ Done | **ĐÓNG v9**. Frontend CSS + markup nhẹ (backend py 0 đổi). `@media(≤640px)`: 3 bảng CEO (Đội/Việc-tasks/Approvals) → card-list CSS-only qua `data-label` + `td::before content:attr()` (ô hành động suppress); touch `min-height:44px`; input `font-size:1rem` chống iOS-zoom; nav/chips/approval wrap. Bảng Nâng cao (Audit/Run/Proposals/Overview) → `.table-scroll` overflow-x. **Review bắt coupling**: Overview dùng chung `.agents-table` → bị card-list nhưng thiếu data-label → giá trị xếp chồng không nhãn → vá tách `.agents-table-advanced` + `.table-scroll`. 86 vitest; preview HTML (token production) verify card-list vì jsdom không test breakpoint. Bài học: class dùng chung = coupling ẩn (media query nuốt mọi view reuse class) → tách class cho behavior khác; card-list CSS-only `data-label`+`attr()` đủ đẹp không cần component riêng; preview HTML = verify visual rẻ nhất khi không browser. 🏁 **v9 HOÀN TẤT**: CEO low-tech duyệt-hiểu (P1) → tạo-qua-chat không dead-end (P2) → giao diện WCAG đồng bộ (P3) → dùng-trên-phone (P4), backend py 0 đổi toàn bộ. |
| 2026-07-07 | [v9 P3 — design-token visual](260707-v9-p3-design-token-visual.md) | ✅ Done | Frontend CSS + class, **0 dependency** (CSS thuần, giữ no-UI-kit). `:root` design-token (màu/spacing/radius/shadow/type-scale) — 112 var(), hex hard-code → var giá trị tương đương. **Contrast WCAG AA**: `.muted` #777→#6b6b6b (5.74:1), th/chat-who #888→#707070 (5.13:1). Type scale: h1 1.1rem (nhỏ hơn nav!)→1.4rem dẫn hierarchy. Component-class `.btn`/`.btn-primary`/`.btn-danger`/`.card`/`.badge` áp Team/Work/Chat; element `button` global GIỮ → Nâng cao không vỡ. CompanyDocs `.active` (gán class thiếu CSS) → styled. 86 vitest (class additive không phá query text/role). Bài học: design-token = 1 sửa re-theme cả app không cần UI kit; contrast fix vô hình mắt thường nhưng qua ngưỡng WCAG; class visual additive an toàn vì test query text/role không query class. |
| 2026-07-07 | [v9 P2 — nav-flow create-via-chat](260707-v9-p2-nav-flow-create-via-chat.md) | ✅ Done | Frontend-only. Nút Đội "+ Tạo nhân sự ảo": `<Link>`→`<button>` kiểm `opsChatAvailable()` → available→`/chat?intent=create-agent`, KHÔNG available/lỗi→`/create` wizard. **Chống dead-end red-team B3**: CEO mới (chưa admin-agent/telegram) luôn tạo được agent đầu tiên; test 3 nhánh (available/unavailable/throw). Chat prefill intent qua **lazy useState init** (không clobber CEO gõ — reviewer đề xuất thay effect `[searchParams]`); màn "Chưa dùng được" thêm link wizard. Tab Kiến thức: dirty-tracking 4 section độc lập (SOUL/PROJECT/kỹ năng/tài liệu), "● Chưa lưu" khi sửa, save-lỗi GIỮ dirty (đúng bằng omission). 86 vitest. Review **DONE** 0 blocking. Bài học: nút thân thiện phải luôn có đường ra (test cả nhánh unavailable+throw); query-param lazy-init > effect khi chỉ cần lúc mount (effect `[searchParams]` = trap clobber im lặng). |
| 2026-07-07 | [v9 P1 — i18n + human-hoá](260707-v9-p1-i18n-human-hoa.md) | ✅ Done | Mở v9 (UX/i18n polish, frontend-only, **backend py 0 đổi**). Việt hoá + human-hoá 4 điểm chạm CEO. `labels.ts` (mới): RUN_STATUS/KIND/VERDICT + `labelFor` (thiếu→"—" không rỗng) + formatDateTime VN + formatCron ("09:00 Thứ 2, Thứ 4"/"chạy thủ công"). **`action-summary.ts` (mới) = TRUST SURFACE**: tóm tắt tiếng Việt đọc ĐÚNG field-shape thật (`mcp_tool`→`args` camelCase, `email_send`→top-level to-LIST, `gh_cli`→argv), phủ đủ Lớp B universe (jira create/close/transition/assign, slack internal/external, confluence, linear, gh pr), map thiếu→raw 1-dòng, external flag ngoài `<details>`. ConfirmDialog rewrite (tiếng Việt + modal a11y Esc/focus/scroll). Work reject=window.confirm. Team/wizard/DomainPicker/health/client.ts (lỗi HTTP→VN)/Approvals Việt hoá; delete-note KHÔNG lộ path. 79 vitest (58 cập nhật + 21 mới) + tsc + build sạch. **ADVERSARIAL review trust-surface (verify vs backend builder, không đoán)**: summary đọc ĐÚNG mọi shape (không lỗi casing/nesting — nỗi lo lớn nhất), JSON auditable, no XSS; vá M1 (class `.confirm-external` thiếu CSS → cảnh báo external render text thường), M2 (email.to là list, type sai), L1 (Linear thiếu issue id); H1 (external suy từ regex `reason` — an toàn hôm nay, ghi LIMITATION, fix đúng = structured flag backend, defer). Bài học: trust surface phải verify field-shape với NGUỒN backend không đoán (camel/snake + nested args = chỗ đẻ tóm tắt rỗng "trông hợp lệ"→duyệt mù); style-hook thiếu CSS = cảnh báo chết; suy dữ-liệu-an-toàn từ free-text = nợ kỹ thuật, ghi rõ cạnh code. |
| 2026-07-04 | [v8 M23 — Trust ladder](260704-v8-m23-trust-ladder.md) | ✅ Done | Kết thúc v8. Lần NỚI QUYỀN THỰC THI đầu tiên từ v1: auto-approve Lớp B có kiểm soát, **Lớp A/kill-switch/dry-run KHÔNG đường nào tới auto**. Dual-surface (red-team B1: Lớp B = 2 cơ chế): `scheduled_reports:` tại `approval_gate` (auto-deliver không interrupt) + `actions:` tại gateway (re-enter `_execute(approved=True)` → Lớp A re-apply, red-team B2 CẤM inline). Config `auto_approve:` (validate fail-loud, vắng=OFF byte-identical) destination-bound + trần claim-slot local-date atomic (chống TOCTOU) + trusted_senders Telegram-DM (user-id bất biến). UI "Đã tự duyệt hôm nay" đọc run-event flag. 1205 py + 58 ts. **ADVERSARIAL review: THE INVARIANT HELD mọi probe**; 3 defect vá: HIGH (inbox gateway thiếu auto_approve → chat auto CHẾT trong runtime thật, E2E đầu bỏ sót vì hit gateway trực tiếp), MED (automation propose no-handler tiêu slot + drop proposal), LOW (empty-id short-circuit). E2E live config profile thật: 2 surface + kill-switch/Lớp A vẫn deny. Bài học: "Lớp B" là 2 cơ chế khác tên chung; nới quyền = re-enter guard đầy đủ không inline; E2E phải đi ĐƯỜNG THẬT không đường tắt (test sạch bỏ sót wiring runtime). |
| 2026-07-04 | [v8 M22 — Multi-project rollup](260704-v8-m22-multi-project-rollup.md) | ✅ Done | Ràng buộc lớn nhất công ty >1 team: single-project cứng. Giữ 1 agent=1 project + report kind `project-rollup` admin-pack gom summary báo cáo gần nhất từng agent (KHÔNG live-fetch/pack mới/multi-binding). Worker ghi `report_summary` bounded (500 char, strip tag, cắt câu) vào runs.jsonl CHỈ internal delivered (external/pseudo-kind omit → byte-identical). `build_project_rollup` nhóm theo project (jira_key/repo), exclude fleet-read agent theo CAPABILITY (`reports ∩ fleet-kinds`, không domain-name), never-run→"chưa có báo cáo". External audience → raise (chứa summary internal). **RED-TEAM B3 vá**: `report_summary` leak qua status API (`agent_views` echo raw event) → `_public_last_run` whitelist field; reviewer trace 6 client path, timeline (`visualize_views` allowlist riêng) cũng an toàn → test khóa CẢ HAI. 1170 py. E2E live hr+sales-pm 2 project: rollup đúng 2 summary, admin excluded, status API no leak, external blocked. Bài học: thêm field vào log dùng-chung phải rà MỌI client path (mỗi surface allowlist riêng); exclude theo năng lực không theo tên; field additive omit-khi-rỗng = backward-compat. |
| 2026-07-04 | [v8 M21 — CEO-observability](260704-v8-m21-ceo-observability.md) | ✅ Done | Gap bị bỏ sót: agent chết ngầm (key hết hạn/MCP chết/token revoke) CEO low-tech không biết. 2 alert CODE-only vào `team_alerts`: `missed_schedule` (kind có `schedule:` mà run gần nhất cũ hơn max(2× chu kỳ, 6h), disabled→im, poller không overdue) + `failing` (≥3 error liên tiếp, success phá streak). `ops-alerts` run-kind admin (synthetic cron 6h) tính alerts → nhắn **1 tin gộp** Telegram `ops_operator_id` qua gateway, dedup per (agent,kind,LOCAL-date) qua `DedupStore.claim` bền restart. UI badge health nav Đội (`use-team-health` đọc `/api/team/alerts`, không route mới). 1153 py + 57 ts. **Review 1 HIGH**: `read_run_events(limit=10)` GLOBAL → agent vừa report vừa poll làm inbox event evict HẾT report event → cả 2 alert BẤT HOẠT cho cấu hình PHỔ BIẾN (E2E đầu pass vì test-agent không poller) → vá `_recent_events_per_kind` giữ 10 event/KIND trên tail 5000. M1 local-vs-UTC cron → `_prev_fire` convert local khớp scheduler. E2E live: sales-pm 5d stale → **CEO nhận DM Telegram thật ✓**; sales-pm 400 inbox flood → report alert vẫn sống. Residual (ghi rõ): watchdog không tự canh mình (admin/service chết → không alert). Bài học: test single-kind = phantom coverage cho hệ multi-kind; window toàn cục evict nguồn thưa → down-sample per-entity; detection phải cùng đồng hồ scheduler. |
| 2026-07-04 | [v7 M20 — CEO-first nav](260704-v7-m20-ceo-first-nav.md) | ✅ Done | Kết thúc v7. Gom nav 12 mục → **4 mục CEO cần mỗi ngày**: Trợ lý (chat, mặc định) / Đội (team→trang agent, Tạo, Kho tài liệu) / Việc (gộp approvals-mọi-agent + tasks board, badge đỏ chờ duyệt) / Cài đặt (kết nối + Nâng cao giữ đủ 7 view kỹ thuật). FRONTEND-ONLY, **backend 0 đổi** (diff chỉ web/+static). Badge = client fan-out `getAgents→getApprovals` đếm pending, KHÔNG route mới (red-team SCOPE-2). URL cũ `/approvals`+`/tasks`→`/work` redirect. Bỏ agent-picker toàn cục → AdvancedAgentView bọc picker cho 7 view per-agent. Chat quick-chips (3 câu + chip động approvals). 56 vitest (py 1140 không đổi). Review: no-function-lost verify từng đích (13 đích cũ đều tới được), H1 double-poll `/chat` (Layout+Chat mỗi cái mount hook fan-out riêng) → vá PendingApprovalsProvider 1 nguồn chung. Bài học: backend-0-đổi = aggregate client trên endpoint sẵn có; gom nav = đổi chỗ đứng KHÔNG xóa (redirect + Nâng cao); hook fan-out mount nhiều nơi = poll nhân bản → context provider. |
| 2026-07-04 | [v7 M19 — Company Docs](260704-v7-m19-company-docs.md) | ✅ Done | Tính năng MỚI duy nhất v7: kho tài liệu chung công ty. CEO paste tài liệu (`company-docs/*.md` frontmatter title/updated, CRUD atomic, 50KB reject) → tick per-agent (`company_docs:` trong profile.yaml, mirror `skills:`) → inject `<company_docs>` vào prompt INTERNAL 3 builder (report/okr/resource) + Q&A, bounded 12KB khai báo cắt. Tên tách khỏi "knowledge" M18b (chủ dự án chốt "Company Docs"). **RED LINE external=0 mutation-proven**: `company_docs_text` guard `audience!="internal"→""` (y hệt `select_skill_text`); test 2 lớp mỗi builder (internal có sentinel, external không có sentinel+tag); `company_docs=""`→byte-identical. Inject-all-ticked (không selector LLM: CEO tick = đã chọn). 1140 py + 50 ts. Review: red line HELD, 1 MEDIUM (VN slug "Chính sách"→"doc" collapse ascii-only regex → collide 409) → vá fold dấu (đ→d + NFKD) → "quy-trinh-nghi-phep". E2E live hr/sales-pm: paste nghỉ phép→tick HR→HR internal CÓ, HR external SẠCH, PM không mang. Bài học: red line copy CẢ guard ở nguồn không chỉ pattern; sản phẩm Việt-first slug phải fold dấu (ascii-only = bug im lặng); tên là quyết định thiết kế (hỏi trước, tránh lẫn M18b). |
| 2026-07-04 | [v7 M18b — knowledge form + skills](260704-v7-m18b-knowledge-form-skills.md) | ✅ Done | CEO nuôi kiến thức agent không cần markdown: SOUL/PROJECT thành FORM (↔ markdown bọc marker `<!-- field:KEY -->`, mất marker → raw_mode → editor raw KHÔNG đè prose viết tay), skills tick chọn (`GET/PUT /api/agents/{id}/skills`, chỉ tên trong catalog domain, ghi `skills:` vào profile.yaml giữ nguyên key khác). 1100 py + 47 ts. Review 1 CRITICAL C1 (marker-injection phá round-trip: value chứa `<!-- /field:x -->` → mất data raw_mode-vẫn-False → hỏng IM LẶNG) → render raise `MarkerInValueError` + self-check render→parse lossless. H1 ghi-đè-file-raw-qua-PUT-form (TOCTOU UI-only) → server re-parse 409. H2 `{"fields":{}}` blank-đè → đòi ≥1 key nhận diện. M1 agent-lạ tạo-file-rác → `_require_agent` 404. Tách studio routes: shared/telegram/knowledge + `AgentKnowledgeTab.tsx`. E2E live agent `hr` (non-destructive): SOUL viết tay→raw đúng, form round-trip, skill hr-pack ghi profile, guard 409/400/404. Bài học: value có thể chứa chính delimiter → cấm+self-check fail-loud (hỏng im lặng nguy hơn crash); invariant "form biểu diễn được không" phải server-side không chỉ UI; phân biệt None/`{}`/đủ-key-rỗng ba nghĩa khác nhau. |
| 2026-07-13 | [v34 — Lõi autonomy: checkpointer, interrupt, follow-up, criteria](260713-v34-core-autonomy-checkpointer-interrupt.md) | ✅ 5/5 Done | Team-step graph có checkpointer (attempt mới adopt tiến độ attempt chết, không double-deliver, GC eager+sweep); interrupt() clarify (nháp an toàn → hỏi CEO → pause → resume rework theo answer, expired ship nháp); follow-up sweep 3 bậc SQL-only cooldown 8h; review chấm từng tiêu chí (parse bao dung, room chỉ mang đếm). Review vá 2 HIGH state-machine (dead-end vs waiting_clarify; crash window interrupt→mark) + bug thứ tự update_state. P4 fan-out thiết kế lại trên nền mới (ticker-insert + gather fan-in, 0 LLM call thêm) rồi cook cùng ngày: mint atomic, readiness gate, gather-copy, chặn amend khi dở. 2103 BE + 200 FE. |
| 2026-07-13 | [v33 — Màn Kết nối, hub Kết quả, clarify buttons, tìm lịch sử](260713-v33-connections-output-hub-clarify-history.md) | ✅ Done | Màn Kết nối = UI của .env (catalog cố định, presence-only, restart nói thật); design-system pass (30→2 hard-code, office 1280×800 thấy đủ 5 vùng); hub Kết quả cross-task + download confined + kanban team-task read-only; clarify buttons (target "ceo" trong propose-consult → store first-answer-wins → Telegram inline buttons qua bot admin + web Duyệt, answer inject bước sau, E2E thật); history.search FTS5 + ops search_history có trích nguồn. Review vá HIGH: newline-injection value .env lách whitelist (có từ wizard v7). 2066 BE + 200 FE. |
| 2026-07-13 | [v32 — One-click templates + crew, đại tu office 3D, tổng rà UI/UX](260713-v32-templates-crew-office-ux.md) | ✅ Done | Audit toàn surface (40 screenshot + ma trận capability↔UI) làm input; template card thành executable (spec build server-side, agent ra đời TẮT, crew.yaml 1 crew skip-existing idempotent + coordinator domain-guard); office 3D bỏ wireframe → solid low-poly flat qua gate 3 mockup (Style A, palette 2 theme), panel 70vh→38vh, desk click/tooltip + fallback parity, error-boundary+watchdog chống kẹt "Đang tải"; 4 quick-win (ops commands listing, orphan-profile recovery hint...). 2032 BE + 186 FE; phát hiện dist stale từ v30 (tsc -b vỡ) → vá + quy trình rebuild. Chờ CEO: duyệt style 3D, quyết hr mồ côi. |
| 2026-07-12 | [v31 — Agent-tools capability wave: hậu kiểm fleet + 4 native type + wake-gate + OpenAlex](260712-v31-agent-tools-capability-wave.md) | ✅ Done | Nửa còn thiếu của v30 + đợt công cụ: view/ops-chat "Hoạt động công ty" (audit LLM-tóm-tắt, row bọc untrusted); 4 type native (`schedule_update` self-only closure + cron floor + CEO notice, `team_task_create/move` quyền đối-chiếu-store, `gws_write` bảng 3 prefix cứng — deny cấu trúc = category CỨNG vì autonomous re-entry bỏ qua NOT_ALLOWLISTED); wake-gate watcher hồi sinh v29a (poll 0-LLM đo capture, wake = team-task 1 step pre-built không decompose, hash advance CHỈ sau wake ok); OpenAlex keyless opt-in. Review bắt HIGH trước ship: move planning→open bypass confirm_plan → cấm. 2019 BE + 183 FE; live E2E từng phase. |
| 2026-07-12 | [v30 — Autonomy-first: trust_mode autonomous mặc định, guarded opt-in](260712-v30-autonomy-first-trust-mode.md) | ✅ Done | **Pivot định hướng CEO**: agent mặc định tự chủ (Lớp B/allowlist-miss có handler chạy NGAY, audit hằng số `AUTONOMOUS_RATIONALE`), guarded = hành vi cũ opt-in per-agent (`safety.trust_mode`, yaml thắng `TRUST_MODE` env). Giữ 1 cửa gateway flip policy — single-door + audit + dedup + dry-run + kill-switch + **Lớp A bất khả xâm mọi mode**. Chat flatten (mọi sender reach được → chạy — quyết định tường minh) + fleet-flip khi upgrade + không daily-cap (accepted risk; backstop $2/task+timeout). **Propose-only (handler=None) VẪN queue** — thiếu carve-out này automation proposal bị nuốt im lặng (red-team bắt trước cook). approval_gate nhận settings (None ⇒ guarded fail-safe) wire 3 report graph. Surface: wizard "Chế độ hành động" + agents API effective mode + AgentPage badge + chat reply tự-chủ/dedup/dry-run. Docs 9 file reposition + 5 disclosure. 1894 BE + 180 FE; UAT live 4/4: worker external autonomous chạy thẳng (`auto_approved: true`, 0 pending, Slack+Confluence thật) · guarded PAUSED→resume approve · Lớp A denied 2 mode · fleet-flip hr/admin xác nhận live. Bài học: red-team đảo claim "pin 1 điểm/flip 4 file" (thật 33 file builder trực tiếp); docs-subagent BỊA claim ("self-approved", chèn enum PII) → grep-verify sau mọi docs-delegation. |
| 2026-07-12 | [v28 — Runtime consolidation: migrate tools-tier to `create_agent` + DRY](260712-v28-runtime-consolidation.md) | ✅ Done | Scope THU HẸP sau red-team 4-reviewer (cắt report-path dead-code + giữ 2 kind, bỏ taxonomy churn). 3 việc: **DRY loop core** (`community_loop_core.py` MỚI): `record_loop_result` (post-invoke tail chung: text+`sum_usage_metadata`+`estimate_cost`+`telemetry.record`) + `invoke_capped` (recursion cap + catch `GraphRecursionError`→degrade empty + tracing-off); cố tình KHÔNG gộp agent-build/system-prompt (mỗi tier tự sở hữu — RT-2). **Migrate tools-tier** `langgraph.prebuilt.create_react_agent`→`langchain.agents.create_agent`; shell-tier `deepagents` GIỮ. **Deps** `langchain>=1.3,<1.4` vào base. 1871 BE+178 FE. **Đo lúc cook ĐẢO 2 finding red-team**: RT-1 (recursion mất vòng) SAI — 2 factory parity 8 vòng@limit16; RT-3 (langsmith hồi quy v28) SAI — `langgraph.prebuilt` production đã kéo 28 module langsmith từ trước. **Bug thật lộ khi cook** (kiểm-độc-lập bắt trước reviewer): `callbacks=[]` KHÔNG tắt tracer (CallbackManager tiêm LangChainTracer từ env) → fix `_tracing_off()` env-blank quanh invoke→0 handler. **Code-review SHIP** (acceptance a-i PASS, 3 Low vá). Live UAT 3-engine: create_agent N-round model thật, deep_agent Docker+qwen tạo file teardown sạch, native+kill-switch. Bài học: red-team để BẮT ĐIỂM CẦN ĐO không phải kết luận cuối; test tracing phải assert `tracing_is_enabled()` THẬT không config dict; live-test cap phải dùng cap runtime thật không tự-bịa. |
| 2026-07-12 | [v27 — deep-agent-harden (F1-F7)](260712-v27-deep-agent-harden.md) | ✅ Done | Vá đánh giá bảo mật bên thứ 3 (`docs/research/deep-agent-safety-checklist-v205-v26.md`) — CHỈ deep_agent path. **Sanitize-at-source** (`deep_agent_sanitizer.py`, thay `deep_agent_pii_gate.py` xoá): LLM làm sạch 5 kênh (persona/project/memory/capability/handoff) — handoff là kênh red-team chứng minh rò (dep result + consult SOUL/PROJECT) mà gate cũ bỏ sót. **Network AND-gate fail-closed**: `effective_network=opt_in AND sanitize_ok`, sanitize lỗi→network OFF; network default TẮT + opt-in `sandbox:{network:true}`. **Container harden**: cap_drop=ALL/no-new-priv/nobody/network=HARD (fail-closed nếu reject), mem/pids/read_only/tmpfs(1777)=degradable (loud WARNING); HOME=/work chỉ docker env. **Reaper** (`sandbox_reaper.py`+ticker): sleep 600+auto_remove+label, sweep orphan age>TTL+grace, `_parse_docker_created` truncate nano→micro, docker timeout 5s. F4 doc loop*2+test · F7 price guard isfinite&>=0 (nan/inf/âm→None) · F5 wizard mapping+agent_create · F6 docstring. 1863 BE+178 FE. **Red-team plan 7 findings** (C1 exfil-via-handoff→sanitize redesign, H1 degrade tách privilege/resource, H2 reaper nano CI-green-prod-dead). **Code-review SHIP** (fail-closed 3 đường an toàn); LOW persona-raw đóng bằng sanitize kênh 5. **Live Docker UAT**: docker inspect harden thật + reaper xoá container nano-timestamp THẬT; vấp tmpfs mode=1777 cho nobody+read_only. |
| 2026-07-12 | [v26 — Capture Telemetry + Session Log thống nhất](260712-v26-capture-telemetry.md) | ✅ Done | Port "capture tầng 1" my-dandori: bảng `captures` RIÊNG (`.data/captures.sqlite3`, WAL multi-writer, 1 row/step-ATTEMPT, 17 cột). **Unify cost**: 2 engine langchain (create_agent/deep_agent) trước `cost=None` (ChatOpenAI bypass LlmClient) → SUM `usage_metadata` mọi AIMessage × giá per-model (`model_pricing.py`+`config/model_prices.yaml`) = `cost_source=estimated`; native giữ cost thật = `exact`; model thiếu giá→None, tokens vẫn ghi (không bịa). **Side-channel collector** (`step_telemetry.py`): `run_work` 2-tuple CỨNG → tokens/source đi kwarg phụ, seam thêm param TƯỜNG MINH vào build_team_task_graph+default_team_task_deps (langchain runtime pop; F1). **Hook** tại `run_team_step`: `time.monotonic()` trong worker process (không `spawned_at` cross-process); best-effort log-WARNING. **Remember-node team-step** (rewire deliver→remember→END, `result_text` OUTPUT không chạm PII-gate; `CostedMemoryExtractor` cộng cost extract vào capture → 'exact' trung thực F4). 1842 BE+178 FE. **Red-team plan 7 findings** (F1 seam explicit-param bỏ hand-waving, F2 `external_write` bug tiềm ẩn không viện dẫn precedent, F4/F5/F7 honesty). **Code-review CRITICAL C1**: `_run_review` gọi `telemetry=` nhưng không nhận → TypeError MỌI review step; suite xanh vì 0 test drive review qua run_team_step → fix 2 dòng + regression test. **UAT 3-engine live**: 6 capture rows (native exact, create_agent/deep_agent estimated, review row exact) — worker subprocess re-import nên C1 fix hiệu lực ngay. KHÔNG git-delta/grading/flywheel/UI (YAGNI). |
| 2026-07-14 | [v41 — deep_agent lease + artifact read-back + create_agent docs](260714-v41-deep-agent-lease-artifact-createagent.md) | ✅ Done | Benchmark-lại (sau v40): 0/4 crash write nhưng deep_agent 565-612s đụng trần lease 600s cứng → 2/4 chết. **P1**: `sleep {lease}` (hằng SANDBOX_LEASE_S=1800, cap 3600, config `sandbox:{lease_seconds}`); reaper threshold = max(step-lease 600, sandbox 1800) → không false-reap container hợp lệ. Report gộp 2 lease (worker vs container) — verify code tách riêng, KHÔNG đổi DEFAULT_LEASE_TTL_S. **P2**: read-back `/work/*.md` gắn result trước teardown (best-effort, cap 256KB). **P3**: deployment-guide §6a bật create_agent/deep_agent, native giữ default. Real-Docker E2E: cmd `sleep 1800`. 2242 BE. |
| 2026-07-15 | [v50 — UI catch-up](260715-v50-ui-catchup.md) | ✅ Done | Audit FE lộ backend v40-49 tiến nhưng nhiều capability không có UI. Vá 4 gap + 2 inconsistency: **P1 actor (v46)** cột "Ai thực hiện" + tag "[bởi X]" khi actor≠owner (data đã trên wire, FE drop). **P2 tier badge (v45)** board card `steps_needs_shell` → "🔒 N sandbox" (bước leo deep_agent/Docker nhìn thấy). **P3 per-task cost (v26)** endpoint `/team-tasks/{id}/cost` bọc `list_for_task` (allowlist+tổng) → `TeamTaskCost` lazy-expand. **P4 deep_team (v43)** toggle wizard chỉ deep_agent + agent_create passthrough guarded. **P5** comment dual-mount banner + type drift vá P1. Vấp: card refactor (Link→div+sibling để nút cost ngoài anchor) rơi CSS → title link xanh gạch chân, jsdom không bắt style → review HIGH bắt, vá. Moat nguyên (mọi endpoint GET read-only, deep_team config-only qua create path). Review DONE (1 HIGH CSS fixed). 2344 BE + 201 FE. **UAT browser thật 5/5 PASS** (data+kết nối thật; P1 non-empty actor live 39 row; bài học: restart server pre-UAT). |  Lời khuyên report 3-harness "giảm barrier-to-entry". Scout code: cấu-trúc-config TỰ-CẤP gần hết (registry bootstrap, company degrade, profile default, 8-agent team ship) + máy-móc onboarding ĐÃ có (wizard v17, one-click crew v32, demo-mode) → barrier THẬT hẹp hơn report nghĩ (họ bias CLI-only, tự nhận). Fix = compose + doc + 3 vá, KHÔNG build init/preset/dual-mode mới (research đề = trùng). **`mpm quickstart`** (OpenRouter-only→daily dry-run ép cứng). **`mpm crew init`** (đội THẬT giữ lại, reuse `create_crew` v32 idempotent). **`<CoordinatorHealthBanner/>` lên màn Đội** (DRY reuse, poll `/health/coordinator`, hint-only không auto-spawn daemon từ web). BÁC A3 default-enabled (ship đã đúng; `false` là user-data gitignored v18). Doc §3b. Moat nguyên. Review 0 issue (1 UX nit fixed). 2340 BE + FE tsc/build clean. |  Mục #5 (MCP-overhead) chuỗi "3,2,5 tuần tự". Benchmark: office cross-synth 92s vs Hermes 56s = spawn `node` MCP mỗi `call_tool` vì team-step KHÔNG bọc pool. 3 nhánh anh em (report/inbox/tasks worker.py:142/212/235) đã bọc `_run_with_mcp_pool` sẵn; team-step (worker.py:452) là nhánh DUY NHẤT gọi `run_team_step` trần → `current_pool()` None → spawn-per-call (mcp_adapter.py:124). Fix 1-site: bọc `run_team_step` trong `_run_with_mcp_pool` (TRONG try cũ → failure vẫn `_write_outcome`; phủ cả review vì `_run_review` chạy trong `run_team_step`; sync main thread khớp owner-task). Test seam: stub run_team_step bắt `current_pool()` non-None. Pool v11 P3 nguyên. Moat nguyên (0 exec/host-shell/write mới). Review DONE (0 issue mọi mức). 2334 BE. |
| 2026-07-22 | [v55 — Cockpit viewport shell](260722-v55-office-cockpit-viewport-shell.md) | ✅ Done | UAT: màn Văn phòng phải scroll ~5000px mới giao việc được (grid không cap chiều cao, 39 room + 17 watch-run trùng, Kết quả chôn đáy). CEO chốt **cockpit 1-viewport** (`100dvh` flex, cột scroll riêng, scoped `:has()` — view khác không đổi) + **composer LÊN ĐỈNH** (layout B command bar, dropdown/preview absolute overlay không đẩy 3 cột) + phòng việc **gộp watch-run ×N** (helper thuần) + **lọc [●⚠✓]** (✓ tắt mặc định, search bỏ qua filter, active room force-include) + **tab [Phòng việc\|Kết quả]** (chấm ● khi bàn giao live). UAT 2 vòng đường thật (26 mục, 4 việc LLM $0.0070, đội tự chèn soát chéo). VẤP: 3 bug layout browser-bắt-suite-không (feed không scroll vì wrapper `<aside>` display-block; 3D co vì wrapper lấy r3f intrinsic size; feed 2px vh thấp); **chấm ● sửa SAI 2 lần suite xanh cả hai — chỉ live UAT bắt** → baseline chụp LÚC RENDER key theo room. Frontend-only, 273 FE, 2 commits. |
| 2026-08-01 | [v56 — Playwright smoke + vòng dọn dẹp](260801-v56-playwright-smoke-and-cleanup.md) | ✅ Done | Hướng "Nền vững" (brainstorm định giá lại roadmap sau 0.5.0). **Harness Playwright** (`web/e2e/`): toàn bộ `/api` mock trong browser (1 predicate route — glob `**/api/**` nuốt cả module vite, học lúc cook; endpoint quên mock → abort kêu to), fixtures TS typed với types.ts, **SSE mock qua reconnect thật** (`retry:100` + replay, dedup-by-seq giữ UI ổn định) → **8 smoke test đo DOM** cho layout v55 (page-no-scroll, scroll trong khung, composer luôn thấy, overlay không đẩy grid, ×17, filter/search, **chấm ● live** — bug sửa 3 lần v55 giờ có chốt chặn, mobile stack), retries:0 không flaky, CI job secret-free. **Quick wins**: nhắc web_search thiếu key ngay preview giao việc (không auto-tắt flag) · focus-trap + `HTTP status — detail` artifact viewer (GET parse detail như write) · GC artifact mồ côi guard kép. VẤP ĐẮT (review bắt H1): sweep quét `.data/team-tasks/` — path không writer nào ghi, copy từ audit v36 vốn bug y hệt (audit im lặng "clean" từ v36); test xanh vì monkeypatch đúng path bịa = **phantom coverage** → helper chung `team_task_artifacts_root()`. Vitest nhặt nhầm e2e specs (count vẫn 279 passed, chỉ lộ ở "Test Files 2 failed") → exclude. Extra `deep` bốc hơi vì `uv sync` trần → khôi phục. **UAT đường thật sau commit bắt bug thứ 4 chấm ●**: dot sáng từ history toàn cảnh — sentinel `{room:null}` trùng + SSE replay bất đồng bộ đánh bại baseline chụp-lúc-render (jsdom đồng bộ → 3 fix trước xanh giả) → fix "live" = **ts sự kiện** > lúc mở phòng, toàn cảnh tắt dot; verify lại browser thật + hint web_search trên preview LLM thật (@nghien-cuu, huỷ + dọn row). 2392 BE + 280 FE + 8 e2e. |
| 2026-08-03 | [v57 — Thư ký riêng CEO (personal-pack)](260803-v57-personal-secretary.md) | ✅ Done (5/5) | **Domain pack thứ 5 `personal`** — thư ký riêng trên Telegram (tham chiếu Pong/openclaw), "full-ga trong khung": autonomous + dry_run off, GIỮ Lớp A. Chat DM **tức thì** (`telegram_listener`: long-poll peek 45s → spawn worker inbox, tick lịch làm fallback, rate-cap chống flood) · Morning Briefing 7:00 + Weekly CN 8:00 (2 kind pack tự sở hữu — không sửa core) · đọc Gmail/Calendar qua `gws` CLI (degrade nói-thật khi lỗi) · lệnh ghi `tao_lich` (catalog gws_write, argv cố định) · **chat có trí nhớ**: daily notes 7 ngày + mirror MEMORY.md (trước đó đường chat quên sạch sau mỗi reply). VẤP: `{} or None` hồi sinh allowlist mặc định trên pack allowlist-rỗng (reviewer bắt, office-pack đã cảnh báo đúng bẫy) · **phantom coverage lần 2** — argv calendar v39 thiếu `calendarId`, chưa từng chạy nổi với Google thật mà test vẫn xanh (UAT thật mới lộ) · classifier thiếu mốc BÂY GIỜ nên bịa ngày + tạo lịch quá tay từ lời dặn · `**` lọt Telegram dù prompt cấm → strip cấu trúc. P4 web-search xong không mua key (Brave + Firecrawl self-host dùng chung hạ tầng openclaw; chat 2-pass marker `WEB_SEARCH:`). 2423 BE. |
| 2026-08-04 | [v57–v66 shipped as 0.7.0](260804-v57-v66-shipped-as-0.7.0.md) | ✅ Done | Release 0.7.0 lên PyPI (tag → OIDC). Vòng monkey-test cuối: 0 lỗ hổng ("in .env" bị sandbox chứng minh contained — tmpfs rỗng/không mount/network off), 4 fix (reminder synonym misroute, đổi-ý giữa-collect qua `new_intent`, preview stalled validate, coerce int→str). Dọn data test + đồng bộ 14 file docs (kể cả nợ `src/`→`my_crew/` từ v51). Học đắt: prompt-position là behavior — rule đổi-ý ở CUỐI prompt chết, làm BƯỚC 1 mới sống, chỉ stack thật lộ. 2530 BE + 279 FE + 8 e2e. |
| 2026-08-05 | [v67–v68 — Vòng "tự chủ có kỷ luật"](260805-v67-v68-disciplined-autonomy-arc.md) | ✅ Done | Arc 4 vòng học từ điều tra source Hermes + OpenClaw, red-team 10 finding trước khi cook. **P1** tách delivery khỏi execution (chạy xong ≠ CEO đã thấy — "never silently fail" thành hợp đồng truy vấn được). **P2 Lớp B học dần**: `approval_rule_store.py` per-agent, rule key derive từ `_MUTATING_TYPES` (không bịa taxonomy), `params_hash` bind bắt buộc cho kind hướng ngoài; deny rule CHỈ áp `guarded` (CEO chốt sau red-team), CRUD chỉ CLI/web — chat hoãn 2b. **P3 nhịp thư ký**: pseudo-kind `secretary-heartbeat` + cron riêng, `heartbeat.every` đặt ở profile.yaml (company.yaml bị `save_company` dựng lại từ dict cứng, key viết tay sẽ mất); digest chỉ tín hiệu THẬT, im lặng thì miễn phí; UAT Telegram thật. **P4 reflection**: task terminal → 1 lượt LLM nhỏ chưng cất bài học về CÁCH GIAO VIỆC vào `(coordinator_id,"memory")` — namespace của coordinator vì nó là agent giao việc, ghi vào worker sẽ phá WO-self; marker cooldown ra namespace RIÊNG `"reflected"` (marker ghi mỗi lần/lesson thì hiếm → trộn chung sẽ chôn fact thật với 3 chỗ đọc, và sweep 90d xoá marker mở lại đường trả tiền lần hai); `_reflect_safely` nuốt mọi exception vì `run_one_tick` không có except riêng. VẤP chuỗi 3 nhịp ở guardrail: chặn TOOL nhưng quên chặn NGƯỜI ("đừng bao giờ giao cho agent-b" = lệnh cấm tuyển dụng vĩnh viễn từ 1 stall) → vá; phụ thuộc DẤU tiếng Việt (bản không dấu lọt thẳng vào memory bền) → `_fold()`; rồi chính cú fold đẻ bug ngược — `đừng`/`dùng` trùng token `dung` làm pattern chặn nhầm "nên dùng web_search", tức chặn đúng loại lesson sinh ra để giữ. Review đưa 1 finding SAI cơ chế (thử 60 marker ở limit 10/20/40, lesson vẫn còn) nhưng đúng kết luận qua đường khác → đo trước khi tin. 2692 BE, ruff sạch. |
| 2026-08-05 | [v69 — Bề mặt chat cho approval](260805-v69-chat-approval-surface.md) | ✅ Done · **0.8.0** | Đóng đúng mục "mở/sang sau" của v67–v68: 0.7.0 cho AI tự duyệt, vòng này làm phần CÒN LẠI của con người **với tới được**. **P1 push**: enqueue Lớp B → DM ngay (id + agent + 1 dòng nhận diện), đi qua `notify_operator_best_effort` chứ không nối dây từng gateway (~20 chỗ dựng `ActionGateway`, nối tay sẽ sót đúng agent hay queue nhất); `approval_summary` chỉ render trường ĐỊNH DANH, không bao giờ subject/body, gộp newline chống vẽ dòng giả. **P2 duyệt/từ chối + rule từ chat**: bề mặt THỨ BA trên cùng đường gateway (không phải đường duyệt thứ hai), admin-only vì thò tay vào store agent khác = thẩm quyền fleet; cặp `(agent_id, approval_id)` **bind ở PREVIEW không resolve lại** — push chen giữa 2 bước không dời được đích (bài học v64 H1); rule always/deny mô tả bằng LỜI dịch từ binding thật, không phải `params_hash` (đồng ý với digest mù là đồng ý mù), action không summarize nổi thì TỪ CHỐI tạo rule. **Sửa `reject` thành CAS cả 3 bề mặt** — reject mù có thể đè lên row bề mặt khác vừa approve+chạy xong, store ghi "rejected" cho hành động ĐÃ CHẠY rồi dạy luôn deny rule từ quyết định ma; `ApprovalStore` sang WAL + busy_timeout 30s (3 writer 1 hàng đợi). **P3 digest tín hiệu 6**: mọi approval pending mọi agent, KHÔNG ngưỡng tuổi (draft cũ là CEO tự bỏ, approval treo là agent ĐỨNG IM) + `list_lessons` (v68 ghi bài học mà chưa ai đọc lại được) + số lần hồi sinh trong `list_team_tasks`. VẤP: **UAT thật bắt lỗi 2778 test xanh không thấy** — nhịp heartbeat đầu với 1 approval treo trả `suppressed`, model trả token ack nuốt đúng tin BẮT BUỘC phải tới (test kiểm digest, không kiểm quyết định im lặng của model) → miễn suppression cho key `approval:`, nới đúng tiền lệ `scratch:`. **Review bắt lỗi VĨNH VIỄN mà cả CI lẫn fleet thật đều không gặp**: `read_pending_actions` raise khi `approvals.db` chỉ có bảng `approval_rules` (`ApprovalRuleStore` tạo cùng file) → 1 agent như vậy giết tín hiệu approval TOÀN fleet mọi nhịp; docstring lúc viết cân nhắc đúng đánh đổi nhưng cho *blip tạm thời*, lập luận sụp khi trạng thái VĨNH VIỄN → hỏi "trạng thái xấu này tự khỏi được không"; sửa ở NGUỒN vì reader đỡ cả digest lẫn lệnh chat. Plan ghi sai path (`agent/` vs `runtime/`) → grep symbol, đừng tin đường dẫn. 2781 BE, ruff sạch, tag v0.8.0. |
| 2026-08-15 | [v79 — Model 3 tầng + phanh trần chi phí + cổng release 0.10.0](260815-v79-release-gate-and-hardening.md) | ✅ Done | Model config 3 tầng fleet→agent→role (`role_models`), fleet mặc định sang `deepseek/deepseek-v4-pro-0813`. **Phanh in-flight** cho trần chi phí (halt bước đang chạy khi chạm trần + reap sweep khi cancel) — trả nợ vấp A9 v78 "cancel không phải phanh". Harness **fullflow in-process** 8 kịch bản người-dùng-thật, mutation-verified (`docs/fullflow-testing-guide.md`). Chuỗi fix giao việc: bản giao terminal nguyên văn không cắt 500 ký tự; review truy số liệu về đầu vào thay vì chấm mù; hết flood Telegram sau hoàn thành; verdict đạt trả nguyên bản; rework thừa kế quyền web. Cổng release 0.10.0: delta-UAT sống 4/4 hành vi, 2 phát hiện cosmetic (boilerplate PHẦN THIẾU trên bước không cần web; acceptance không đòi độ tươi dữ liệu) sửa xong TRƯỚC phát hành rồi re-verify sống — bản giao sạch. Vấp đáng nhớ: venv mang shebang repo-tên-cũ làm pytest chết 86 collection error trong khi `python -c import` chạy ngon — chữ ký của entry-point cũ, không phải thiếu package. 3257 BE + 282 FE + 8 e2e xanh. |
| 2026-08-16 | [v80 — Quan sát bước làm việc kiểu pi-sessions](260816-v80-step-observability-pi-sessions.md) | ✅ Done | Đem cơ chế session-file của pi vào team-step: mỗi attempt ghi **transcript JSONL per-attempt** (3 hook: `LlmClient.complete` · community loop · prefetch; scrub secret khi ghi, best-effort không bao giờ giết step) + work-order artifact + CLI `step-replay` chạy lại một bước ngoài pipeline. Bốn consumer trên cùng dữ liệu: **review chấm bằng chứng quá trình** (tool đã gọi + nguồn đã mở + usage, cap 8000 — trả nợ v72 "bảng giá bịa well-formed"); **office feed thấy hoạt động sống** (`step_activity` allowlist CỨNG trong code `{agent,task,step,tool,count,phase}` — PII invariant giữ bằng cấu trúc, không phải filter); **reflection nhận behavior summary CHỈ tên tool + số đếm** (threat model: output reflection vào memory bền mọi sibling đọc — không nhúng args/kết quả/query attacker ảnh hưởng được); **bench phân rã usage per-step** từ transcript, ledger vẫn là nguồn sự thật kế toán. Fullflow scenario mới soi cả 4 mặt trên MỘT task thật — và lộ **bug harness có sẵn ngủ từ v79**: module import GIỮA test (worker) giữ double của test trước, teardown không biết attr đó → test sau chạy với cast/data_dir của test trước; fix marker `_fullflow_double` cho sweep nhận cả double sót. Vấp thứ hai: refactor `_parse_events` để lại nhánh `parsed_any` mồ côi — NameError chỉ lộ khi chạy suite CŨ ngay sau refactor. ScriptedLlm tự ghi `record_event` vì double phải trung thực với contract mới của seam. 3320 BE (9 fullflow) + 284 FE, tsc/ruff sạch. |
| 2026-08-16 | [v81 — Sprint entity-lookup v2 + release bench 4 trục](260816-v81-sprint-entity-lookup-release-bench.md) | ✅ Done | Parser sprint goal v2: prose-list sau "của", goal deliverable-head, governor back-off "gói cá nhân", list mất "và" (floor 3 tên). Bench release chuẩn hoá (`release_bench.py`, 8 case — 2 case mới đóng băng từ intake-rephrase live verbatim, 3 lặp determinism). Đo thật vs v0.10.0 cùng brief C3: baseline **stall 2 lần** ($0.0915 mất trắng, 1 query kitchen-sink) vs candidate **done 5.6 phút sprint, $0.0544, 5 query trúng đích**; offline 8/8 đóng coverage (baseline 4/6). Bằng chứng đẹp nhất: **A/B tự nhiên trong 1 task** — attempt-1 spawn trước fix chạy code cũ fail, attempt-2 worker mới import code mới done, cùng brief cùng model. Vấp: transcript v80 ghi vào data-dir CỦA AGENT không phải root (`lsof` mới lộ); verify parser phải bằng goal live verbatim từ DB — paraphrase từ trí nhớ cho kết quả khác hẳn. Kèm Task B: design redesign web + fix artifact panel nhận step sprint + feed filter giữ `step_activity`. |
| 2026-08-16 | [v82 — Web redesign: perf data layer + sprint surfaces lên UI](260816-v82-web-redesign-sprint-surfaces.md) | ✅ Done | Trả lời phàn nàn "vào màn hình chờ lâu": `fetchCached` TTL+dedupe, SSE resume theo seq, paginate board, **three.js rời main bundle** (`index` 1628KB → 764KB, -53%, chunk lazy `agent-desk` chỉ tải khi mở góc 3D) + 3 bug fix (ArtifactPanel lọc mất step sprint; `step_activity` vắng feed; `external_action` dòng rỗng). **4 surface đem v81 lên web**: badge SPRINT/TEAM trong preview giao việc; `GET /team-tasks/{id}/route` → dòng "Đường đi: SPRINT — vì sao" trong expand thẻ kanban (allowlist `mode/source/reason`, giấu `signals`); tab **"Quá trình"** trong ArtifactViewer (`/office/tasks/{id}/steps/{seq}/transcript` đọc JSONL v80 theo data-dir CỦA AGENT làm bước, attempt mới nhất theo mtime, high ui-mode only, không bao giờ inline prompt); `GET /team-tasks/{id}/metrics` bọc bench lib store-only → "⏱ 3m19s · N bước". Mọi fetch mới đều lazy trong expand — không fan-out per-card, đúng bài học gốc. Quyết định lệch design: transcript route đặt cạnh route artifact (`/api/office/...`) thay vì `/api/team-tasks/...` — FE surface nhất quán thắng sơ đồ giấy; `_parse_events` → public `parse_transcript_events` thay vì import private xuyên module. Vấp: thêm `useUiMode()` vào component cũ làm 5 test render thiếu provider nổ; jsdom suite này không có `localStorage` thật — stub theo mẫu `ui-mode-context.test.tsx`. **3357 BE + 297 FE + 10 e2e** (2 smoke mới: badge sprint, tab transcript), ruff/tsc/build sạch. Chốt cùng ngày: **retire block "Việc đã giao"** trên trang Việc (trùng kanban team-task) — xoá Tasks.tsx + i18n/CSS/types/api client; backend `/api/tasks` giữ cho API/CLI. |
| 2026-08-17 | [v83 — Rà "no sub-agent": benchmark live 5 run vs v0.10.0](260817-v83-no-subagent-live-benchmark-vs-v0100.md) | ✅ Done | Arc "no sub-agent" đóng bằng bằng chứng sống: 4 fullflow test failure-path + fix ⛔ delivery-head (task stalled luôn bàn giao "KHÔNG LÀM ĐƯỢC + lý do" — verify sống 2 lần). Benchmark 5 run cùng brief vs v0.10.0, blind judge 2 vòng đảo nhãn: baseline thắng cả 2 (26-19, 27-21.5) — thua lặp lại ở trục **nguồn** (rework deep-loop lấy nguồn thứ cấp vs trang chủ + ngày truy cập) → tật hệ thống của prompt rework, không phải nhiễu. 3 fix từ run sống: `_proper_noun_items` hết nuốt danh sách entity trong brief prose (prefetch 1 query kitchen-sink → 5 query/entity); gate reassign chỉ tin khai báo `needs_web` (R3: step không-cần-web kẹt vì người giữ CÓ web, task ~90% bị kết thất bại); rework kế thừa `needs_web`. R4 lộ failure mode phương sai: reviewer in-attempt đòi dữ liệu prefetch không có → model xóa draft thành vỏ rỗng → chết trước đường rework-có-search (R5 cùng điều kiện thì done, $0.128/879s). 3370 BE xanh, ruff sạch. |
| 2026-08-17 | [v84 — Sprint đọc trang chính thức (fetch) thay vì chỉ snippet](260817-v84-sprint-official-page-fetch.md) | ⚠️ Đã đo — arc BỊ BÁC, chưa release | Trục `nguon` thua baseline 3/3 vòng blind judge, và v83 đã sửa prompt chuẩn nguồn mà **model tuân đúng nhưng điểm không lên** ⇒ nút thắt ở tầng THU THẬP, không phải chỉ dẫn. Đọc code xác nhận: sprint thu thập DUY NHẤT qua `prefetch_queries` → `web_search_tool`, mà tool đó **cố ý snippets-only** ("NEVER a follow-up GET to any result URL"), và sprint pipeline không có tool loop — nên trên đường sprint **không tồn tại đường nào** dẫn tới việc mở một trang web; snippet trang chủ hiếm khi chứa con số giá nên model buộc rơi xuống đại lý/blog, đúng thứ judge trừ điểm. Phát hiện thu hẹp phạm vi: năng lực fetch **ĐÃ CÓ** (`firecrawl_tool` production-grade, SSRF guard tại nguồn, đã nối vào tool-calling runtime là `web.scrape`) — việc cần làm chỉ là nối nó vào đường sprint, nơi duy nhất còn mù. Ship: `official_page_pick` chọn URL trang chủ **bằng code thuần** (đọc lại URL đã nằm sẵn trong bundle từ v73 — 0 search thêm, 0 LLM call thêm; sprint code-paced là lý do có mode này nên không đánh đổi bằng một call chọn link) + `official_page_fetch` scrape từng trang độc lập qua đúng lớp bọc `format_search_results` (trần 3 trang × 8000 ký tự — ràng buộc là **lease** không phải context: 5 × 60s timeout = 300s trong một node trên lease 600s) + vòng fetch chèn giữa prefetch và draft (TRƯỚC draft, để bản nháp ĐẦU đã có nguồn chính thức, không phải vá nguồn sau khi đã viết bằng nguồn kém) + `record_event({"t":"fetch"})` kèm trường `skipped` nêu LÝ DO không fetch, nối vào cả 3 consumer transcript (office feed `web-fetch` · review evidence thấy URL đã mở · reflection chỉ số đếm, giữ đúng threat model v80). Fail-open tuyệt đối: fetch lỗi trả `""` **không** trả sentinel THIẾU — bất đối xứng có chủ ý với vòng search (search hỏng = "không được nhìn"; fetch hỏng = "không có thêm", vì snippet bundle vẫn nguyên, bắn sentinel ở đây là nói dối người đọc). **Vấp lớn nhất tự tìm ra**: khớp host bằng substring là lỗ **bảo mật**, không chỉ lỗi chất lượng nguồn — URL được chọn thì SẼ BỊ FETCH mà kết quả search là nội dung kẻ khác tác động được; dò tay thấy `spotify.com.evil.tld` / `notspotify.com` / `my-spotify-hack.ru` / `spotify.evil.com` đều lọt ⇒ sửa sang khớp **đúng nhãn registrable domain** (bỏ public suffix 2 phần) + chặn aggregator theo nhãn nguyên (nên `amazonia-music.vn` không bị quét oan là Amazon). Siết xong lộ hồi quy ngược chiều: `Apple Music` không còn khớp `apple.com` mà 2/5 entity trong brief đúng dạng đó, và `music.youtube.com` bị chặn oan — học: mỗi lần siết matcher phải chạy lại ma trận CẢ HAI phía, không chỉ phía vừa sửa. **Bài học đo lường**: đọc transcript run xác nhận 1 thấy hai bước rework gọi `web_search` 21 và 27 lượt, `web.scrape` **0 lượt** (grep cả task không có chữ đó — tool chưa từng được mời vì sandbox không có key Firecrawl); đồng thời sửa một chẩn đoán của chính mình: rework chạy **tool-calling loop** chứ không chạy sprint pipeline, nên fix rework-search của v83 tới giờ vẫn chưa bắn live lần nào. Trong kết quả rework-0 có sẵn `www.spotify.com` / `www.apple.com` / `www.nhaccuatui.com` — đúng các trang cần, nhưng chỉ ở dạng snippet: chính dữ liệu run xác nhận chẩn đoán gốc rễ. **Hai phát hiện nặng từ vòng review, cả hai đã tự tái hiện trước khi sửa**: (a) **bundle plain-text đang là control plane** — `sprint_runner` đọc chính marker của mình NGƯỢC ra khỏi bundle làm tín hiệu điều khiển (`_source_refused` · `_has_results` · `missing_note` · gate `_NO_CAPABILITY`), marker được tin chỉ vì nó *xuất hiện*, không có gì ghi ai viết nó; sống được khi mọi byte đến từ snippet provider, nhưng vòng này gộp cả THÂN TRANG nên một dòng giả trong trang scrape làm pipeline tự khai "nguồn tìm kiếm gặp lỗi" khi search thật ra thành công — đo được: prefetch 2 vòng → 1 (mất vòng search bù gap) và note cho CEO đổi từ "đã tìm nhưng không đủ kết quả dùng được" thành lời cáo lỗi SAI SỰ THẬT. Học đắt nhất arc: **spotlight của `format_search_results` không che được lớp này** — nó chặn injection nhắm vào MODEL, còn marker nhắm vào CODE (`quarantined=0` với text chứa nguyên dòng sentinel); sửa bằng `_strip_control_markers` tại **biên nhập** thay vì dạy 4 reader nghi ngờ đầu vào (một chỗ chặn kiểm được bằng test, 4 chỗ nghi ngờ là 4 chỗ có thể quên khi thêm reader thứ 5). (b) **cửa sổ không heartbeat**: vòng fetch nằm gọn giữa `_beat("sprint_prefetch")` và `_beat("sprint_draft")` ⇒ tới 300s im lặng trên lease 600s (DNS không có timeout), watchdog đòi lại bước ⇒ worker thứ hai chạy lại ⇒ tốn LLM hai lần và bản ghi attempt này bị coi là stale; sửa bằng trần 3 trang + `on_beat` mỗi trang. Kèm 2 lỗ picker (TLD abuse kiểu `spotify.xyz` — luật nhãn registrable KHÔNG hỏi được "đây có phải TLD thật của brand"; và 2 entity cùng site ăn 2 suất trang ⇒ dedup theo HOST). **Test xanh chưa chắc test có kiểm gì**: `test_a_reseller_url_in_the_bundle_is_never_fetched` vẫn XANH khi tắt hẳn `_is_aggregator` (riêng luật khớp nhãn đã từ chối `www.amazon.com` cho "Spotify") ⇒ viết lại, và mutation-test cả hai guard mới (tắt đi phải ĐỎ, bật lại XANH). **Mục thứ 7, do RUN sống lộ ra và test tất định KHÔNG bắt được**: transcript run 3 có HAI attempt trên cùng một bước, ghi hai dạng event `fetch` khác nhau (attempt 1 code cũ: `skipped=None, bytes=0, urls=[4 URL]` — đúng dạng mơ hồ mà #3 muốn bỏ; attempt 2 code mới: `skipped='no-firecrawl', urls=[]`). Nhìn dạng attempt 1 mới thấy #3 còn hở đúng một nhánh: Firecrawl CÓ cấu hình + URL ĐÃ chọn nhưng mọi trang fail (block/404/timeout) ⇒ `fetched=""` ⇒ event cuối vẫn ghi `bytes:0` với `urls` đầy và KHÔNG có `skipped` — sự mơ hồ sống sót trên nhánh duy nhất cần Firecrawl sống mới chạm tới; 3 nhánh cũ đều `return ""` trước dòng đó nên test tất định mù. Sửa: `skipped="all-pages-failed"`, **giữ** `urls` (biết trang NÀO fail là giá trị chẩn đoán, khác nhánh no-capability vốn không có gì để chẩn đoán); mutation-verified ĐỎ khi tắt. Học: nhánh chỉ tồn tại khi phụ thuộc ngoài CÓ THẬT thì test tất định không thấy — phải đọc transcript sống. **Bằng chứng sống ĐẦU TIÊN của arc lại là của PICKER**: trên bundle prefetch thật của brief C3, `pick_official_urls` chọn đúng 4 trang chủ chính thức (`spotify.com/vn-vi/free/` · `apple.com/apple-music/` · `zingmp3.vn/` · `nhaccuatui.com/`), gồm cả ca `Apple Music → apple.com` ⇒ luật bỏ từ sản phẩm chung chạy đúng trên dữ liệu thật, không chỉ trong test — nhưng vẫn KHÔNG phải bằng chứng cho trục `nguon` vì chưa fetch nổi trang nào. 3443 BE / 9 skipped (trước arc 3384), ruff sạch. **CHƯA đo được trục `nguon`**: Docker daemon tắt ⇒ không dựng nổi Firecrawl, `.env` sandbox không có key ⇒ verify tách 2 mức, mức tất định xong, mức sống còn treo — không ghi "đã cải thiện" khi chưa có số. Chưa phát hành theo yêu cầu. **3 run xác nhận: 2 done / 1 stalled** (không phải 3/3; baseline v0.10.0 cùng brief stall 2 lần nên CÓ tiến bộ, nhưng n=3 và còn 1 lần stall ⇒ chưa được ghi "đã hết stall"). Phương sai nằm ở vòng review chứ không ở bước sprint: cost bước sprint gần bằng nhau ($0.0456 vs $0.0707) trong khi tổng số vòng lệch 3× (6 bước vs 2 bước), và run 1 rẻ hơn nhưng CHẬM hơn 1.6× — mỗi vòng rework-review thêm wall-clock nhiều hơn tiền (khớp v78). Run 3 truy tới cùng và **vô can với arc fetch** (attempt 2 ghi `skipped=no-firecrawl` ⇒ bundle giữ nguyên đúng đường cũ): cả 2 attempt đi hết 3 vòng review rồi cùng ra `needs_decision` → `stuck_decision` chọn đổi người sang `analyst` → `_can_do_step` từ chối (step `needs_web=1`, analyst không có `web_search`) → `_give_up` ⇒ step `failed`, task `stalled`, vẫn deliver một `final_summary` nói rõ "KHÔNG LÀM ĐƯỢC vì X" (nên `delivery_status=delivered` dù stalled), tốn $0.1676 ≈ 2× một run thành công mà không giao được gì. Gate từ chối là ĐÚNG thiết kế (thà kết luận trung thực hơn quay bước web giữa các agent đều thiếu cùng một tool, hoặc để analyst bịa số), nhưng nó bộc lộ lỗ thật ở TẦNG TRÊN: với brief `needs_web` mà chỉ `researcher` có tool, "đổi người" không bao giờ là lối ra — can thiệp cuối đáng ra phải `retry_with_guidance`; học: nhánh quyết định nào phụ thuộc vào việc TỒN TẠI một ứng viên đủ điều kiện thì phải kiểm sự tồn tại đó TRƯỚC khi chọn nhánh, không để nhánh chết ở gate rồi mất luôn lượt (ghi làm việc riêng, chưa sửa trong diff này). Còn nợ có chủ ý: tẩy marker là fix ĐỦ cho vòng này nhưng chưa sửa gốc — tín hiệu điều khiển vẫn đi cùng kênh với dữ liệu không tin cậy (fix cấu trúc: `prefetch_queries` trả `(bundle, status)` thay vì nhét sentinel vào text) — và diff còn chở `listed_entities` / `_proper_noun_items` / `retry_round` — **nay đã review xong** (2 lần delegate agent đều chết API 529 nên tự review): truy nguồn thấy entity list đến DUY NHẤT từ `goal`+`acceptance` do operator viết, kết quả search không đi vào đó, nên là lỗi chất lượng nguồn **không phải lỗ bảo mật** — 3 thay đổi sạch về correctness (modulo `retry_round` an toàn mọi lượt; guard over/under-capture còn nguyên; entity list dài không phình fetch budget vì `limit=MAX_FETCH_PAGES` chặn ở đầu ra). **Cập nhật sau khi user bật Docker — vòng fetch bắn thật lần đầu**: Firecrawl self-host sống trên :3002, run `e905215baf03` **done + delivered $0.147, đạt review ngay round 0** (các run xác nhận trước cần 3 vòng và 2/3 lần stall), event `fetch` ghi `bytes: 18079` không còn `skipped`, nội dung vào đúng prompt draft `role=content seq=7` bọc đúng `[EXTERNAL_DATA source=www.spotify.com rank=1]`, và báo cáo giao ra dẫn TOÀN nguồn official (spotify.com · apple.com · zingmp3.vn · nhaccuatui.com), **không reseller nào**. **Nhưng lộ ngay bài học mới: chọn đúng NHÀ không đủ, phải đúng TRANG** — trong chính báo cáo đã giao chỉ có **2 con số giá cho 5 dịch vụ**, vì picker lấy trang khớp host ĐẦU TIÊN theo rank rồi `break`, mà bundle có sẵn 4 trang spotify: nó chọn `/vn-vi/free/` (1785 ký tự, **0** token giá) và bỏ qua `/vn-vi/premium/` (6651 ký tự, **27** token giá — "33.000 ₫ cho 3 tháng, sau đó là 65.000 ₫/tháng") nằm cùng bundle. Vòng fetch chạy đúng thiết kế và mang về 0 token giá. Học: **rank của provider trả lời "trang nào liên quan tới CÂU TRUY VẤN", không trả lời "trang nào chứa CON SỐ"** — hai câu hỏi khác nhau, và vòng fetch chỉ có giá trị khi trả lời câu thứ hai; fetch được một trang official là điều kiện CẦN, không phải điều kiện ĐỦ. Sửa: `_pricing_affinity` xếp hạng giữa các trang của **cùng một host đã được nhận là official** (+1 path có `premium/pricing/gia/goi/...`, −1 có `free/signup/download/...`, 0 trung tính), rank giữ nguyên làm tie-break, và vì chạy SAU `_is_official_host` nên **không bao giờ** đổi sang host khác (test pin: lookalike có path giá đẹp vẫn thua trang trơn của host thật). Mutation-verified 2 chiều (tắt điểm affinity ⇒ 3 ĐỎ; bỏ riêng nhánh −1 ⇒ 1 ĐỎ), 38 test passed, và **xác nhận SỐNG**: run F2 spawn sau khi fix vào working tree fetch đúng `/vn-vi/premium/`, bytes 18079 → **22945** trên cùng brief. Vẫn chưa chạy blind judge trục `nguon` trên tập có Firecrawl ⇒ **chưa** được ghi "trục `nguon` cải thiện N điểm", và F1/F2 khác code base nên không phải 2 mẫu cùng phiên bản. Lỗ `reassign`-khi-không-có-ứng-viên **đã sửa**: nhánh bất khả thi degrade thành `retry_with_guidance` (giữ bước cho người đang giữ — người duy nhất đủ tool) thay vì `_give_up`, kèm `escalate` nêu đúng tên năng lực thiếu vì "đổi người không được" không cho CEO hành động gì còn "X thiếu công cụ" chỉ ra đúng hai lựa chọn thật (cấp thêm tool hay chấp nhận thiếu); trần can thiệp vẫn kết luận task khi hết lượt thật. TDD: test ĐỎ tái hiện đúng stall của run 3 → XANH, và **một test CÓ SẴN bắt được hồi quy do chính bản sửa gây ra** (escalation mất chữ "công cụ") — học lần hai: test cũ là thứ bảo vệ mình khỏi bản sửa của chính mình. 51 test xanh. Còn nợ: `retry_round > 0` vẫn chưa bắn trên run sống nào (F1 đạt ngay round 0 nên không có retry). **Cập nhật cuối — blind judge đã chạy và BÁC giả thuyết của arc**: 2 lượt đổi nhãn A/B (loại thiên lệch vị trí, cả 2 lượt cùng kết luận), candidate thua đậm. Bản đưa ra chấm là **F1** (`cand-f1.md` 4609 B, đối chiếu md5 với `mapping-round*.txt`) — **F2 chưa từng được chấm**; qua 3 lượt trục `nguon` F1 **5 / 6.5 / 7** vs baseline **8 / 9 / 9** (bản ghi đầu viết "3-4 vs 9" và ngầm hiểu là F2 — cả hai đều sai, kết luận "candidate thua" không đổi). Nhưng KHÔNG nhận điểm theo lời judge: mở đúng những URL mà baseline tự dẫn thì thấy nó thắng bằng **số BỊA** — ghi cả 5 dịch vụ đều "59.000đ" kèm URL official + "truy cập 26/04/2026" (4 tháng TRƯỚC ngày chạy), trong khi trang thật trả **65.000₫** cho cả Spotify và YouTube Music, còn `zingmp3.vn/vip` + `nhaccuatui.com/vip` là SPA render JS nên Firecrawl chỉ nhận 909/260 ký tự với **0 token giá**; candidate ghi 65.000₫ = đúng trang thật. Truy tới gốc: `59.000` chỉ tồn tại trong `result_text` của artifact, **không** trong bất kỳ bundle đầu vào nào ⇒ model tự nghĩ ra con số, tự cho 5 dịch vụ CÙNG một giá, gắn URL official thật lên đó — và **peer review `passed=true` ngay round 0** dù luật chống bịa ĐÃ có trong prompt (`team_task_prompt.py:236-243`: "đối chiếu mọi số liệu … với đầu vào"). Đây là phát hiện nặng nhất của arc và nó nằm NGOÀI phạm vi arc. **Đồng thời công của vòng fetch nhỏ hơn giả thuyết**: đếm giá phân biệt theo từng version artifact của F2 thấy sprint (đã có fix picker, fetch `/premium/`) ra **3** mức giá → rework-0 ra 6 → rework-1 ra 7 rồi review pass; 2 bước rework dùng **tool loop có sẵn từ trước arc** (46 tool call: 34 `web_search` + 6 `web_scrape`) ⇒ vòng fetch góp **3/7**, không phải toàn bộ. Câu mở plan "trên đường sprint không tồn tại đường nào dẫn tới việc mở một trang web" đúng với bước `sprint` nhưng **bỏ sót `rework`**, mà brief dạng này gần như luôn sinh rework — học: khi kết luận "không tồn tại đường nào" phải kiểm mọi LOẠI bước của pipeline, không chỉ bước đang sửa. F2 cũng có lỗi riêng: gán nhãn "trang chính thức" cho số lấy từ nguồn thứ cấp (khác bịa vì số CÓ thật trong đầu vào, nhưng nhãn nguồn chính là thứ trục `nguon` cho điểm). **Bài học tổng quát của arc: verify cái THƯỚC trước khi tin con số nó cho** — đọc riêng điểm judge ⇒ "arc thất bại"; đọc riêng bytes fetch ⇒ "arc thành công"; cả hai đều sai, và một thước đo do LLM chấm mà không truy được về dữ liệu thật thì thưởng HÌNH THỨC (bảng kín, citation trông đẹp) chứ không thưởng SỰ THẬT — thưởng mạnh nhất đúng cái ta muốn diệt. ⇒ **Không release**, và việc tiếp theo không phải fetch thêm mà là: sửa judge để đối chiếu số với trang thật; truy vì sao peer review trượt số bịa; buộc nhãn "chính thức" truy được về URL đã thật sự mở — **cả 3 làm ở v85, và chẩn đoán "peer review trượt" được sửa lại ở đó: luật chống bịa CÓ điều kiện ("nếu có khối ĐẦU VÀO") mà bước sprint `deps_json=[]` nên handoff rỗng bị lọc khỏi prompt ⇒ reviewer bị bỏ đói bằng chứng, không phải luật yếu.** |
| 2026-08-18 | [v85 — Sửa thước đo nguồn: reviewer thấy NỘI DUNG trang](260818-v85-source-integrity.md) | ✅ Done · chưa release | Làm cả 3 việc v84 để lại, nhưng **đảo thứ tự có lý do**: truy gốc TRƯỚC (vì kết quả nó quyết định chỗ sửa), sửa hệ, rồi mới sửa thước đo — sửa thước trước là đo lại một hệ đang lỗi. **Gốc rễ tìm bằng đọc code, không suy đoán**: luật chống bịa (`team_task_check_prompt.py:34-40`, `team_task_prompt.py:236-243`) là luật **CÓ ĐIỀU KIỆN** — *"nếu có khối ĐẦU VÀO, đối chiếu mọi số liệu…"* — và điều kiện đó vĩnh viễn không thoả đúng ở bước `sprint`: `deps_json=[]` ⇒ `_read_handoff` trả `""` (docstring `team_task_graph.py:12`: "No deps ⇒ empty context") ⇒ `build_self_check_messages` lọc `if p` **bỏ hẳn khối rỗng khỏi prompt**. Cộng thêm 2 kênh bằng chứng khác cũng vắng: `bundle` của sprint là **biến local** chết cùng `_work` (hợp đồng trả `(text, cost)` không chở nổi nó), và baseline không có transcript (layout cũ) ⇒ `extract_review_evidence` trả None. Ba kênh độc lập cùng vắng ⇒ reviewer chấm **chỉ trên văn xuôi**: bảng bịa "hợp lý + đồng nhất + có citation" là thứ duy nhất nó thấy. **Bằng chứng đối chứng quyết định hướng sửa**: `step-47-review-0.json` của F2 tự ghi *"Kết quả không tự ý bịa số giá YouTube Music khi thiếu nguồn"* và **từ chối** chốt `59.000₫` (Apple Music) + `49.000₫` (Zing MP3) vì "chưa có URL nguồn xác minh" — đúng những con số baseline bịa và được judge cũ cho 9 điểm; cùng prompt, cùng model, khác duy nhất là F2 CÓ transcript ⇒ luật đúng, **đường ống mù** ⇒ không sửa prompt chống bịa, sửa việc **cấp bằng chứng**. Ship: `content_head` vào event `prefetch` (`collect_prefetch.py` — kênh giá trị hơn vì chạy trên MỌI bước sprint kể cả không có Firecrawl) và event `fetch` nhánh thành công (`sprint_runner.py`; 4 nhánh `skipped` cố ý không đổi — không có nội dung), `_append_content` render dòng `→ nội dung:` trong `transcript_evidence` — trước đó transcript chỉ ghi `queries`/`urls`/`bytes`, tức chứng minh trang ĐÃ mở nhưng **không chứng minh con số nào nằm trên trang đó** (reviewer không đối chiếu `65.000 ₫` với `bytes: 18079`); vắng `content_head` ⇒ **không thêm dòng nào**, không suy diễn (transcript cũ + round skipped vẫn render như trước). `scrub_secrets` phủ luôn nội dung mới vì nó chạy trên cả dòng JSON đã serialize. Thêm `QUY TẮC NHÃN NGUỒN` vào **cả** prompt review và prompt self-check: gọi "chính thức" một số lấy từ báo/blog/đại lý/trang tổng hợp là **sai nhãn** ⇒ nêu ở `failures`; chỉ chấm khi nhãn MÂU THUẪN với nguồn thấy được; và ghi rõ "nguồn thứ cấp"/"chưa có nguồn chính thức" là **TRUNG THỰC, không phải lỗi — kể cả khi trang chính thức đã mở nhưng không trả về số nào** (mệnh đề cuối đóng đúng ca SPA của v84, để không phạt oan bản đã mở trang mà trang rỗng). **Thước đo sửa xong thì PHÁN QUYẾT ĐẢO**: `blind_judge_v2` (sandbox, không vào repo) nhận `--evidence` là nội dung trang mở LIVE và đếm `nguon_verified/unverified/contradicted` **theo từng số** nên điểm truy được; chấm lại đúng cặp v84 với ground truth live (`spotify.com/vn-vi/premium` 6397 ký tự · `music.youtube.com/music_premium` 5696 — cả hai ghi **65.000 ₫**): baseline `nguon` **9 → 2** (2 số `contradicted`, **0** `verified`, 13-14 `unverified`), F1 **6-7** (0 contradicted), **verdict F1 thắng ở CẢ HAI** chiều đảo nhãn A/B ⇒ không phải thiên lệch vị trí. Judge tự nêu đúng 2 số bịa mà không được mách bản nào là bản nào: `['Spotify giá 59.000đ/tháng (bằng chứng: 65.000đ/tháng)', 'YouTube Music giá 59.000đ/tháng (bằng chứng: 65.000đ/tháng)']`. Truy vì sao v1 sai **chiều**: chính lời v1 khen baseline *"số liệu nhất quán"* — mà sự ĐỒNG NHẤT (5 dịch vụ khác nhau cùng một giá) LÀ chữ ký của bịa, v1 tính nó thành điểm CỘNG; v2 đảo lại: nhiều dịch vụ chung một số ⇒ dấu hiệu trừ, ngày "truy cập" không kiểm được ⇒ trừ, và **ô trống có lý do chấm CAO HƠN ô đầy bằng số không truy được** ("đừng thưởng cho độ đầy của bảng"). **Tự sai attribution rồi tự bắt được bằng hash**: đã báo "candidate thua với `nguon` 3-4" và ngầm hiểu bản bị chấm là F2 — đối chiếu `mapping-round*.txt` + kích thước (`cand-f1.md` 4609 B vs `cand-f2.md` 6340 B) cho thấy bản bị chấm là **F1** (`nguon` 5/6.5/7 qua 3 lượt, baseline 8/9/9) và **F2 chưa từng được đưa ra chấm**; học: nhãn A/B ẩn danh **không mang danh tính**, phải truy lại file thật bằng hash trước khi gán số cho một biến thể. Học thứ hai: **luật có điều kiện là luật có thể bị vô hiệu hoá bởi hình dạng dữ liệu** — *"nếu có khối ĐẦU VÀO"* nghe vô hại, nhưng bước không deps thì tiền đề vĩnh viễn FALSE và luật im lặng, không cảnh báo ⇒ mỗi tiền đề trong prompt là một nhánh cần biết ai làm cho nó sai. Học thứ ba: trước khi kết luận "gate yếu" phải tìm một ca mà gate đó CHẠY ĐÚNG — nếu có, vấn đề là **đầu vào của gate**. Phạm vi nói thẳng, không claim quá: peer review (cửa quyết định `passed`) **đã có** bằng chứng, nhưng **self-check của bước sprint vẫn mù** (`bundle` vẫn là local; bịt được bằng side-channel kiểu `StepTelemetry` `team_step_runner.py:70` — scope riêng, chưa làm). Cũng hạ một giả định của v84 xuống "chưa biết": `firecrawl-playwright-service-1` đang chạy và 2 trang official trả full nội dung ⇒ "Firecrawl không render JS" có vẻ **SAI**, nhưng chưa xác nhận trên đúng ca SPA (`zingmp3.vn`, `nhaccuatui.com`). 3 test mới TDD ĐỎ→XANH, suite **3462 passed / 1 skipped** (trước 3459), ruff sạch. **Cập nhật cùng ngày — arc xác nhận end-to-end bằng 3 run sống, và mỗi run bóc đúng một tầng đứt ống mà unit test cùng-fixture không thấy**: run #1 (805d6b68f76d) producer đúng (event `fetch` chở `content_head` 2063 ký tự chứa `65.000`) nhưng prompt review KHÔNG có khối bằng chứng — `65.000` grep thấy trong prompt hoá ra là **false positive** (nằm trong bảng của chính bài bị chấm; phải grep marker khối, không grep giá trị); gốc: worker ghi transcript vào jail riêng `.data/agents/<id>/…` còn `_run_review` glob root chung, best-effort nuốt lỗi ⇒ review lại mù đúng kiểu pre-v85 ⇒ fix `ReviewStepInput.graded_assignee` + lookup jail-first fallback root chung, khớp tiền lệ `routes_office_artifacts` (`f179788`). Run #2 (ed30096396ea) khối bằng chứng ĐÃ vào prompt nhưng `_append_content` cắt nội dung trang bằng hằng tool-result 500 ký tự — `65.000` nằm sau vị trí 500, khối mới dùng 1622/8000 budget mà con số cần đối chiếu vẫn mất ⇒ hằng riêng `_CONTENT_CHARS=4000` cho nội dung trang (`540e7af`). Run #3 (b1fb6f39cb62, done/delivered $0.26): `65.000` xuất hiện 4 lần TRONG khối `BẰNG CHỨNG QUÁ TRÌNH` của prompt review round 0, verdict passed=true failures=[] — tiêu chí đóng arc ĐẠT. Kèm: Firecrawl render JS trên đúng ca SPA được xác nhận (`zingmp3.vn/vip` trả giá thật "13.000đ/41.000đ/tháng", `nhaccuatui.com` render nhưng redirect app listing) ⇒ giả định v84 chính thức bị đảo; suite 3465 passed, 5 commit theo arc `e3eef5b`…`540e7af`. Còn nợ: `retry_round > 0` chưa bắn live; self-check sprint vẫn mù (scope riêng). Chưa release — chờ quyết định người dùng. |
| 2026-08-18 | [v86 — Thin tool loop tự chủ thay LangChain create_agent ở react tier](260818-v86-thin-loop-harness.md) | ✅ Done · **0.11.0** | Thay `langchain.agents.create_agent` ở react tier bằng **thin loop tự chủ** trên OpenAI SDK (`thin_tool_loop.py` + `typed_tool_specs.py`): wire rules W1-W6 (content `""` không null · reasoning passback đúng turn tool-call · `(no output)` · **cost EXACT từ usage extras** · args bịa drop+echo), typed snake_case specs + repair hook, guard length-finish + repeat-batch; dispatch `loop_engine: thin\|langchain` default thin, LangChain giữ selectable làm baseline A/B. Đếm lỗi tool-call từ transcript (`summarize_tool_errors`, 5 lớp marker-pinned vào chuỗi producer thật, mỗi result tối đa 1 lớp) + bench wiring cột rounds/tools/t-err. **Bench live 3+3 run** interleaved cùng brief, web thật: pass-rate 3/3 = 3/3 (blind judge), prompt token rẻ **3.5×**, wall **1.6×** nhanh, tool errors 0/45 vs 0/77, langchain-r2 đâm trần recursion_limit=32 phải salvage còn thin không chạm trần 16. VẤP đáng nhớ: định bench full-task rồi mới phát hiện work step chỉ tới tool tier khi prefetch FAIL (prefetch gate cùng opt-in+key với tool loop) → A/B phải drive thẳng seam `_run_work`; đọc `resolve_step_runtime` TRƯỚC khi thiết kế bench. Suite 3525 BE, ruff sạch. |
| 2026-08-18 | [v87 — Vòng dọn dẹp sau release: CI, deprecation, bundle, tags](260818-v87-cleanup-ci-bundle-tags.md) | ✅ Done | Dọn 4 mục sau khi phát hành 0.11.0. **CI thoát Node 20**: bump 6 action trong 3 workflow (checkout/setup-node/upload-artifact v7, download-artifact v8, setup-uv **v10.0.1 pin full version** vì action này ngừng publish tag major/minor từ v8 để chống supply-chain, action-gh-release v3) — đọc breaking notes từng cặp major trước khi bump, CI xanh 5/5 job, annotation deprecation sạch. **Hết warning cuối của suite**: `httpx2` vào dev group (TestClient của Starlette bỏ backend httpx cũ; httpx2 là package riêng nên httpx mà openai SDK dùng không bị đụng). **Entry bundle -38%**: chart.js (493K nguồn) rời entry qua `lazy-charts.tsx` — index 777→504 kB (gzip 240→149), chunk `chart-theme` mới chỉ tải ở Cost/Guardrail, lỗi tải degrade thành một dòng thông báo. Quyết định **KHÔNG** split `agent-desk` 900 kB: sourcemap cho thấy 94% là `three`+r3f của đúng một view đã lazy sau /office → nâng `chunkSizeWarningLimit` 950 có căn cứ thay vì chia cưỡng ép. **Tag sạch**: xoá v39/v49/v50 tiền-semver (+ Release đính kèm v39), ghi lại commit hash trước khi xoá. VẤP: grep chuỗi trên bundle minified để đoán thư viện nằm chunk nào cho kết quả vô nghĩa — sourcemap `sourcesContent` mới là nguồn số đo. 3526 BE (0 warning) + 303 FE + 10 e2e, tsc/ruff sạch. |
| 2026-08-19 | [v88 — Redesign toàn bộ web FE: 5 hub, chat làm nhà](260819-v88-web-fe-redesign-5-hub.md) | ✅ Done · chưa release | Quy hoạch lại IA từ ~20 route phẳng (ánh xạ 1-1 với endpoint, tức hình dạng của BE rò ra mặt user) thành **5 hub**: `/chat` (HOME), `/office` (3D), `/work`, `/team`, `/system`; mỗi màn cũ thành một tab **có URL riêng** (`?tab=`) bên trong hub đã hấp thụ nó, nên deep link + bookmark mount đúng tab khi cold load. Chat dựng theo lối app chat nhưng khai thác BE (giao việc · duyệt approval · artifact ngay trong luồng). **21 redirect** giữ mọi URL trước redesign còn resolve, `/agents/:id` giữ id trong path. Đổi tầng dữ liệu sang **TanStack Query** (`web/src/api/queries/`, `query-keys.ts` là factory key duy nhất để cầu SSE→invalidate gọi đúng slice), bỏ hẳn `AgentProvider` + `PendingApprovalsProvider` — agent nằm ở route, approvals là 1 query cache (mất luôn vòng fan-out 30s toàn fleet). Code chuyển sang `web/src/features/<hub>/`. Dọn 300 entry dictionary + 66 rule CSS + 10 file chết; entry bundle **540.05 → 475.37 kB**. **VẤP nặng nhất: suite xanh hoàn toàn vẫn ship được lỗi nhìn bằng mắt** — 344 vitest + 28 e2e + tsc + cổng bundle đều sạch, browser thật với fleet thật (11 agent, $25.33/$550) lộ 2 lỗi không assertion nào trong repo đang canh: `systemInsights.total` mất param `{spent}/{cap}/{pct}` (tổng fleet render ra mỗi chữ "Tổng") và `.company-identity` không có rule CSS (form dồn 1 hàng, label dính input). **Vấp thứ hai, suýt ship chuỗi key thô ra user**: `git checkout dictionary.ts` để revert một lần sweep hỏng đã nuốt luôn **25 key mới** của chính Phase 6 trong cùng file — chỉ lộ vì tsc fail sau revert; học: revert theo file là revert cả phần chưa commit của file đó, sweep và feature không được ở chung file chưa commit. **Vấp thứ ba: grep chuỗi không thấy tên dựng bằng nối chuỗi** — sweep CSS nuốt 5 họ `${base}-${value}` (`.workroom-*`, `.office-3d-state-*`, `.verdict-*`…), khôi phục 13 dòng + comment tại chỗ, scanner i18n whitelist theo prefix cùng lý do; và sweep dictionary hỏng 2 lần vì heuristic đọc sai biên entry (xóa theo dòng vỡ 8 entry đa dòng; "span kết ở dòng đầu có dấu phẩy" tràn sang entry sau) trước khi "span dừng ở dòng entry kế / comment / đóng block" mới sạch. Một bug thật do e2e bắt: `data?.skipped.length` chain `data` nhưng không chain `skipped` ⇒ `CompanyActivity` crash trắng màn — ô hiển thị lỗi mà lại là thứ làm hỏng màn hình thì phải phòng thủ mạnh hơn phần còn lại. Giữ `lib/api-cache.ts` dù plan liệt vào danh sách xóa: còn 4 caller sống ngoài query layer, xóa = tái phát burst fetch trùng. 3538 BE / 1 skipped + 344 FE + 28 e2e, tsc sạch. Còn nợ: đổi mật khẩu chưa làm được (`api/client.ts` không có endpoint), thư mục phụ thuộc ở root chưa vào .gitignore. |
| 2026-08-19 | [v89 — Đóng arc v88: mobile, cold start, dọn nợ, cổng release 0.12.0](260819-v89-arc-closeout-release-gate-0-12-0.md) | ✅ Done · chưa release | Đóng 4 phase khép arc v88. **Điện thoại**: thanh tab dưới đủ 5 hub, chrome gộp vào menu `⋯`, chat thành hai màn (danh sách → hội thoại) có Back + deep link; 5 e2e mobile ở 390px canh tràn ngang. **Cold start** (từ audit bản cài mới): tuyển người đầu tiên có đường tới; Resume xoá luôn cổng `enabled` trong profile — trước chỉ lật registry nên nút vô tác dụng, chỉ sửa được bằng tay; thông báo chặn giao việc nêu đúng màn cần mở (`Đội ngũ → Kênh`) thay vì từ vựng backend. **Dọn nợ v88**: `views/office-unified` + `views/office-shared` chuyển về đúng chủ theo consumer thật (`features/office/`, `features/shared/` mới) — `src/views/` chỉ còn cửa pre-auth Login/Setup, 0 tham chiếu treo; xoá 3 api method chết + 1 type mồ côi; đồng bộ 6 file docs về as-built v88. **Cổng release 0.12.0**: 6 ca delta-UAT trên daemon thật (150 room, 115 workroom, 11 agent) + browser thật cả 5 hub (0 lỗi console, 0 5xx) + wheel thật cài vào venv sạch (78 file `_shipped/`, cold home seed + bootstrap OK, màn Đăng nhập sạch). **VẤP: suite xanh vẫn giấu bug thật, lần thứ hai liên tiếp** — `test_list_survives_a_broken_profile` mock `raise RuntimeError(...)` nên luôn xanh, nhưng loader chỉ ném `RuntimeError` khi YAML *parse được nhưng sai hình dạng*; file **không parse nổi** (người mới gõ nhầm — ca phổ biến nhất) ném `yaml.YAMLError`, thoát cả 2 chốt, 500 cả roster lẫn khung chi tiết. Chỉ lộ khi cho file hỏng thật chạy qua loader thật → vá + pin test hồi quy. **Vấp hai**: SSE trả 0 payload qua 3 kiểu đọc stream tự viết, đã đi tra DB path/proxy/auth — hoá ra `curl -D -` báo "1118 bytes received", server luôn đúng, reader của mình nuốt dữ liệu. 3543 BE / 1 skipped + 344 FE + 33 e2e, tsc/ruff sạch, entry 476.74 kB. Bump 0.12.0 + changelog đã commit, **chưa tag chưa publish** — chờ CEO. |
| 2026-08-19 | [v90 — Đóng 5 mục treo sau cổng release 0.12.0](260819-v90-open-items-hardening.md) | ✅ Done · chưa release | Đóng đúng 5 mục treo §6 của biên bản gate 0.12.0. **Rail escalation hết ép Telegram**: `operator_channels.send_via_channels()` thử telegram → smtp → webhook, seam đặt trong `operator_notify._try_send` nên **8** consumer thoát cùng lúc (plan dự tính 2); hợp đồng 3 trạng thái `True/False/**None**` — `None` nghĩa "agent này không có kênh" để caller duyệt tiếp danh sách agent thay vì dừng như gặp lỗi. Cấu hình bằng env presence (`OPERATOR_EMAIL`, `OPERATOR_WEBHOOK_URL`), **không** vào `company.yaml` vì `save_company` dựng lại từ dict cứng nên nuốt key viết tay. **Đổi mật khẩu** (`POST /api/auth/change-password` + ô trong tab Cài đặt) ghi lại luôn `WEB_SESSION_SECRET` ⇒ mọi cookie đã ký chết ngay, kể cả của chính người đổi — đổi mật khẩu khi nghi bị xâm nhập mà phiên cũ còn sống thì đổi làm gì. **Bỏ bản nháp ngay trên bảng việc** (trước đây nháp bỏ dở là thẻ ma, chỉ màn giao việc đang mở mới hủy được), không xoá lạc quan mà invalidate để backend nói lại. **`scripts/cold-start-smoke.sh`** thay ca chạy tay của gate: dựng wheel → soi trong zip (`_shipped/` ≥60, FE ≥5) → cài môi trường sạch → seed home rỗng → `/health` → `--browser` chạy Playwright thật trên backend thật qua config thứ hai (suite cũ mock mọi `/api`, mock ở đây là vô hiệu hoá chính bài test); chạy ra **đúng** các con số gate đo tay (78/26/0.12.0/200). **Thứ tự `/api/approvals/pending`**: D1 của gate chạy trên hàng chờ rỗng nên không lộ — index toàn fleet đang gom theo vòng duyệt registry, tức xếp theo bảng chữ cái tên agent; sửa thành cũ-nhất-lên-đầu với tie-break `(created_at, agent_id, id)`, test viết trước và ĐỎ thật. **VẤP: tạo `operator_notify.py` mà không grep chính tên file sắp ghi → đè mất module có sẵn** — grep theo *nội dung* kỹ đến mấy cũng không thay được một lệnh `ls` trên đường dẫn đích. **Vấp hai: xác minh sống lần đầu chứng minh nhầm thứ** — chạy với `coordinator` (CÓ Telegram) nên Telegram thắng, webhook chưa hề được thử; chạy lại với `researcher` mới thấy `['webhook']` → `True` → receiver ghi payload thật. Đếm luôn được quy mô vấn đề gốc: **7/11 agent** fleet thật không có kênh báo nào. **Vấp ba**: thiếu dấu phẩy trong `__all__` không phải lỗi cú pháp — hai literal tự nối, export lặng lẽ biến mất. 3550 BE / 1 skipped + 355 FE + 33 e2e + cold-start smoke, tsc/ruff sạch, entry 478.89 kB. Vẫn **chưa tag chưa publish** 0.12.0. |
| 2026-08-20 | [v91 — UX real-use journeys: cold-start integrity + nút-hoá ops sẵn có](260820-v91-ux-real-use-journeys.md) | 🟡 In progress · chưa release | Sửa 4 dead-end cold-start rồi nút-hoá năng lực BE sẵn có để hành trình thật không phải rời web app hay sửa YAML tay. **P1** cold-start: wizard hết brick, đường không-mật-khẩu render sạch, dry-run hết bẫy im lặng, thông báo restart không nói dối cơ chế. **P2** dry-run hiện + điều khiển từ web. **P3** gỡ kẹt + hủy task thành REST (`routes_team_task_actions.py`) + nút "Giao lại việc này". **P4** `profile_patch.py` (ruamel round-trip, whitelist khối, atomic) + route safety/settings sửa profile.yaml giữ nguyên comment + key viết tay; `webhook_url_guard.py`. **P5** seed brief giao-lại (router state one-shot), edit-request client-side, command-seed + context chip, toggle autopilot + ô concurrency. **Quyết định gai nhất: `save_company` chuyển sang ruamel round-trip (P5-D0)** — bản cũ rebuild từ dict 6-key cứng nên nuốt mọi key/comment viết tay, đúng lớp sự cố `profile_patch` sinh ra để tránh; thêm dep `ruamel.yaml` chỉ ở write path, đọc vẫn `safe_load`. **VẤP: cook không tự chạy được live-UAT** — bước 2/3/4 (wizard thật trên browser, bind bot Telegram thật + chào, gửi dry-run-off thật) cần OpenRouter key + Telegram token + trình duyệt mà controller không có; không bịa pass, tách phần tự động (gate, doctor, docs, cold-start smoke đều xanh) khỏi phần chỉ người làm được rồi bàn giao đúng 1 handoff. **Vấp hai**: bẫy CWD Bash (`cd web` dính từ lượt trước) làm lệnh path tương đối nổ; **vấp ba**: e2e cold-start "2 failed" không phải regression mà do config không có `webServer` — phải chạy `cold-start-smoke.sh --browser`. 3619 BE (2 đỏ tồn từ trước: deepagents optional + test repo anh em) + 404 FE + 35 e2e, tsc/ruff sạch. **P6 live-UAT còn cần người.** |
| 2026-08-21 | [E2E full-flow cho journey v91](260821-e2e-full-flow-v91-journeys.md) | ✅ Done | Bịt lỗ e2e browser-level v91: +7 case (suite 37→44, 39 chromium + 5 mobile), 0 dòng UNMOCKED. Trọng tâm **unstick từ TRANG CHI TIẾT** (#22/#23) — trang này render từ `artifacts.room(roomId)`, cache entry khác board, nên repaint phụ thuộc invalidate riêng đúng chỗ review từng thấy thiếu; mutation-test tự gỡ dòng invalidate để xác nhận case đỏ thật. Thêm form cấu hình agent round-trip (#25/#26), badge dry-run + sửa yêu cầu (#27/#28), autopilot + concurrency load-modify-save (#24/#25 system). Fixture: bịt gap `/api/agents/{id}/band`, thêm `expectNoUnmockedRoutes()` biến cảnh báo console-only thành assertion. **Vấp lớn**: tester chạy `git checkout` để revert mutation → xóa luôn 463 dòng chưa commit, reflog/stash trống, phải rebuild → quy tắc mới: commit trước khi spawn agent chạm git, cấm agent dùng lệnh git mutate tree. Đính chính số liệu docs-manager tự báo (3690 passed +1 skipped, không phải 3691). |
| 2026-08-23 | [v92 — Vòng sẵn sàng release: 3 phát hiện ops + bug hybrid-state](260823-v92-release-readiness-and-hybrid-state-fix.md) | ✅ Done · cắt v0.13.0 | Sửa 3 phát hiện UAT ops-chat live (history search fallback khớp-bất-kỳ-từ + cờ "kết quả gần đúng", sửa PIC trỏ bước đã kết thúc trong code, báo tuổi stall). **Bug nặng nhất: hybrid-state** — `_retry` gọi `reset_step_to_pending` xoá attempt_id, `_give_up` cùng chuỗi vẫn guard theo lease cũ → UPDATE khớp 0 dòng → task `stalled` giữ bước `pending` mà `retry_stalled_step` không cứu nổi (`_dead_steps` chỉ nhận failed/timeout), chỉ còn cancel; sửa bằng `mark_failed_if_pending` (bỏ guard, `only_if_status` lo nguyên tử). Mọi ghi terminal giờ qua `_terminal_write` chung, no-op có WARNING thay vì nuốt boolean. **Cổng typecheck FE hoá ra không kiểm gì**: `tsc --noEmit` đọc `tsconfig.json` rỗng nên pass, `tsc -b` (build thật) đỏ 15 lỗi — sửa hết bằng cách chỉnh mock/fixture cho khớp route thật (không nới type production), rebuild dist đã trôi sau 38 file `web/src`. **Vấp**: test double `mark_failed` trả `None` che mất nhánh hồi phục mới → double phải giữ đúng contract kể cả giá trị trả về. **Full-flow LIVE dữ liệu thật + LLM thật** qua `POST /api/ops/chat`: giao việc → decompose → tự xác nhận → spawn → stuck → retry-with-guidance → reassign researcher → done → review vòng 0 trượt → rework → review vòng 1 đạt → task `done`, 0 WARNING no-op. |
| 2026-08-24 | [omp/Pi emulation: advisor sweep, prompt audit, role models, provider registry](260824-omp-pi-advisor-prompt-audit-providers.md) | ✅ Done | 4 đề xuất chọn từ report omp/Pi. **P1 advisor sweep**: cặp mắt thứ hai đọc delta step-transcript, chèn `nit`/`concern` không cắt ngang agent chính, ≤1 note/task/sweep, advisor hỏng không làm hỏng tick, mặc định TẮT. **P2 prompt audit đo 94 call thật** từ `llm_request` trong 37 transcript — và tìm ra lỗi ĐÚNG-SAI chứ không phải chỗ béo: diff hai grader (`difflib` 0.615) lộ peer review thiếu 2/4 quy tắc bằng chứng mà self-check có → nó *pass* kết quả thiếu URL và *fail* kết quả không đạt tiêu chí khắt khe hơn cả điều CEO hỏi; gộp về `EVIDENCE_RULES`, prompt **to ra** (+782/+372 chars) thay vì giảm 30% như nhắm — trả lại 2 quy tắc thiếu đáng giá hơn token tiết kiệm. **P3** `role_models` lên form web (textarea `role = model`) + toggle advisor; ghi đè NGUYÊN KHỐI vì merge lá sẽ để override đã xoá nằm lại **âm thầm tính tiền**; GET cố tình không trộn env kẻo một lần Save vô hại ghim giá trị fleet vào yaml riêng. **P4 provider registry**: entry chain chỉ định endpoint khác bằng `provider::model` (`::` vì `/` và `:` đã bị id OpenRouter chiếm), yaml chỉ chứa TÊN biến môi trường không bao giờ chứa key; không hỗ trợ OAuth thuê bao (fleet 24/7 trên subscription = vùng xám ToS). **Kiểm chứng fleet thật**: task `b34ebc3db8b4` trên `analyst` chỉ đẩy role `content` qua registry (cost 0.0091), `plan`/`review`/`util` vẫn OpenRouter — đúng 3 tầng; fallback xuyên provider ăn 400 thật rồi đi tiếp, `fallback_from` giữ tiền tố để quy được chi phí; browser thật xác nhận `::` sống sót trọn vòng textarea→PATCH→yaml→GET. **Vấp**: đoán endpoint sai tốn 20 vòng poll (transcript nằm ở `.data/agents/<agent>/artifacts/...`, không cùng chỗ artifact); console error 400 tưởng lỗi hoá ra là bước test cố tình để backend từ chối. 3785 BE (+36) / 417 FE / `tsc -b` + ruff sạch. |
| 2026-08-24 | [v91 P6 — cold-start UAT chạy thật](260824-v91-cold-start-uat-real-run.md) | ✅ Done · đóng plan `260819-2041-ux-real-use-journeys` | Chạy trọn 8 bước UAT trên máy-trắng THẬT (venv mới + `pip install .` + `MY_CREW_HOME` rỗng + port 8799 tách khỏi fleet 8765, chromium thật, OpenRouter + bot Telegram thật). **4 bug — cả 4 chỉ lộ khi cài mới, không unit test nào bắt được vì fixture luôn dựng sẵn trạng thái đúng**: wizard instance thứ hai kickstart launchd label `com.mpm.web` mà không kiểm label đó có quản tiến trình đang chạy không → đá chết fleet thật PID 97132 (`b79a101`); bind Telegram không bao giờ ghi `ops_operator_id` nên guard giao việc không thể thoả, chỉ thoát được bằng sửa YAML tay (`6cdee39`); `mcp` là transitive dep KHÔNG ghim của `langchain-mcp-adapters==0.3.0` → cài mới resolve 2.0.0 (đã bỏ `mcp.shared.context.RequestContext`) giết mọi tích hợp MCP ngay từ lúc cài, mà doctor lại bày hướng dẫn "đặt token" cạnh ImportError (`020385b`); form điền `schedule` đẩy khối comment mẫu `# inbox:` xuống thành con của nó — ruamel gộp comment inline của key và khối comment NGAY DƯỚI vào MỘT token, thử 3 hướng code-level đều ra output y hệt nên sửa bằng đảo thứ tự trong template + test canh viết trên **text đã render** vì assert mức value mù hoàn toàn với lỗi này (`0af1b84`). **Ba trong bốn cùng hình dạng**: app chỉ đúng việc phải làm, làm theo thì không sửa được gì. Đóng băng hành trình thành `web/e2e-uat/` (7 spec; bước 8 là CLI nên không có spec) — commit xong mới lộ `e2e-uat` không nằm trong tsconfig nào nên chưa từng typecheck (một `string|undefined` lọt qua, tên biến môi trường cũ `HR_...` pass chỉ nhờ shell tình cờ có export), và vitest exclude liệt kê theo tên nên collect luôn spec Playwright → đổi sang khớp tiền tố `e2e-*`; cùng lúc lộ toggle advisor mượn `.agent-dry-run-toggle` (class không CSS, thuần test hook) làm 2 spec trust-mode nhập nhằng. 3792 BE / 417 FE / 44 e2e / ruff + tsc sạch. |
| 2026-08-25 | [Fast/crew lane routing v2 — effort tier + hai lỗ chèn lệnh bậc hai](260825-fast-crew-lane-routing-v2-effort-tier.md) | ✅ Phase 1-3 (4-6 ở entry 08-26) | Chấm **độ khó bản chất** của đề ngay trong lượt gọi intake đã có (`sprint_intake`) — 3 bậc `low`/`medium`/`high`, **0 lượt gọi model thêm**, phân vân thì hạ bậc. Chỉ `low` đổi hành vi: role model mới `sprint_low`, budget tìm kiếm 4, tối đa 1 vòng sửa (thường 2); `medium` LÀ hành vi cũ và cũng là đích fail-open của mọi nhánh hỏng, nên tier rác hay intake lỗi đều không đổi gì. Bậc ghi vào `route_json` chứ không thêm cột — bản ghi định tuyến vốn nằm cạnh kết cục task nên `route_stats` đọc thẳng ra "đề chấm khó bế tắc bao nhiêu %"; vòng này **chỉ đo**, effort chưa có quyền đổi lane. `sprint_low` chưa cấu hình thì degrade về model fleet ⇒ tính năng nằm im tới khi có người bật. **Bug nặng nhất là do test của chính mình bắt**: `effort_of_task` viết `with TeamTaskStore(...)` nhưng lớp này không phải context manager, `except Exception` fail-open nuốt luôn `TypeError` ⇒ tier LUÔN đọc ra `medium`, tính năng chết lâm sàng mà nhìn ngoài vẫn lành — fail-open che lỗi lập trình chứ không chỉ che lỗi dữ liệu. **Hai lỗ chèn lệnh bậc hai** (code-reviewer bắt, tự dựng lại được cả hai) hoá ra **cùng một gốc**: khối context của `upgrade_to_team` đi nhờ trường `brief`, mà tầng dưới coi `brief` là lời CEO tự viết — cắt 120 ký tự làm tiêu đề đưa chữ do LLM viết thẳng vào tin Telegram gửi CEO, và cắt lại 2000 ký tự cho prompt **mọi bước** làm lát rơi giữa khối bọc, để dấu mở lơ lửng ⇒ mọi thứ sau nó nằm trong một khối không bao giờ đóng. Sửa ở chỗ **TIÊU THỤ** (tiêu đề lấy đoạn đầu; cắt có nhận biết delimiter) chứ không ở chỗ dựng đề, vì cắt làm hỏng khối bọc là lỗi bất kể ai viết đề. **Học**: test bảo vệ đặt ở chỗ DỰNG không bắt được lỗi ở chỗ TIÊU THỤ; và test phải dùng nội dung **lành mà dài** — chuỗi thù địch bị L2 cách ly thành placeholder ngắn nên không tràn cửa sổ cắt, lỗi biến mất. Probe hàng rào chuỗi nâng cấp cho thấy docstring nói sai lớp nào đang chặn: `_upgradable` chặn trước, `_already_upgraded` không bao giờ tới lượt trên đường thường — sửa tài liệu + thêm test riêng cho lớp thứ hai. Tự probe fix của mình còn lộ một ca biên: đề **mở đầu** bằng dấu `===SEARCH_RESULT===` do người tự gõ (luôn hở theo định nghĩa) bị cắt về rỗng, tức xoá sạch đề CEO → đổi sang vô hiệu hoá dấu tại chỗ. **Vấp**: id test sinh từ `hash()` (bất định mỗi tiến trình) làm một lần chạy mutation treo 2 phút rồi timeout giết shell trước dòng restore, để lại file đã mutate trong worktree — phát hiện bằng `grep`, khôi phục từ backup. 3896 BE / 1 skipped, ruff + `tsc --noEmit` sạch. |
| 2026-08-27 | [Chuẩn nguồn thực tế trước release — vá 3 tầng](260827-source-bar-realism-three-layer-patch.md) | ✅ Done · chưa release-ready | **Kẻ leo thang tiêu chí không phải reviewer mà là `guidance` của stuck judge** — nó không có luật nguồn lẫn mốc ngày, tự đòi "báo cáo uy tín như Statista", "URL kiểm chứng độc lập", và tin hôm nay là 2024 nên bắt sửa ngày ĐÚNG 27/08/2026 thành sai; grader vòng sau chấm theo guidance nguyên văn ("như chỉ dẫn yêu cầu"), còn acceptance sạch trong mọi case. Vá 3 tầng ba commit: anchor ngày worker/grader (`a335e32`), port luật nguồn intake sang decompose (`ce7cd7f`), anchor + trần guidance cho stuck judge (`a8c832b`). Bench lanes6: sprint/ecommerce là case ĐẦU TIÊN toàn arc giao được báo cáo thật (4990c); 0 đòi siêu dữ liệu, 0 ngày bịa; "blindspot 23%" hóa ra là artifact đếm của chính script đo (một event prefetch gói nhiều truy vấn — angle rotation v81; đối chiếu 61=61 → 0% thật). Nhưng 3/4 case vẫn stall vì lớp lỗi KHÁC: guidance leo chuẩn chiều "tên trang → URL" + lạm phát chi tiết, lặp gần nguyên văn giữa các lần can thiệp, decompose đếm NCT và NhacCuaTui là 2 nền tảng, bước QA tự nâng chuẩn giết task dù báo cáo đã viết xong — nên KHÔNG tuyên release-ready. Cổng: 4000 BE passed / 1 skip (deepagents extra đã sync lại), ruff sạch. |
| 2026-08-27 | [Vòng rework mù suốt từ đầu — và bản vá trước khi cắt v0.14.0](260827-rework-blind-fix-and-v0140.md) | ✅ Done · tag v0.14.0 | **Bước `rework` chưa bao giờ nhận được lời soát chéo.** Nó đọc artifact bước trước theo `seq` của CHÍNH dòng review, trong khi review chỉ ghi `step-<seq_bị_chấm>-review-<vòng>.json` → đọc trượt → handoff RỖNG → vòng sửa viết lại từ đầu chỉ với cái tiêu đề, nên mọi vòng review sau vòng 0 đều mù. Đo toàn fleet: danh sách lỗi tới được **0/87 → 87/87** dòng rework. Phản chứng mạnh nhất: cùng một dòng, cùng dữ liệu trên đĩa, code cũ đọc **0 ký tự** — code mới đọc **1296 ký tự có đủ danh sách lỗi**. Live 2 task chạy trọn 1 và 2 vòng rework rồi PASS (trước đây là kịch bản kẹt chắc chắn); judge mù candidate thắng **6/6 phiếu**. **Lỗi 2**: `DEFAULT_JUDGE_MODEL` trỏ model provider đã gỡ → HTTP 400, mọi lượt chấm chết ngay phiếu đầu — judge là mode duy nhất tiêu tiền và chỉ chạy tay nên không test nào chạm tới. **Lỗi 3**: `self_check` cộng dồn lỗi mọi vòng vào artifact; đo trên `74e16044cb6f` thấy 8 lời phê từ 3 vòng, **4 cái SAI** so với chính bản văn được giao (bảo 'chỉ có mục Discord' trong bản có đủ 3 công cụ với 14 trích dẫn có ngày) — mà `stuck_decision` đưa đúng cụm đó cho điều phối ĐẶT CẠNH bản nháp đã sửa xong, nên nó chấm theo lỗi không còn tồn tại: task đốt 4 lần can thiệp rồi stall. Sửa bằng cách buộc danh sách lỗi vào đúng bản nháp được giao qua chung swap keep-best. **Vấp**: tự đo sai 2 lần (vớ phải mục 'Đạt ✓' của reviewer; quên tiền tố `- `), cả hai tự bắt được nhờ dump một dòng end-to-end trước khi tin số tổng hợp; chẩn đoán đầu tiên ('rework chạy mù, 0 lượt tool') SAI — audit.jsonl có 3 lượt brave, `result_count: 5`. **Và fix query hầu như không kích hoạt**: cả 3 rework live có tiêu đề 44–62 từ, dài hơn cả ngân sách 44 từ, nên chúng sửa được là nhờ fix handoff — suýt ghi công nhầm. Cổng: 3997 BE passed, 9 skipped, ruff sạch. |
| 2026-08-27 | [Vòng can thiệp hội tụ + salvage khi bỏ cuộc](260827-intervention-loop-convergence-salvage.md) | ✅ Done · chưa release-ready | **Judge kẹt lặp nguyên văn chỉ dẫn thất bại; give_up vứt trắng báo cáo đã viết xong.** Vá 3 mảnh: brief cho judge THẤY guidance đã ra + QUY TẮC HỘI TỤ (chỉ còn hạ chuẩn/reassign/give_up, cấm lặp); `_give_up` giao kèm phần done tốt nhất (≥400c, cap 6000c, câu bỏ cuộc luôn đứng đầu); trần decompose (cấm liệt kê thực thể từ trí nhớ model, cấm "ít nhất N" tự bịa) + trần bước thẩm định (chấm đúng đề gốc). Bench lanes7: 0 cặp guidance gần-nguyên-văn (lanes6 có lặp verbatim; max jaccard 0.325), 2 give_up lane team kèm salvage 2420c/1577c, sprint/ecom nhanh gấp 4 (104s vs 449s, $0.042). Vẫn 1/4 giao được — gate cặp-chấm-được FAIL lần 4; finding mới: judge tự chốt danh-sách-nguồn-đóng rồi trượt worker dùng nguồn hợp lệ khác; music đòi số đo không công khai. 4023 BE xanh, ruff sạch. |
| 2026-08-28 | [Degrade-and-continue + review-drop guard](260828-degrade-and-continue-review-drop-guard.md) | ✅ Done · chưa release-ready | **Task chết một bước không còn stall trắng tay — và cái vòng review đè lên bước đã bỏ qua.** P1 skip-with-gap (`fc6ebf4`): bước chết được drop kèm placeholder, giao với header khoảng trống. P2 routing guard (`05675b1`): decompose suy biến (≤3 bước, chain thẳng 1 người) kéo về sprint. Fix nặng nhất (`213f119`+`ee57ce3`): band supervised bỏ qua hoàn toàn `needs_review=0` mà `mark_step_dropped` ghi → mint review lên placeholder; drop retire lease làm `locked_version=''` không khớp → stale → verdict None → mint lại CÙNG vòng → 6 re-review đốt sạch budget trên task content đã done 100%. Chữ ký drop = done ∧ lease rỗng (độc quyền, sweep xác nhận), gác cả 3 cổng mint kể cả khi CEO drop chính review/rework row (M1/M2 từ code review). Manh mối chẩn đoán: chỉ bước analyst bị — band store thật của repo leak vào bench qua `settings.DATA_DIR`, tức bug production thật. Bench 3 run: lanes9c xác nhận live — bước `research` bị drop, 0 review mint lên nó, 2 case team giao salvage-gap. Post-fix lộ 2 lớp mới: reviewer flip-flop trên đề 2-đáp-án (YouTube vs Zing MP3) → hết cap rework → stall dù content xong (lanes9b 3/4 case); gap cascade — skip research đầu chuỗi thì bước tổng hợp thiếu input, worker thú nhận bịa rồi fail trung thực. Gate cặp-chấm-được FAIL lần 6-8. 4044 BE, ruff sạch. |
| 2026-08-30 | [Step-death salvage — nháp trượt + luật gộp thu thập](260830-step-death-salvage-draft-quarantine.md) | ✅ Done | **Bước bị skip-with-gap không vứt nháp trượt nữa.** `drop_step_with_placeholder` đọc artifact trước khi ghi đè, đính nháp cuối (≥200c, cap 4000c) dưới marker `BẢN NHÁP CHƯA ĐẠT SOÁT`; hạ nguồn ĐƯỢC dùng nháp nếu dán nhãn 'dữ liệu chưa qua soát' — luật gom về hằng chung `INHERITED_GAP_RULE` (vốn lặp nguyên văn 2 grader) lan 5 tầng prompt. Decompose thêm QUY TẮC GỘP THU THẬP (cấm bước tra-cứu-trần 1 thực thể — hình dạng chết lanes12), live ăn ngay: plan matrix ra bước gộp tra cứu+bảng. **Bug tương tác tìm ra live**: nháp chứa cụm vô hại "bỏ qua yêu cầu tìm kiếm" khớp `_INJECTION_MARKERS` → handoff cách ly TOÀN BỘ artifact — sửa lúc GHI (rà marker phía người ghi), không nới quarantine. lanes13: decision team hoàn thành SẠCH 0 drop (vòng trước chết giữa chuỗi); judge sprint 2·0. lanes13b: matrix CẢ HAI lane give_up trung thực khi search chỉ trả nguồn thứ cấp → ngừng dùng matrix làm trục so lane; checker bắt đúng 1 vụ "rửa nhãn" nhãn-không-nháp. 4058 BE, ruff sạch. |
| 2026-08-30 | [QA rescue ô không công khai + tín hiệu material_transform](260830-nonpublic-cell-rescue-material-transform-signal.md) | ✅ Done | **Ô ghi trung thực "không công khai" kèm nguồn đã tra giờ là ô ĐẠT — matrix_canary sprint từ 0/2 trắng tay thành bảng 3×3 đủ.** `NONPUBLIC_RULE` chung trong grading_rules tự lan sang self_check + peer review; thêm quy tắc worker (giao ĐỦ bảng, điền ô thiếu trung thực), stuck judge (không công khai ≠ bế tắc → retry_with_guidance), decompose escape-hatch (tiêu chí phải ghi lối thoát kể cả khi CEO đòi đích danh số). Bench lanes12 4 case × 2 lane, judge mù: SPRINT 3 · TEAM 1 — team thắng draft_critique 3-0 và còn RẺ + NHANH hơn sprint. Chữ ký "đề hợp team" từ 11 case lanes9-12: chất liệu cấp sẵn trong đề + biến đổi 2 tầng phân tích→sản phẩm + bước cuối tự viết trọn deliverable gọn; team thua MỌI đề lắp ráp mảnh song song (aggregate mỏng 2-4×). Router học được tín hiệu `material_transform` (bộ ba hint chất liệu × phân tích × sản phẩm) ghi vào route_json — CHỈ ĐO chưa đổi lane theo tiền lệ effort_high, test ghim lane-neutral. Rescue KHÔNG cứu được team matrix: bước chỉ-thu-thập vẫn skip-with-gap → tầng ghép 0 dữ liệu — nguồn thua số 1 của team giờ là mid-chain step death, hoãn có chủ đích. 4046 BE, ruff sạch. |
| 2026-08-30 | [Graph-engineering crew lane — boundary, fold, gate trượt](260830-graph-engineering-crew-lane.md) | ✅ Done | **Decompose phải chứng minh ranh giới, không được chẻ bước vô cớ.** `TeamStepPlan.boundary` (5 loại) khai vì sao bước xứng là node riêng — observational-only, phân bố vào `route_json.signals.boundary_counts`; lưới cưỡng chế là `fold_unjustified_steps` chạy SAU fanout: gộp bước đúng-1-dep + cùng người + cùng quyền, suy từ cấu trúc DAG thuần nên model bịa nhãn giữ bước không được gì, fail-open. Tiền kiểm định lượng (`deterministic_step_check`): code đo phủ thực thể + đếm mục TRƯỚC LLM checker — gap code-found fail ngay confidence 1.0 không tốn call; sprint tắt (`deterministic_precheck=False` — `coverage_gaps` riêng của nó biết "nguồn từ chối" không phải gap). **Bench vòng 6 (lanes14, 11 cặp): gate trao quyền material_transform TRƯỢT** — team judge-win 3/7 < 2/3, no-salvage completion 64% < 80%, chỉ cost đậu (1.19×) → `classify_brief` giữ nguyên, đúng triết lý đo-trước-trao-quyền-sau; chữ ký thắng n=2 ở lanes11/12 không tổng quát hoá qua 7 biến thể. **Salvage hạ nguồn verified live (nợ vòng 5)**: ab_test team chết organic 2 bước → terminal tiêu thụ nháp salvage, giao có dán nhãn 'chưa qua soát' — trong khi đề chuỗi dàn dựng chạy sạch cả 3 bước cả hai lane. Fold đổi kinh tế lane: team về 1–3 bước, cost 1.19× sprint (trước 3–4×), case fold→1 rẻ hơn cả sprint (sprint effort=low đốt 3 review + 2 rework). Dead-end bắn live 2 lần tự leo team. 4088 BE, ruff sạch. |
| 2026-08-30 | [Crew reliability — guidance đóng băng, bench vòng 7 ĐẬU](260830-crew-reliability-guidance-freeze-fix.md) | ✅ Done | **Vòng rework 2 hết bị nhắc sửa đúng thứ vòng 1 vừa sửa.** Taxonomy bước chết vòng 6 trên transcript thật → nguyên nhân top: guidance điều phối ĐÓNG BĂNG cả attempt (perceive chạy 1 lần/attempt). Fix `_strip_guidance`: chỉ vòng rework đầu tiêu thụ guidance, các vòng sau strip — giữ dòng wake-context `Bối cảnh:` của rework soát chéo, anchor `rfind` chống header bị nội dung nhại; test mutation-checked. **Bench vòng 7 (lanes15, re-run 4 case chết): gate khóa trước ≥2/4 sống sạch → ĐẬU 3/4** (baseline 0/4; drop 2→0, salvage 2→0, failed 3→1; cost team −60% — reliability fix cũng là cost fix). Judge mù: 2 case lật 0-3→3-0 sau khi sạch. Giả thuyết chữ ký `multi_deliverable` HỦY sau khi đo (0 từ vựng tách nhóm thắng/thua — sạch mới là biến trung gian). Tìm + sửa bug bộ lọc judge (chấm cả bản giao hỏng, nhiễm verdict vòng 6 — tally 7-4 cần chú thích; P2 hủy càng vững). Kiểm chứng routing thật 11 brief: đúng 7/11 = luôn-chọn-sprint, 2 tín hiệu hiện có khớp-chuỗi sai nghĩa, giá trị ròng 0 → routing 0 diff đúng cam kết. meeting_notes vẫn stalled vì mơ hồ phân rã (class ngoài phạm vi fix). 4091 BE, ruff sạch. |
| 2026-08-30 | [Zalo business fleet P2/P3/P4/P6 — và hai lỗi chỉ model thật mới lộ](260830-zalo-business-fleet-p2346-release-0150.md) | ✅ Done · tag v0.15.0 | 4 phase của plan `260830-1311-zalo-business-fleet` (P1 Zalo + P5 assistant khách **hoãn**, chờ OA verified). **Control-plane API**: `delegate_work` + status hợp nhất + fleet overview cho caller ngoài SPA, confirm bắt buộc mang plan hash hiện hành (hash cũ/thiếu → từ chối). **Escalation→manager**: việc vượt thẩm quyền mint task 1 bước cho manager thay vì chết tại chỗ, 3 chốt chống bão (không escalate lại, cap ngày theo nguồn, manager không giao được → báo thẳng chủ). **Credential store**: `credentials.enc` Fernet per-account, master key do store tự ghi, egress filter học cả hai dạng Fernet. **Worker packs** accounting + Meta Ads đọc-insight + media dir bền. Hai lỗi hạng production **chỉ lộ khi chạy model thật**: hỏi giá/tỷ giá trả lời từ trí nhớ cũ thay vì cử người tra; việc đụng bên ngoài trả về danh sách lệnh thay vì thành việc — admin trượt 4/5 vs personal 1/5 (catalog lớn cho model thêm cớ kết luận 'không lệnh nào khớp'). Sửa ở prompt + mô tả lệnh, **không nới assert**; sau sửa 42/42. Cũng sửa `dead_end` đè `source` của route (phá đúng thứ escalation cần). Live fullflow 33 case vs model thật ($0.19, 19 phút). Cổng: 4212 BE / 417 FE / 44 e2e, ruff sạch, bench 0 delta. |
| 2026-08-31 | [Live fullflow v3 — topology thật, journey xuyên tính năng, benchmark 7 trục](260831-live-fullflow-v3-topology-journeys.md) | ✅ Done | **Nâng trục "thật": tiến trình `my-crew serve` thật + HTTP thật + tick thật, không còn gọi thẳng route handler.** 54 case (từ 33): journey xuyên tính năng J1–J5 gồm kill -9 giữa task rồi boot lại, adversarial injection X1–X4 mỗi đòn kèm case đối chứng dương b/c để assert không xanh rỗng, topology smoke. Benchmark thêm 2 mode `reliability` (router quyết giống nhau qua k lượt) + `journey` (hình dạng việc: chặng, lần chạm người, chết ở đâu); baseline 0.15.0 để `bench/` chứ KHÔNG trong `my_crew/` vì `packages = ["my_crew"]` sẽ ship artifact nội bộ tới mọi người cài gói. Chạy 2 lượt: **53/1** rồi **54/54** (27:56). **Bug sản phẩm thật**: B0 cụm ngoặc viết thường nuốt mất chủ thể — đề 12 sàn bị parse thành 8 *hạng mục*, đếm 8 < ngưỡng 10 nên brief 12 chủ thể ở lại lane một người; sửa bằng chính bộ phân biệt module đã tin (thuộc tính tiếng Việt viết thường, tên riêng thì không). B6 lộ ra khi **in giá trị thật thay vì tin test xanh**: `(` không kết thúc mục sau hai chấm → thực thể thứ 12 là `'Nhà Thuốc Long Châu (hạng mục'` — số đếm vẫn đúng 12 nên MỌI assert theo số lượng đều xanh, thiệt hại nằm ở hai consumer dùng CHUỖI (`_split_entities` ghi tên vào title+acceptance của bước, sprint tìm kiếm nguyên văn) ⇒ **đếm đúng ≠ phân tích đúng**. B5 tài liệu benchmark mô tả sai chính công cụ đo: epilog ghi "Five modes" khi có 7, `releasing.md` nói `journey` ghi JSON qua `--out` trong khi nó compare-only *và tự mâu thuẫn 100 dòng bên dưới*; **bản vá đầu của tôi bịa ra đúng cái flag đang đi sửa**, bắt được nhờ chạy `--help` thật — nay mọi dòng lệnh trong epilog parse-check đối chiếu argparse (10/10). Kỷ luật: mọi comparator phải chứng minh **ĐỎ được** (tiêm `pass_rate` 1.0→0.4, `format_version` lệch → exit 1), vì tự-so-chính-nó luôn in "no differences" nên mode hỏng vẫn trông chạy được tới đúng lần đầu có việc thật. Vấp: dựng lại đề hỏng bằng trí nhớ ra 0 thực thể chứ không phải 8 — phải moi đề THẬT từ sqlite; `-q` buffer tới lúc thoát nên im lặng và crash trông giống hệt nhau. B1 (kế toán chi phí) phát hiện ở vòng này, **đã vá 2026-08-31** — xem hàng dưới. Cổng: 4265 BE passed, 69 skipped, ruff sạch. |
| 2026-08-31 | [Vá B1 + B7, và truy nguyên vụ Gmail](260831-b1-b7-fix-va-gmail-root-cause.md) | ✅ Done | **B1**: hai màn hình chi phí cộng hàng CaptureStore, lệch với số cap thật sự hành động — lệch **cả hai chiều** (thừa lần retry bỏ dở, thiếu decompose/aggregate). Đo trên task thật `30cbc8baa90d`: CEO thấy $0.0349 trong khi cap dùng $0.0323. Cả hai surface (`control_plane_views` + `routes_outputs`, FE đọc cái sau) nay đọc chung `task_cost_total` → `sum_cost`; vá một nơi thôi thì hai màn hình cãi nhau, tệ hơn trước. **B7**: `react_loop` nhập `deepagents` (extra tuỳ chọn) vô điều kiện → `loop_engine: langchain` chết trên bản cài mặc định; cho degrade, và phải dời việc dựng middleware lên TRƯỚC prompt để không hứa model một tool nó không gọi được. **Vấp**: lần kiểm-đột-biến đầu in "10 passed" làm tôi tưởng test yếu — thật ra tôi đột biến nhầm **nhánh không chạy** (fixture không có `captures.sqlite3` nên hàm thoát sớm); đúng chỗ thì đỏ ngay, và lộ ra nhánh production **chưa hề có test** → đã bổ sung dựng đúng hình lỗi thật. Một test cũ đỏ hoá ra là **fixture thiếu nhất quán** (ghi capture cho task không tồn tại trong TeamTaskStore), sửa fixture chứ không nới assert. **Vụ Gmail qua Telegram: không phải bug** — `secretary` bị CEO cắt gws từ v71 vì từng lộ dữ liệu cá nhân trong UAT; đã loại trừ "thiếu tool" (có `gws.gmail`) và "OAuth hỏng" (chạy `gws gmail +triage` thật, đọc được). Không tự bật lại vì đó là đảo quyết định CEO. Cổng: 4270 BE passed, 70 skipped, ruff sạch. |
| 2026-08-31 | [v92 — cờ `needs_mail`: chặn giao việc cần tool mà người nhận không có](260831-needs-mail-capability-gate.md) | ✅ Done | Nối tiếp vụ Gmail hàng trên: CEO **chốt bật lại** quyền Google cho `secretary` (đảo v71 của chính mình — gốc rễ vụ lộ dữ liệu UAT nằm ở `schedule` + `memory`, cả hai VẪN tắt, không phải ở gws), verify sống `build_read_toolset` bind `['gws.calendar','gws.drive','gws.gmail']`. Lưu ý đánh đổi: không có nấc read-only — `gws_enabled` gác cả đọc lẫn ghi, nên bật đọc là mở lại cả ghi; hàng rào thật là allowlist (`gmail +send`, KHÔNG reply/forward) + Lớp B có audit. Rồi vá gốc: task đó đốt **$0.029 chỉ để trả lời "em không có quyền"** vì không cổng nào bắt "đề bài cần tool mà người nhận không có". Thêm **cờ thứ ba** `needs_mail` — mẫu đã có sẵn 2 lần (`needs_shell` v64 gác plan-time, `needs_web` v74 gác reassign-time), cờ mới gác **cả hai**. **Suýt làm tính năng chết ngay từ đầu**: `resolve_step_runtime` ép bước `work` thường xuống native, mà native KHÔNG BAO GIỜ nhận `gws_context` (`team_step_runner.py:557` chặn `_extra` cho non-native) ⇒ không sửa router thì bước mail bị tước đúng cái tool nó vừa khai báo, thất bại y hệt task $0.029 — **cờ mới phải hỏi "tool đi đường nào tới bước việc", không chỉ "ai được cấp quyền"**. Hệ quả: probe `agent_mail_capable` cố ý **không** kiểm tier (khác `_web_search_enabled`) vì tier là thuộc tính của BƯỚC, kiểm ở đây sẽ loại oan agent native-pinned. Hash bind **có điều kiện** như `needs_web` ⇒ DAG cũ hash byte-y-hệt, không stall lúc migrate. Kế thừa cờ ở mọi chỗ mint dòng mới (review/fanout sub/rework/thay-người-chết/amend swap) vì repo đã ghi sẵn 2 ca chết thật do quên đúng việc này với `needs_web`. **Vấp**: đoán tên hàm private `_plan_hash_from_rows` thay vì tra — tên thật `_confirmed_plan_hash`. Mutation ×5 đều đỏ đúng chỗ, 2 phát cuối đỏ ở **hai assert khác nhau** của cùng một test. Backlog: thêm mục nghiên cứu harness openhuman. **E2E sống 2 fleet cùng 1 brief** (seam mới `seed_home(..., mail_capable={...})`, mặc định `frozenset()` ⇒ mọi ca cũ giữ fleet byte-y-hệt): M1 không ai đọc được thư → vẫn lập được kế hoạch và giao cho `writer`, **không** ném lỗi lên CEO (guard raise BÊN TRONG vòng retry decompose nên model tự lập lại); M2 chỉ `secretary` đọc được → không bước nào lạc chỗ. Đọc cờ **thẳng từ sqlite** vì `build_task_status` không project `needs_mail` — assert qua HTTP sẽ xanh rỗng trong MỌI thế giới. **Ba lần đoán sai nguyên nhân timeout, mỗi lần log bác bỏ**: đổ cho guard ép retry (log: M1 retry 1, M2 retry 0), rồi đổ cho brief 6 phần (rút 3 phần vẫn timeout, 0 retry); số đo mới đúng — `confirm: true` gộp preview+confirm vào **1 request đồng bộ** nên mọi lượt thử trả giá trước khi POST trả về, M2 duyệt-ngay tốn $0.062/lần decompose còn M1 buộc ≥2 lần ⇒ ceiling 180→900s (tách 2 bước preview/confirm **vô ích**, preview vẫn decompose y hệt). **Kiểm đột biến trên ca sống lộ hai assert không ngang nhau**: vô hiệu guard rồi chạy M1 thật ($0.063) → assert "không bước nào đặt `needs_mail`" vẫn XANH (planner ra kế hoạch không có bước mail nào), chỉ assert log đỏ ⇒ assert log mới là thứ chứng minh bất biến **đang chịu tải**; unit offline đỏ cả 2 nhánh nhưng tiêm thẳng `capable_ids` nên không trả lời được câu này. **Lỗ chưa vá (báo, không nới assert)**: nhánh xử lý câu trả lời bị cắt cụt chỉ kích hoạt khi `finish_reason == "length"`, chạy sống gặp ca body cụt giữa field mà KHÔNG có tín hiệu đó ⇒ rơi vào nhánh "JSON sai", model viết lại đúng kế hoạch quá dài, đốt 1 trong 4 lượt. Cổng: 4278 BE passed, 70 skipped, ruff sạch. |
| 2026-09-01 | [Benchmark live 5 trục + 2 bug (sản phẩm & harness)](260901-live-benchmark-5-truc-va-bug-cost-cap-note.md) | ✅ Done (3/5 trục; L3/L5 xanh) | Chạy 4 journey live (j1, j1b, j2, j5) hai vế baseline `c00ca30` ↔ candidate: **0 vượt ngưỡng** ở cả 3 trục đo được (chi phí / thời gian tường / số lần gọi model), `llm_calls` + `terminal_state` khớp tuyệt đối. **Bug 9 ký tự do bộ live tìm ra** (`397fecb`): `COST_CAP_GAP_NOTE` không kèm prose dài **191** ký tự, sàn cứu nháp `_MIN_DRAFT_SALVAGE_CHARS` là **200** ⇒ `_salvageable_draft` vứt luôn ghi chú, step bị drop ghi đè artifact bằng placeholder, mọi bước phụ thuộc thấy "KHÔNG CÓ KẾT QUẢ" mà **không có lý do**. Hỏng **đúng lúc guard siết mạnh nhất**: cap nổ ở vòng 0 thì chưa kịp sinh prose nên note là TOÀN BỘ `result_text` (191); cap nổ muộn thì note bám vào prose sẵn có (502) nên qua sàn — tức là ca tệ nhất mới lộ. 14 ca cost-cap offline không thấy vì chúng stub qua nhánh drop. **Không hạ sàn** (sàn đang lọc stub thật, có test `test_drop_without_a_real_draft_writes_the_bare_placeholder` ghim `"Lỗi: hết lượt web."` phải bị vứt) mà **miễn trừ riêng** ghi chú do code sinh; marker lấy từ `COST_CAP_GAP_NOTE.split("{")[0]` chứ không chép tay, để đổi câu chữ sau này không để scan khớp vào chuỗi guard không còn phát ra. Tái hiện lại được offline ⇒ phát hiện đắt tiền thành check miễn phí lặp lại được. **Ghi thẳng "chưa đo được" cho 2 trục thay vì nới assert**: trục 4 (cap dep vào prompt) L2 timeout 1282s > 900s — **không phải đỏ assert, cũng không phải skip**, là loại kết quả thứ ba "chưa bao giờ chạm tới điều kiện đo"; brief phải đủ nặng để dep vượt 8000 ký tự nhưng đủ nhẹ để settle. Trục 5 (chất lượng) hủy phiếu **2–2** của `quality_judge` dù nhìn rất gọn: heuristic "artifact dài nhất" bốc nhầm **bài phê bình của reviewer** 2/8 file (review của câu trả lời tệ thì dài hơn chính câu trả lời), đổi sang "step cuối" thì trúng stub `waiting_clarify` 145 ký tự, hai vế lại chạy **hình dạng kế hoạch khác nhau**, và 7/8 kết cục là từ chối vì fleet không có web ⇒ judge đang chấm "bên nào từ chối hay hơn". Trước khi tiêu tiền có verify **đề bài byte-y-hệt** giữa hai vế (65/129/97/202 ký tự) và bản `step_texts` inline **AST-body y hệt** bản repo (helper này MỚI nên import trên baseline sẽ `ImportError`) — nếu không, trục 5 đo chính cái harness. **Vấp**: J5 phân loại sai **ba lần** ("quá tải harness" → sai; "baseline chậm hơn có hệ thống" → sai, mẫu 4 settle 192.9s rồi fail ở assertion KHÁC; mẫu 5 pass 120.86s nhanh nhất) — dừng ở mẫu 4 thì đã viết ngược hẳn vào báo cáo; `git diff` trả lời **miễn phí** câu hỏi tưởng phải mua bằng tiền (`worker coordinator/team-tick failed` nghi hồi quy → diff `team_tick_runner.py` + `coordinator/` **rỗng** ⇒ lỗi có sẵn); và tôi **bịa tên test** từ trí nhớ trong báo cáo, grep ra 0 khớp, phải tra tên thật rồi sửa. **Bug thứ hai, lần này ở HARNESS** (`928f496`): L3/L5 chạy live ra cặp `E.` — L3 timeout 900s, L5 pass — mà hai case dùng CHUNG fixture `tool_journey`, nên L5 pass trên một journey vừa timeout là **bất khả thi**. Hoá ra cả `tool_fleet` lẫn `tool_journey` để **scope mặc định (function)**, trong khi docstring module ghi rõ *"ride ONE journey... paying twice would measure two different runs while claiming to cross-check one"* ⇒ mã làm **đúng cái điều nó nói nó tồn tại để tránh**. Thiệt hại tiền (2 journey nghiên cứu, 1675s) là phần nhẹ; phần nặng là L5 đem `collect_tool_stats` so với transcript của **run khác**, tức bất biến "hai writer độc lập trên CÙNG tập row" bị rỗng mà **vẫn báo xanh**. Sửa: `scope="module"` cho fleet + tách `_tool_journey_run`, giữ `journey_budget` ở function scope vì nó dùng chung với mọi file journey khác và key theo `node.name`. Lần sửa đầu đỏ ngay: `ScopeMismatch` (module-scoped không request được function-scoped `live_api_key`) → thêm `live_api_key_module`, cả hai gọi chung `_live_api_key()` để không thể đọc key khác nhau; đáng nhớ là **`--collect-only` KHÔNG bắt được** lỗi scope (giải quyết lúc setup, không phải lúc collect). Sau khi sửa: **2 passed, 1132s**, một journey phục vụ cả hai case, và pass không rỗng (L5 đỏ nếu transcript không có `tool_call` nào, rồi mới so `counted == len(recorded)`) ⇒ trục audit trail + stats **đo được ở live**. Cổng: 4395 BE passed, 70 skipped, 0 failed, ruff sạch. |
| 2026-09-01 | [Release readiness gates → cắt 0.16.0](260901-release-readiness-gates-0160.md) | ✅ Done 4/5 trục | **Đóng trục 4 ở live cả hai nửa** sau khi vòng bench 260831 phải ghi "chưa đo được": L2 (cap dep 8000 ký tự) xanh 2 mẫu ($0.006516/502.1s · $0.012265/1029.5s) — mất **7 vòng**, không vòng nào vá bằng cách nới assert (vòng 1 bug sản phẩm thật `8d58e52` handoff rỗng do sai data root · vòng 2 deadline 900s ngắn thật, nâng 1500s và vòng 7 chạy 1029.5s chứng minh mốc cũ sẽ trượt oan · vòng 3 thiếu tiền đề lane · **vòng 4 sai ở CHÍNH PHÉP ĐO**, so hai đại lượng khác đơn vị · vòng 5 injector bắn một phát trong khi artifact **không write-once**, vòng rework ghi đè lại). L4 (stash tool result >12k) xanh 2 lần chạy $0.0062/lần với ngưỡng `TOOL_RESULT_STASH_CHARS` **giữ nguyên 12.000** — chuỗi dài cấp bằng fixture chứ không hạ ngưỡng cho dễ chạm. L1b giữ `needs_web` 5/5 nên brief không cần sửa. **Sản phẩm tự đánh dấu bài nộp cuối**: cờ `final_deliverable` suy từ hình dạng DAG (`team_task_steps.py:341-343` — step không là dep của ai), thay heuristic "artifact dài nhất" từng bốc nhầm bài reviewer; harvest chỉ nhận `via flag` khi **đúng một** row mang cờ, gặp nhiều terminal thì **từ chối chứ không đoán**; live 4/4 case harvest via flag. Cờ **không** vào `plan_hash` vì nó là thuộc tính hình dạng, đưa vào sẽ làm hash cũ stale vô cớ. **Nhưng trục 5 vẫn ghi "chưa đủ mẫu", không ghi "không hồi quy"**: chỉ 2/4 case chấm được (cần ≥3), n=2 **hoà 1-1** kèm confound độ dài. Nguyên nhân là **OpenAlex trả HTTP 429** — rate-limit theo IP sau ~14 brief, xác nhận bằng `curl` chứ không phải suy đoán, và **không phải** hồi quy sản phẩm. Đường đi tới kết luận đó là cái đáng nhớ: **giả thuyết đầu của tôi SAI** — đoán 2 case hỏng do tranh provider, chạy lại lúc máy rảnh thì **hỏng y hệt**, lúc đó mới đi đọc transcript (19 lần "429" cạnh `academic_search`) rồi verify; đoán-rồi-thử-lại rẻ hơn đọc log **chỉ đúng lần đầu**, lần hai phải đi tìm bằng chứng. **Suýt chấm dữ liệu vô nghĩa lần thứ hai**: byte count 4 file nhìn đều bình thường, mở nội dung mới thấy 1 file là marker `waiting_clarify` (không phải bài nộp) và 1 file là **lời từ chối do chính 429 gây ra** ⇒ kích thước file không chứng minh file có nội dung. Cũng **suýt chấm dữ liệu ôi**: thư mục `deliverables2-*` còn file 00:01 của vòng bị bỏ dở, đã cách ly. Và tôi **nghi cờ `final_deliverable` bắn sai** (nó chấm `review_final`) rồi **tự bác bằng code**: cờ suy từ DAG và bỏ qua loại step ⇒ planner thật sự làm step review thành terminal — không phải bug. **Cắt bản 0.16.0**: CHANGELOG tách rõ default-on ↔ opt-in (`b4c1845`, `cost_cap_usd` mặc định `None` cả 3 tier và chỉ `thin_tool_loop` enforce — không hứa quá), baseline journey 4 chặng (`8e08e9c`), tag local `v0.16.0`. **Baseline lần cắt đầu bị dán nhãn sai version** — file tên `0.16.0` ghi `"version": "0.15.0"` — và **không phải lỗi code**: `conftest._version()` đọc metadata **bản đã cài**, không đọc `pyproject.toml`, mà editable install không tự refresh khi bump; hàm làm đúng ý đồ (docstring: nhãn sai version còn tệ hơn nhãn "uninstalled") ⇒ lỗ hổng ở **quy trình**, thêm bước reinstall trước khi cắt baseline vào checklist releasing. Lần cắt đó cũng `1 failed` mà notification báo **"exit code 0" — lần thứ 4 trong vòng này**; j1 `TimeoutError` ở `topology.py:238` là timeout của HTTP client trong test, không phải assert sản phẩm, và tôi **không kết luận** là nhiễu cho tới khi chạy lại lúc máy rảnh ra **9 passed**. Baseline giữ nguyên 2 số khó nhìn thay vì làm đẹp: j1 đổi `parked:open`→`done` (test **cố ý** không pin terminal state — docstring dòng 9: tránh assert *how the crew chose to do the work, because that is a model choice*) và j2 `cost_usd 0.0` dù 1 llm_call (`total_cost_usd` lấy từ `sum_cost` — sổ step mà cost cap enforce — còn escalation là one-step vehicle nên call rơi vào capture; tái hiện cả 2 lần cắt, đặc tính **có sẵn** không phải thay đổi của 0.16.0). Tiện thể extra `deep` **lại** biến mất khỏi venv (trùng hiện tượng journal v56), khôi phục 13 gói. Cổng: BE 4404 passed/70 skipped · FE 417 vitest · ruff sạch · tsc 0 · cold-start smoke `--browser` 6/6. **Chưa push gì**: ~50 commit + tag đều còn local, chờ duyệt. |
| 2026-08-29 | [Review-cap salvage + verdict-lock](260829-review-cap-salvage-verdict-lock.md) | ✅ Done | **Trần soát chéo không giữ con tin task đã xong nữa.** Hết lượt soát/sửa → chuỗi soát kết thúc lặng lẽ (zero side effect — row done re-inspect mỗi tick) và task TỰ GIAO kèm header code "Soát chéo chưa đạt: …" quote ý kiến reviewer còn bỏ ngỏ, trên cả 3 đường giao; xóa cảnh lanes9b 3/4 case stall dù content done 100%. Aggregate suy cap-exhaustion từ bất biến "verdict trượt + không rework ≥ vòng" (tương đương chính xác vì dưới cap luôn mint rework). Fix verdict-lock: re-mint verdict-None mang `source_step_id=deps[0]` — re-review vòng ≥1 chấm đúng bản rework thay vì bản gốc. Harness bench patch thêm `settings.DATA_DIR` bịt leak band store thật của repo. Autopilot hết việc trên path này (`attempts == 0`); ladder các nguồn stall khác giữ nguyên. Test pin stall cũ nằm rải 3 tầng — full suite mới bắt hết. 4045 BE, ruff sạch. |
| 2026-08-26 | [Fast/crew lane routing v2 — live suite, benchmark v2, và cái gate chưa từng tồn tại](260826-fast-crew-lane-routing-v2-live-suite-benchmark.md) | ✅ Done | Khép plan 6 phase. **Live fullflow suite** 18 case chạy model thật, assert trên **route record** (`route_json` + hình dạng DAG) chứ không phải prose — thứ duy nhất ổn định qua model nondeterminism; opt-in `-m live`. **Lỗi nặng nhất của cả plan là cái gate an toàn chưa từng chạy**: `pytestmark` đặt trong `conftest.py` bị pytest **bỏ qua hoàn toàn** — không lỗi, không cảnh báo — nên `-m live` chọn ra **0/18** case: máy không key sẽ ĐỎ vì lỗi xác thực thay vì skip, máy có key thì `pytest` trần tiêu tiền thật. Cả phase không ai thấy vì tài liệu mô tả cái gate *lẽ ra phải có* còn mọi lần chạy đều né thư mục bằng `--ignore` thủ công; nay dán marker bằng `pytest_collection_modifyitems` + `addopts = ["-m", "not live"]`, đo cả hai chiều. **Đếm đầu việc đánh số giữa câu**: cùng ba việc, xuống dòng thì ra team, viết liền một câu thì ra sprint — quyết định phụ thuộc CEO có bấm Enter hay không. Chỉ nhận dạng CÓ NGOẶC (`(1)`/`[1]`/`1)`), loại `1.` trần vì giữa câu tiếng Việt nó gần như luôn là số thứ tự của DANH TỪ ("Điều 1. Điều 2." bị đếm thành 3 việc → đẩy nhầm sang lane đắt); dò dãy liên tiếp ở BẤT KỲ đâu nên một con số lạ đứng trước không tắt được khả năng phát hiện. **Lane stats** đo 3 miss-rate trên `routed_tasks` — chia cho TẤT CẢ task khiến chỉ số **tự đẹp lên** khi store dài ra. **Benchmark v2** 4 mode (`routing`/`release` 0 call, `tasks` đọc store, `judge` chấm mù); judge luân phiên vị trí tất định vì đo trên 2000 seed thấy ~24% số lần cả ba phiếu cùng thứ tự — đúng lúc "bias tự triệt tiêu" là sai. **Vấp**: subagent viết docs tự báo "đã verify mọi tên hàm với code", đối chiếu ra **5 khẳng định sai**, 3 cái *đảo ngược* thiết kế thật (nói `upgrade` dùng lại `routed_task_id` — thật ra cố ý dựng task MỚI vì kế hoạch sprint là DAG đã chốt hash; nói nháp sprint làm "starting point" — thật ra cố ý chỉ làm THAM KHẢO), văn phong khớp hoàn hảo docs viết tay ⇒ **phần kém tin cậy nhất của output một agent là lời nó tự khai đã kiểm chứng**. 3 commit: `fix(ops)` · `feat(bench)` · `docs`. Cổng: 3991 BE passed, 1 skipped, 18 live deselected, ruff sạch. |
| 2026-08-11 | [v78 — Phễu định tuyến Team vs Sprint](260811-v78-routing-funnel.md) | ✅ Done | v77 để lại một router **đoán một lần rồi thôi**: whitelist từ khóa, nghi ngờ thì đẩy team. Vòng này đổi thành **phễu 6 lớp** — tiền tố CEO → refusal cứng → heuristic cấu trúc → downgrade sau decompose → dead-end → `route_json` — để bản thân phép đoán không cần đúng, mỗi hướng sai đều có lưới đỡ riêng. **Lật mặc định sang sprint** (v77: nghi ngờ → team; v78: nghi ngờ → sprint) vì chi phí route sai **bất đối xứng**, đo ở `benchmark-260810-1602`: sai về sprint thì dead-end kéo về sau vài phút; sai về team tốn **20m14s/$0.0757 vs 7m48s/$0.0191** trên CÙNG đề, chấm mù **10 vs 29**, *và không ai biết* — không có tín hiệu nào báo rằng việc này lẽ ra một người làm là xong. Heuristic mới đọc **cấu trúc** thay vì từ khóa (>1200 ký tự · >10 thực thể · ≥3 đầu việc tách dòng) đúng vì whitelist từ khóa là thứ vừa fail: đề tự nhiên "cho tôi biết giá X, Y, Z hiện nay" không chứa từ nào trong đó. `downgrade_to_sprint` đặt **SAU decompose** — kế hoạch model vừa dựng là bằng chứng mạnh hơn mọi heuristic đọc chữ (nó đã biết roster, đã chia việc), suy biến ≤2 bước/1 người/tuyến tính thì kéo về sprint, **0 lượt gọi model thêm**, nghi ngờ thì trả None. **3/3 ca live đúng** trên daemon thật: đề tự nhiên → sprint/heuristic; 3 bullet → team/heuristic; `sprint:` + "gửi email" → **team/refusal** — tiền tố chọn CHẾ ĐỘ, không mua được đường vòng qua review bắt buộc. Ca 3 còn kẹt giữa chừng rồi được hệ **tự chỉnh kế hoạch**, và bước ghi-ra-ngoài trong plan MỚI vẫn giữ `external_write=True`+`needs_review=True` — bất biến an toàn sống sót qua replan, không chỉ qua đường assign. **Tải review GIẢM chứ không tăng** (lo phình là lo sai: review mint theo content step, sprint chỉ có một; cặp benchmark v77 sprint 2 vs team 6). VẤP: ca thứ tư dựng riêng để thử lớp downgrade **không kích hoạt nổi** — đề 1335 ký tự cho việc một người, decompose trả 4 bước/2 người (tách mỗi dịch vụ một bước + thêm `qa` riêng), vượt cả `_MAX_DEGENERATE_STEPS` lẫn guard cùng-một-người; guard từ chối là ĐÚNG hợp đồng nhưng nó chỉ cứu plan **đã suy biến sẵn**, không kéo ngược được plan **đã bị thổi phồng**. Lật mặc định làm 2 test cũ đỏ vì đề của chúng đổi hướng — đỏ đúng chỗ nên đỏ. 3194 BE, ruff sạch. **NGHIỆM THU (2026-08-11, 18 task live, $0.86/$1.50)**: 13/13 ca định tuyến PASS, cả 6 lớp phễu có ca sống — kể cả hai lớp trước nay chưa từng bắn: nhánh entity (12 thực thể) và **decompose-downgrade**, tức cái VẤP ở trên đã có ca sống. **0 bug chặn phát hành**; hai mâu thuẫn nghi là bug điều tra ra đều không phải: (a) `route_json=downgrade` nhưng bảng step có 3 `work` — vì dead-end **lật route rồi lập lại kế hoạch**, nên hàng step lúc terminal mô tả kế hoạch SAU fallback, mọi audit đọc step suy ra route đều báo dương tính giả; (b) sprint × band trusted = **0 review** — không do band mà do `_build_sprint_task` hardcode `needs_review=False` giao với việc trusted waive review nội bộ, mà sprint 1 bước thì nội bộ là tất cả. Refusal cứng **lọt 2 họ đề** ("clone repo chạy bộ test", "cần cả team cùng làm") nhưng lưới dead-end che được cả hai — đúng luận điểm nhan đề. Hai ca đắt nhất đều là team và đều do **vòng review→rework→review** (một ca 11 review + 7 rework trên 6 bước nội dung), không do chia nhiều bước. Nợ v76 (trusted×external_write) **không dựng được ca nên còn treo** — ghi rõ thay vì nhận bừa; đổi lại làm sáng kiến trúc egress: cờ `external_write` ở step chỉ là khai báo định tuyến review, quyền gửi thật nằm ở opt-in profile `team_step_egress` → gateway. **7** lần độc lập agent từ chối bịa dữ liệu, gồm một lần ở tổ hợp quyền cao nhất (trusted + 0 review + sprint). **BENCHMARK 3 cặp chấm mù**: cổng định trước (sprint thắng-hoặc-hoà ≥2/3) **ĐẠT 2/3** — C1 content sprint thắng (cost 52%, điểm 95%, nhanh 2.95×), C2 phân tích sprint thắng **cả hai trục** (cost 18%, điểm 126%), C3 research team thắng đậm (24.5 vs 9.5). Cộng 2 cặp cũ: **sprint thắng 4/5**. Số đắt giá nhất: nửa team C2 chạy 6 review + 3 rework mà judge vẫn bắt lỗi số học trong bản giao — **thêm 9 lượt soát/sửa không mua được độ chính xác**. C3 là cặp team thắng duy nhất, nhưng truy nguyên nhân (ghi TRƯỚC khi chấm) ra **năng lực bước, routing vô can**: team tự tách bước lúc chạy nên có nhiều lượt tra cứu hơn, sprint một bước không có đường làm thế → **không** phải cớ hạ ngưỡng 1200. HAI VẤP ĐO LƯỜNG: (a) A9 chốt **$0.179 vượt trần $0.15** dù đã cancel lúc ~$0.13 — `cancel` không phải phanh, đọc code xác nhận `_kill_pid` chỉ bắn khi lease hết hạn nên bước đã spawn chạy nốt và tính tiền tiếp; muốn trần cứng phải chặn lúc mint bước. (b) chấm mù C3 khi task chưa terminal là **chấm nhầm bản** — bước cuối `approval` xong sau đó viết lại toàn bộ bản giao; chấm lại trên văn bản cuối sprint 11.0 → 9.5, phán quyết không đổi. **HẬU NGHIỆM THU (CEO ủy quyền)**: 7 mục chờ đã quyết theo vision — sprint LUÔN mint 1 review mọi band (hết đường zero-eyes); trần review/rework TẦNG TASK (2× bước nội dung, sàn 5) → chạm là stall+escalate thay vì đốt tiếp; vá 2 họ needle refusal lọt; giữ nguyên cả 3 ngưỡng routing; duyệt hướng cấp lượt tra cứu cho sprint nhưng hoãn triển khai; luật thường trực chạy lại B1 trước lần cấp egress đầu tiên; đóng cả 4 clarify trung thực — từ chối gửi email (#29, cancel task trước khi trả lời), nói thẳng buổi họp không tồn tại (#33). **DELTA-UAT (2026-08-12, $0.032)**: 5/5 ca live re-verify PASS — 3 ca sprint (2 trusted + 1 normal) đều đúng **1 review vòng 0**, chôn hẳn kỳ vọng cũ B5=0; 2 brief A4/A5 nay bị phễu bắt **ngay tại cửa** (team/refusal, trích đúng needle mới) thay vì phải nhờ lưới dead-end đỡ. Benchmark **không chạy lại** có luận cứ: cost 3 ca sprint ($0.006–0.0094) đều DƯỚI baseline 1-review cũ ($0.0098), tức luật mới đổi **tính tất định** chứ không đổi giá — điều kiện kích hoạt rerun (>2× baseline) không xảy ra. Ghi nhận phụ: một brief nháp chứa chữ "commit message" bị needle external-write bắt (route team) — false-positive đúng thiết kế, không phải bug. |
| 2026-08-10 | [v77 — Sprint mode: việc một người làm trọn](260810-v77-sprint-mode.md) | ✅ Done | Trả lời câu hỏi "sao 1 báo cáo research tốn hàng chục phút": vòng react-loop model-driven. Đo được 1 bước synthesis kiểu react tốn **780s** vs 60–120s native trên fleet model → sprint mode **code điều nhịp**: prefetch (Python chọn truy vấn) → draft (LLM) → `coverage_gaps` (Python) → targeted-search + revise ≤2 vòng. Sprint là **team task suy biến** (đúng 1 bước `step_type="sprint"`) nên thừa kế nguyên review ladder/clarify/escalate/delivery — không có nhánh runtime thứ hai. Override `sprint:`/`team:`, 4 trường hợp không override được (ghi ra ngoài, shell, nhiều nhân sự, dài hơi). **Đo thật 2 benchmark**: A 23 phút → **3m19s** ($0.059 → $0.013); B cùng đề chạy CẢ HAI mode — team **31m12s/$0.070** vs sprint **8m43s/$0.017**, chấm mù **28 vs 8** (giám khảo không biết mode, tự chỉ ra bản team thiếu hẳn phần liên kết ghi chú + không có nguồn cụ thể nào). 4/4 UAT pass: dead-end thực thể bịa → **từ chối bịa số**, hỏi CEO kèm 3 lựa chọn; brief external_write có tiền tố `sprint:` vẫn bị router đẩy sang team. **6 bug UAT tự fix**: poke-race 2 worker · prefetch không tách thực thể · cắt topic giữa cụm danh từ (`"Nghiên cứu so sánh 5 dịch Spotify"` — 5 từ nói về ĐỀ BÀI, từ thứ 6 đứt giữa cụm → search trả blog thay vì trang giá) · intake tự siết "Nêu rõ nguồn" thành "+ngày truy cập" bất khả thi (cả 3 vòng soát trượt CÙNG tiêu chí trong khi báo cáo đã đúng) · escalation chỉ nói "trượt sau 3 vòng" = thứ CEO đã tự thấy · **chain-of-thought lọt thẳng tới CEO** ~2000 ký tự — vì bỏ `make_aggregate` là bỏ luôn luật "bắt đầu NGAY bằng tóm tắt" mà lớp đó âm thầm gánh. Học: khi bỏ một lớp xử lý, phải hỏi lớp đó đang ngầm giữ luật gì. 3122 BE, ruff sạch. |
| 2026-08-09 | [v76 — Đo lường + guardrail mới](260809-v76-measurement-and-guardrails.md) | ✅ Done | Đem CAPTURE/LEARN + governance của my-dandori vào, giữ nguyên autonomy (CEO chốt): **audit hash-chain** (`prev_hash`/`entry_hash` canonical length-prefix, `mpm agent audit verify [--team]`, flock đa-worker, degrade = restart nhìn thấy) · **fail-mode contract + break-glass** (khai báo từng checkpoint gateway fail kiểu gì khi KHÔNG eval được; `MYCREW_GATEWAY_FAIL_OPEN=1` env-only chỉ nới nhánh store-hỏng Lớp B, không bao giờ nới Lớp A — test pin) · **metrics honest-data** (`team_metrics` chat: Wilson CI + n, mẫu <5 gắn `*`, toàn-pass = "chưa có ca hỏng để so", provenance `profile_hash`) · **autonomy band + closed loop bất đối xứng** (supervised/normal/trusted CHỈ đổi cổng review — bất biến pin bằng test nguồn; xấu-rõ tự siết, mơ hồ chỉ đề xuất, hồi phục tự gỡ, trusted chỉ CEO). Kiểm chứng sống: loop tự bắn qua tick thật ngay sau commit — demote `researcher` (nd 46% CI 37–56% n=106 vs fleet p90) kèm audit row có chain hash + Telegram. 3013 BE. |
| 2026-08-09 | [v75 — Coordination chủ động](260809-v75-proactive-coordination.md) | ✅ Done | Brainstorm đối chiếu OpenClaw/Hermes (khảo sát file-level) + landscape 2026: kiến trúc my-crew khớp pattern thắng thế, hơn 2 harness về đội-làm-việc first-class → không đổi kiến trúc, mượn 3 cơ chế đảo chiều an toàn. **F-bundle** (wake-context theo attempt; `web_search_outcome` tách "web nói không có" ≠ "không tới được web" → sentinel 3-path cả 2 tier; watcher `all_polls_failed`) · **goal-replan** (ladder autopilot 2→3 rung: retry → amend-LLM đổi cách tiếp cận qua đúng flow amend+hash, fail-CLOSED đảo ngược Hermes `/goal` — identity/LLM-lỗi = từ chối, stall đứng) · **hybrid collect launcher** (code prefetch 1-3 query không-LLM → inject → collect chạy native; fail-open). Kiểm chứng sống: Zetakron bịa → "THIẾU DỮ LIỆU" không bịa; fanout_gap cưỡng bức 2/2; split-sub thừa kế web=1 lần đầu; prefetch → collect **119s gap 2s** (vs 199–425s loop); task gave_up (Webex) → ladder tự cứu → done đủ 5/5. Học: sentinel phải rà MỌI tier (tool-loop có web.search riêng cùng bệnh); không copy heartbeat self-schedule OpenClaw (chết trong thực tế — mọi HEARTBEAT.md rỗng). 2981 BE. |
| 2026-08-08 | [v74 — Tối ưu tốc độ đa-agent](260808-v74-multi-agent-speed.md) | ✅ Done | Sau capstone vòng 8 sạch, brainstorm đo số chỉ ra 64% wall-clock là bước KHÔNG-tool chạy tier nặng (qa 548s deep, finalize 780s loop) + 253s chờ dispatch. Ship 3 lớp: **tier theo bước** `needs_web` (work không-web + review row → ép native; rework giữ tier agent; hint sai tự hồi phục sau ruling đầu; bind hash có điều kiện như needs_shell) · **dispatch hướng sự kiện** `tick_poke.py` (worker thoát → touch poke; service lát 5s spawn team-tick sớm, debounce + stale-safe, nhịp 60s fallback) · **fan-out ≥4 thực thể** (prompt: 2-3 collect song song, tên đích danh). VẤP đáng nhớ: flag mới phải nằm trong SCHEMA VÍ DỤ prompt — chỉ tả bằng văn thì model không emit (needs_web=0 toàn vòng e2e đầu, suýt research trên tier không search). E2e (5 trợ lý AI): wall 25,8' vs 40' vòng 8, $0.083, 2 collect ∥ thật, gap dispatch 1–17s (trước 253s), qa/finalize/rework native. 2945 BE. |
| 2026-08-08 | [Cứng hoá tầng chấm & phán đoán](260808-grader-judgment-hardening.md) | ✅ Done | Dọn sạch backlog 08-07 (audit actor, artifact lưu lý do self-check, amend deps vào frozen, DEFAULT_MODEL qwen + 4 test heartbeat hết đọc DB production) + 2 tối ưu (sanitizer 5→1 call batch fail-closed chống forge marker; digest mirror coordinator-first — MY ADMIN im lặng, mọi tin một chat pong). Vòng 7 e2e (lãi suất 5 ngân hàng) lộ + vá 5 lỗi sống: grader không biết "hôm nay" chấm dữ liệu 8/2026 thật là bịa → neo ngày; decompose thổi phồng đề → luật TRẦN đề gốc CEO thắng; gate reassign xét tier (deep_agent không mạng ≠ search được); retry adopt checkpoint chết → resume tại rework không-tool lặp nguyên văn "thư xin quyền" → redo xóa thread; researcher cạn 16 vòng → 28/profile. Retry-first coercion bắn sống 2 lần. Kết vòng 7: done "thiếu 100% dữ liệu" — trung thực tuyệt đối xuyên chuỗi drop-bước, không một số liệu bịa. Học: "xin phép tra cứu" có 2 bệnh khác nhau cùng triệu chứng (memory poison vs rework-resume). **Chiều: vòng 8 (đề nhắn lại) PASS SẠCH 5/5 lần 1, 0 can thiệp, $0.05 — capstone kiểm chứng trọn 2 ngày fix**; vá thêm nút "Huỷ việc này" hành động thật (v1 chỉ ghi nhận) + cờ delivered_direct sống sót tường lửa PII projection (hết tin ✅ đúp). |
| 2026-08-07 | [Vòng cứng hoá tự chủ — e2e 2 vòng, 7 fix](260807-autonomy-e2e-hardening.md) | ✅ Done | Từ "Chưa thấy telegram báo xong task" → 5 commit: team-tick miễn tick-cap (phán quyết >3h→~1'), fleet sang qwen3.7-plus + aggregate chống CoT-leak (Telegram từng nhận toàn "thinking process" tiếng Anh do cắt 4096), filter memory chống tự-đầu-độc ("tôi không có khả năng tra cứu web" tự ghi rồi tự đọc → refusal lặp), deep_agent network-off bỏ sanitize (nó gọt sạch URL công khai khỏi handoff), self-check kiểm THẬT tiêu chí đếm được + bước chốt fan-in deps trực tiếp. E2e 2 vòng qua đúng đường preview→confirm: vòng 1 stalled = give_up trung thực (chính nó lộ 4 lỗi), vòng 2 done 5/5 step, báo cáo 9 URL, mirror push Telegram xác nhận qua dedup key. Mở: coordinator chuộng reassign quá tay; audit search thiếu actor. |
| 2026-08-06 | [v71 — Build team nhanh: crew cá nhân](260806-v71-personal-crew-quick-build.md) | ✅ Done · còn 1 UAT chờ CEO | Yêu cầu CEO: "cần cơ chế build team nhanh" — giữ core đội hành chính, THÊM lựa chọn đội cá nhân mà trợ lý là cửa giao tiếp duy nhất (tự xử việc trong tầm, việc chuyên môn giao xuống đội). **Đảo quyết định v32 "nhiều crew là YAGNI"** có chủ đích: cái "ain't gonna need" đã tới ⇒ `crew.yaml` cứng tách thành `crews/office.yaml` + `crews/personal.yaml`, `office` là `DEFAULT_CREW_ID` nên client cũ không gửi `crew_id` vẫn ra đúng đội cũ. Thành viên nhận dạng `{role, id}` để **adopt `pong` SẴN CÓ** làm trợ lý — không đẻ bot thứ ba, skip keyed theo AGENT id chứ không theo role. Bề mặt CLI `mpm crew init [office|personal]` + web chip chọn crew (`/api/crews`). **Cổng `gws_enabled`** (profile-only, mặc định true — cùng lý do `goodreads_user_id` v70: env là fleet-wide thì không tắt nổi 1 agent) chặn CẢ đọc lẫn ghi; lệnh `gws_write` bỏ ở **CATALOG** chứ không chặn ở gateway, vì lệnh không lọt vào prompt classifier thì agent không **đề nghị** làm được — mạnh hơn từ chối sau khi đã hứa; tắt gws vẫn GIỮ NGUYÊN key snapshot (chỉ đổi giá trị "(chưa cấu hình)") để prompt cùng hình dạng giữa agent bật/tắt. VẤP LỚN: **tắt gws KHÔNG đủ để agent ngừng giữ thông tin cá nhân** — cổng xanh, đếm đúng 0 lời gọi, 2812 test pass, mà bản tin vẫn đọc vanh vách deadline hợp đồng, sinh nhật người nhà, email chủ nhân; rò từ BA đường ký ức tách rời khỏi gws (`MEMORY.md` static provider nạp nguyên văn **không có công tắc theo agent**, ghi chú ngày, recall vault kioku), phải tắt cả ba mới sạch — `MEMORY.md` **archive chứ không xoá** (dữ liệu CEO tự viết, thao tác phải đảo ngược được). Nối tiếp v66 "đã wired ≠ có điện": chỉ ĐỌC LẠI bản tin thật mới bắt được, quét 7 từ khoá nhạy cảm sau khi tắt sạch cả 7, chi phí 0.0027588 → 0.0004997. Lại đúng bẫy config layering v70 (field mới phải khai ở CẢ `loader_mapping` LẪN builder) — lần này không tin diff, chạy probe round-trip 3 ca trước khi đi tiếp. Mở: UAT giao việc thật chờ CEO nhắn tay; **đường trả kết quả team task hiện CHỈ PULL** (không path nào gọi `notify_operator_best_effort`, `assigned_by` lưu chuỗi cứng `"ceo-chat"` = nhãn audit không định tuyến được) ⇒ đề xuất vòng sau, không tự mở rộng scope. |
| 2026-08-05 | [v70 — Trợ lý cá nhân `pong`](260805-v70-personal-assistant-pong.md) | ✅ Done | Thay OpenClaw personal assistant bằng agent thứ hai trên `personal-pack`: `pong`, bot Telegram riêng để không nhiễu `secretary` đang UAT. CEO chốt 3 quyết định: chỉ hồi sinh **3 job còn sống** (briefing 7:00, weekly CN 8:00, memory daily-notes) — 8 job chết từ tháng 3–5 không dựng lại vì chưa ai thiếu; dùng SQLite store sẵn có; agent mới + bot mới. Nguồn snapshot đặt ở **tầng pack** nên `secretary` cùng hưởng: `tasks_pending`/`tasks_completed` (gws) + `goodreads_read.py` mới (RSS kệ công khai, stdlib thuần, không key mới). `PersonalToolProvider.read` thành **kind-aware** — mọi kind 7 nguồn dải ngày, `weekly-review` cộng dải 7 ngày thành 11 (lịch tuần, việc đã xong, sách trong tuần, bài học v69); briefing KHÔNG trả giá nhóm tuần (test đếm-lượt-gọi, mutation-proven). VẤP: thiết kế đầu định nhét `goodreads_user_id` vào `integrations:` — sai, block đó coerce sang `McpServerSpec`, key không map trong `build_reporting_dict` thì **không bao giờ** tới config, hỏng không kêu; chốt field phẳng top-level, **cố ý không env fallback** (kệ sách của MỘT người, env là fleet-wide ⇒ đặt biến là `secretary` âm thầm đọc kệ CEO). Review bắt 2 lỗi im lặng: `tasks_list` thiếu `maxResults` trong khi mọi helper gws khác đều có trần, mà lọc `completed` chạy SAU khi API cắt trang ⇒ danh sách bận đẩy việc đã xong khỏi trang đầu và weekly báo "(không có)" — sai chứ không lỗi; và **đo thật** thấy weekly snapshot 5470/6000 ký tự với riêng `unread_email` chiếm 4248 (78%), `render_snapshot` cắt phẳng theo vị trí ⇒ nhóm tuần đứng sau mất trước → đưa hộp thư xuống CUỐI. Tự gây: `git checkout --` lên file có thay đổi chưa commit xoá sạch `tools.py` v70 (khôi phục được). UAT thật cả 2 kind trên data sống; `secretary` không hồi quy; không cần restart daemon (registry đọc lại mỗi tick, `.env` mỗi worker). Telegram cấm bot mở hội thoại trước (`chat_id` chỉ tồn tại sau khi user nhắn lần đầu) — CEO `/start` xong thông ngay, **DM THẬT cả 2 kind** (`delivered=True telegram=executed`, briefing $0.0024 + weekly $0.0014) trên data sống: 10 sách Goodreads, 201 email chưa đọc, task Strava quá hạn 2019. **UAT nội dung bắt lỗi 2802 test xanh không thấy**: weekly viết "Nguồn chưa nối: Goodreads" trong khi `goodreads_activity_7d` đang cầm sách thật, còn bịa thêm GitHub/LinkedIn/Substack — vốn chỉ là NGƯỜI GỬI trong hộp thư; prompt bắt viết dòng "nguồn chưa nối" mà không định nghĩa thế nào là chưa nối ⇒ model tự đoán. Sửa cả 2 prompt (chỉ gọi tên key mang đúng "(chưa cấu hình)", key có nội dung thật là ĐÃ nối, cấm suy nguồn từ tên người gửi) → chạy lại dòng sai biến mất, sách vào đúng mục. Đúng lớp honest-drop v64: snapshot đúng nhưng bản tin nói sai. 2802 BE, ruff sạch. |
| 2026-08-04 | [v66 — Cross-agent memory SQLite](260804-v66-cross-agent-memory-sqlite.md) | ✅ Done | Phiên thiết kế → ship trong ngày (3 quyết định CEO: thư ký CHỈ ĐỌC · nhóm 8 agent `project: company` · default store sqlite). Scout đổi cỡ bài: máy móc v2-era đã có (remember node, sibling read cap 40) — chỉ thiếu backend bền + group. Backend `store: sqlite` = 1 file chung WAL (langgraph SqliteStore, isolation_level=None); `memory_share: read_only` cho secretary (đọc đội, đời tư CEO không vào prompt ai); block sibling qua format_internal_content (fact bền = injection surface lâu dài); retention 90d qua Store API. **UAT sống lộ 2 dây chưa cắm** unit test không thấy: graph team-step compile thiếu `store=` (fact rơi hư không hàng tháng không ai hay — "đã wired ≠ có điện") + 6 agent office còn `dry_run: true` template sót gate mất remember. Chốt sống: researcher persist 20 fact thật, coordinator đọc 40 fact/6 sibling, secretary bị loại khỏi nguồn đúng luật. Mở: extractor tách fact theo dòng (bảng markdown → 20 fact vụn) cần vòng chất lượng. 2525 BE. |
| 2026-08-04 | [v65 — Nhắc hẹn giờ + đóng đuôi treo](260804-v65-timed-reminders-and-tail-closures.md) | ✅ Done | "Giải quyết nốt việc treo" (CEO): **nhắc-việc-theo-giờ** end-to-end ("đặt nhắc lúc 15:51…" qua bot → "⏰ Nhắc hẹn" về Telegram thật, row sent) — lệnh M12 `set_reminder`/`cancel_reminder` → native type actor-bound + Lớp A riêng → `reminders.db` per-agent → pseudo-kind `reminder-sweep` mỗi phút chỉ mọc khi còn pending, no-LLM, dedup. **Đuôi v64**: round-robin stateless (hoạt-động-cũ-nhất trước, nuốt rule chống-đói) · `_approval_status` scope theo assigned_to. VẤP kép đáng nhớ: (1) ops-classifier nuốt lệnh M12 mới → personal unsupported giờ RƠI XUYÊN xuống catalog M12; (2) trần spawn/tick làm `reminder-sweep` đói ĐỊNH MỆNH (deferral + thứ tự ổn định lặp mỗi phút) → kind no-LLM tức thời được miễn trần. Memory chung: CEO chốt SQLite-trước, chờ phiên thiết kế. 2518 BE. |
| 2026-08-04 | [v64 — Vá 4 finding UAT vòng 3](260804-v64-uat-findings-hardening.md) | ✅ Done | UAT pattern phức tạp (fan-out ∥, needs_shell, clarify) lộ 4 finding, CEO chốt 4 phương án cùng ngày: **honest-drop** (placeholder cấm suy diễn + luật trung thực ở SYSTEM/aggregate — vá chuỗi "fail-closed → drop → bước sau BỊA 502/1000 rất thuyết phục") · **shell guard plan-time** (`sandbox_capable_ids`/`validate_shell_steps` trong decompose+amend retry — model tự bỏ cờ khi đội thiếu sandbox; bật `deep_agent` Docker cho analyst) · **terminal review policy** (chỉ terminal + external_write được soát, CODE quyết — hết nổ 5 bước→23 dòng; amend bắt validated thay vì vứt) · **chống đói queue** (task chưa-chạy phục vụ trước — đo được 2 task đứng im 40'). UAT sống: chuỗi viết-code→**chạy Docker thật** (engine=deep_agent, 532/468 ngẫu nhiên thật)→tổng hợp giữ đúng số; clarify pause-ask-resume trọn vòng. 2506 BE. |
| 2026-08-04 | [v63 — Autopilot + review theo cỡ việc + gỡ-stall](260804-v63-autopilot-tiered-review-stall-recovery.md) | ✅ Done | Từ research đối chiếu chuẩn 2026 (supervisor/HITL): cơ chế đã đạt/vượt chuẩn, yếu ở CHÍNH SÁCH. **Review theo cỡ việc**: `external_write` (hash CONDITIONAL như needs_shell), waiver ≤3 bước nội bộ (CEO chốt "kết hợp cả hai"), `passed_with_notes` không mint rework. **Gỡ-stall một chạm**: accept/retry/drop + evidence pack trong escalate. **Autopilot "Toàn quyền thật"** (CEO chốt, được khuyên mức thấp hơn nhưng quyết full): tự confirm (đường auto_confirm sẵn có), sweep thang retry→accept/drop trần 2 lượt không LLM, tự duyệt Lớp B scoped đúng store agent; opt-out "để anh duyệt"; audit qua office event → admin mirror. `list_team_tasks` + retro. Reviewer đối kháng bắt **C1 retry-vô-dụng** (test tự viết che bằng re-stall mô phỏng — bài học: hành vi xuyên-tick phải test bằng tick thật) + H1 approval-id per-FILE không unique + H3 amend rớt flag hash. UAT sống: `9ee8a4f028f0` gỡ qua Telegram-path, autopilot tự accept nấc 2, task nhỏ mới 0 dòng review; lộ vấp v62: plan_hash task terminal lệch CẮN khi reopen — migration sau phải recompute hash cả task terminal. 2499 BE. |
| 2026-08-04 | [v62 — English hoá định danh](260804-v62-english-identifiers-fleet-rename.md) | ✅ Done | "Tất tần tật định danh Anh hoá" (CEO): template → `coordinator`/`researcher`/`content`/`analyst`/`qa` (+ skill file), snapshot keys personal-pack → English, **migration fleet thật** (bootout service → rename 7 cặp profiles/.data + registry + company + 128 hàng store → bootstrap lại; điều kiện kiểm trước: 0 task dispatchable vì `assigned_to` nằm trong plan_hash) · 20 file test + 6 docs sống đổi id, journals giữ nguyên (lịch sử) · UAT: secretary trả lời dưới id mới, decompose ra researcher/content, web /api/agents English, store 0 id cũ. Vấp: `launchctl stop` bị KeepAlive respawn — phải bootout. 2459 BE. |
| 2026-08-04 | [v61 — Thư ký = điều phối viên (ops)](260804-v61-secretary-orchestration-gateway.md) | ✅ Done | Mở tầng ops-chat cho thư ký với **catalog 12 lệnh điều phối** (`catalog_for_domain` — assign_team_task DAG + confirm, adjust/list/cancel, việc định kỳ, send_message, 4 readonly; KHÔNG fleet-admin, test pin) · backend 100% English (id `create/update/delete_event`, `send_email`, arg `at`, slot `request`) · gỡ cặp M12 giao_viec/chuyen_the sau 1 ngày (một bề mặt giao việc duy nhất — reversal có chủ đích) · UAT thật: brief qua bot thư ký → DAG 4 bước/3 agent → **15 bước chạy thật** (11 soát chéo/rework tự đẻ, 4 agent), handoff thật, ~$0.023 · vấp: review bar cho task nhỏ đắt → task stalled đúng human-gate, ghi roadmap cân chỉnh. 2459 BE. |
| 2026-08-04 | [v60 — Cổng điều phối + sửa/xoá lịch](260804-v60-secretary-dispatch-calendar-edit.md) | ✅ Done | Thư ký thành cổng điều phối: `giao_viec`/`chuyen_the` (reuse team_task types + handler actor-bound — card planning, coordinator lo tiếp; mã thẻ nằm ngay trong reply ✅ để "hủy thẻ <id>") · sửa/xoá lịch: resolver tiêu đề→eventId (mơ hồ hỏi lại, arg `luc` phân biệt trùng tên), `doi_lich` giữ thời lượng cũ, `xoa_lich` qua **carve-out Lớp A cấu trúc** `_is_calendar_event_delete` (chỉ đúng shape 1 event primary; pin 9 biến thể xấu) · UAT thật 4 kịch bản (card thật by=thu-ky, dời 10h→14h, xoá, trùng tên). Backlog: Postgres cross-agent memory + nhắc-việc-theo-giờ. 2459 BE. |
| 2026-08-04 | [v59 — UAT vòng 2 + multi-command](260804-v59-uat-round2-multi-command.md) | ✅ Done | UAT đối kháng 7 pattern trên stack thật: từ chối đúng lệnh chưa có (dời/xoá lịch), Lớp A chặn secret trong mail, English OK, recall memory OK. 2 finding thành 2 nâng cấp: **gui_email nhiều người nhận** (schema comma-separated, gws +send vốn hỗ trợ) + **multi-command** (1 tin ≤3 lệnh theo thứ tự, mỗi lệnh đi đủ gateway độc lập, lệnh hỏng không hủy lệnh khác — UAT thật: lịch + mail cùng 1 tin đều chạy, secret chỉ giết đúng 1 lệnh). Vá **H1 sót 5 chỗ** `allowlist or None` (đường chat Telegram của thư ký đang hồi sinh allowlist core!) + test pin quét source. Vấp: CI đỏ 3 push không ai nhìn (test gọi `shutil.which("gws")` thật). 2450 BE. |
| 2026-08-03 | [v58 — Quét roadmap](260803-v58-roadmap-sweep.md) | ✅ Done (7/7) | Một tối quét sạch backlog: thư ký biết **roster crew** + lệnh **gui_email** (nới vetted-types đúng 1 type, test pin) + **kioku memory** (v19.5 thi công — recall ngữ nghĩa theo câu hỏi, 7/7 điều kiện red-team, UAT thật "vụ hợp đồng gì tuần này" trả lời từ vault) · **queue transparency** (card ⏳ xếp sau N việc — hết kẹt im lặng, ROI cao nhất định giá 08-01) · review tray join thẳng bằng **attempt_id mờ** (heuristic v54 thành fallback) · nợ dọn: /staff yaml-peek (hết load_profile per-staff), codebase-summary v50→v58, 2 mục roadmap chết đóng · **go-live checklist + drill thật** — drill bắt bug an toàn: kill-switch env bị profile-wins đè chết → vá env-true-thắng-tuyệt-đối. 2443 BE + 281 FE. |
| 2026-07-19 | [v54 — Văn phòng cockpit](260719-v54-office-cockpit.md) | ✅ Done | Gap-analysis: backend > những gì office thể hiện. CEO chốt cockpit-first + layout A rail-trái (3 mockup ASCII). **Rail "Chờ anh/chị"**: duyệt + clarify xử lý TẠI office (API sẵn, 0 đường ghi mới) + "Sắp chạy" (effective schedule — service tiêm watch */5, lọc team-tick heartbeat); **feed "Ra ngoài"**: bridge external_action tại `_record` gateway (try/except-total, no-content-echo); **tray review per-criterion**: criteria_json vào captures (CEO duyệt scope giữa chừng — P3 phát hiện data bị vứt, dừng hỏi đúng quy tắc), correlation đúng-1-khớp; **3D**: ✋ (cả bàn coordinator)/×N fan-out/ghost deep_team — logic pure unit-tested. UAT live 6/6: vòng cockpit TỰ KHÉP (task stalled → escalation đẻ clarify → rail → xử tại chỗ); criteria THẬT render; 3 bug bắt-fix (schedule đọc schedule thô, 480px tràn min-content, dòng external đúp). VẤP: automation click ref-stale báo Done giả → verify STATE sau mỗi action; preview assign gọi LLM phải chờ điều kiện. 2389 BE + 261 FE, 9 commits. |
| 2026-07-19 | [v53 — UI kỷ luật + song ngữ](260719-v53-ui-discipline-language-mode.md) | ✅ Done | Audit lộ drift 40-version: 8 kiểu button/65 nút trần, 5 card, 3 empty-state, cost 2-vs-4 lẻ, 3 token ma, nav trộn EN. **6 primitive** (Button/Card/Badge/Input/EmptyState/PageHeader — wrapper mỏng trên class chuẩn) + App.css 5 section cứng + quy tắc chống-drift; quét ~30 view 3 batch subagent (65 nút convert, 28 giữ có lý do); **formatCost 1 quy tắc** (4 lẻ <$1); **language mode VN/EN**: dictionary typed-keys (thiếu key EN = LỖI COMPILE — type system làm test i18n), chip VN/EN header, ~60 file quét, ranh giới data-vs-layout giữ đúng (backend/LLM strings vẫn VN); Canvas children nhận `t` qua props (r3f ngoài context tree). UAT browser 6/6 (bắt + fix tràn ngang 480px tại chỗ). VẤP: `*/` trong comment CSS đóng comment sớm → minifier gãy khó hiểu; perl `$1{...}` = hash element; 47 test fail một lượt vì thiếu provider. 2370 BE + 221 FE. |
| 2026-07-18 | [v52 — Office dual-lens](260718-v52-office-dual-lens.md) | ✅ Done | Brainstorm→plan→cook 1 phiên: office = 1 màn 2 ống kính (👁 CEO / 🔬 maintainer, toggle header, mode ≠ permission). **Sửa bug UX nuốt `failed`→idle**: desk đỏ pulse + bubble ⚠; verdict flash vòng sàn theo ts event (replay không re-flash); 🔒 sandbox task-level join board pic_id/room_id. High-mode: Health strip (beat+doctor+budget chip) + Desk Inspector + **Captures explorer** + search FTS5 header — 3 GET read-only mới (`routes_observability.py`), allowlist SSE KHÔNG đổi, 0 write-path. Review bắt **M1 đắt**: event failed thiếu attempt_id → zombie worker vẽ desk đỏ GIẢ (bật visual lỗi làm bug im lặng thành ồn ào — soi cả producer khi nâng UI); vá 4 findings. UAT browser thật (instance 8898 auth-off): strip live khớp doctor, captures 79 dòng, search 8 hit; visuals event-driven chờ sự kiện thật. 2370 BE + 211 FE. |
| 2026-07-18 | [v51 P2-6 — Productize: serve/Docker, wheel, CI, docs, lifecycle](260718-v51-productize-phase02-06-serve-docker-wheel-ci-docs-lifecycle.md) | ✅ Done | Cook liên tục 5 phase còn lại: **`my-crew serve`** supervisor (spawn 2 module launchd chạy, SIGTERM forward, chết-1-con→exit để compose/systemd quyết) + **Docker/compose auth-first** (R3 nguyên vẹn, state volume `/data`) · **wheel `_shipped`** (hatch force-include, `SHIPPED_ROOT` seam; E2E venv sạch py3.14 pip-install chạy trọn) · **CI/release OIDC** (wheel gate + smoke-install; secret-free suite) · **docs EN-first** (README install-first, 2 guide EN + vi mirror, CHANGELOG/SECURITY/CONTRIBUTING, `.env.example`) · **doctor/upgrade** (reuse integration_health DRY, live bắt 2 thiếu thật; pins MCP single-source). Container E2E bắt 2 bug fresh-install pre-existing: home rỗng thiếu shipped profiles → seed first-run; example đăng ký `admin` không ship profile → coordinator crash-loop mọi fresh clone → skip-loud-not-crash. Vấp: `uv run` tự sync mất extra deep (68 skip giả); docs-subagent không bịa nhưng SÓT đường cài PyPI mới nhất. 2364 BE + 201 FE. Chờ CEO: push + tag v0.1.0 + trusted publisher PyPI. |
| 2026-07-18 | [v51 P1 — Productize: packaging core](260718-v51-productize-phase01-packaging-core.md) | ✅ Done | Audit 4-nguồn (vs openclaw/hermes/omnigent): gap publish = release engineering, không phải code. Phase 01/6: **rename `src`→`my_crew`** (~530 file, top-level "src" không ship PyPI được) · **CLI `my-crew`** argparse `--help`/`--version`, `[project.scripts]`, 0.1.0 · **seam `MY_CREW_HOME`** (env > checkout > `~/.my-crew`, 8 file user-state; checkout byte-identical) · fix bug v49 quickstart guard không đọc `.env`. Review SHIP_WITH_FIXES bắt 2 HIGH rename-miss ngoài sed-scope: demo-mode.sh (guard pgrep **fail-open**) + banner recovery FE dặn lệnh chết → fix + rebuild bundle. Vấp: biến local `src` bị sed ăn nhầm; `\| tail` nuốt exit code ruff; test nạp `.env` thật máy dev. 2351 BE + 201 FE, quickstart live $0.002. **TREO: máy vận hành phải chạy lại `./deploy/install.sh`** (plist cũ trỏ `src.*`). |
| 2026-07-15 | [v47 — Docker-UX](260715-v47-docker-ux.md) | ✅ Done | Bác exec-tier Seatbelt (cách-ly-policy yếu hơn container + phá YAGNI), chọn cải thiện Docker-UX cho friction-daemon. **Health check chủ động** `_docker_check`: probe `docker info` bounded 5s, degrade ✗ sạch (FileNotFoundError/TimeoutExpired/rc≠0) — panel Sức khỏe báo daemon tắt TRƯỚC khi giao việc thay vì chờ SandboxDenied lúc chạy; nhãn "chỉ cần deep_agent" để đội no-shell không bị hù. **Warm opt-in** `prepull_sandbox_image` + `mpm sandbox prepull [image]`: present-noop→pull, trả dict, KHÔNG raise (daemon tắt/offline→message rõ). DRY hằng số `SANDBOX_DEFAULT_IMAGE` thay 2 literal. Docs §6a/§8/§9 (colima nhẹ hơn Desktop, "shell thật CHỈ trong container = yêu cầu bảo mật", string khớp code). Moat nguyên (0 exec-path/host-shell/external-write mới). Test file prepull TÁCH riêng (tránh skipif-deepagents oan). Review DONE (0 critical/high). 2333 BE. |
| 2026-07-15 | [v46 — central-audit actor](260715-v46-central-audit-actor.md) | ✅ Done | Benchmark v45 chấm governance my-crew thắng dứt khoát nhưng lộ gap: audit phân-biệt agent CHỈ qua PATH (data_dir), không field → dashboard đa-agent không filter được "ai làm gì". Thêm field **`actor`** (agent profile_id) đầu-cuối: AuditEntry (JSONL, 0 migration) + approvals (sqlite ALTER migrate-free) + query filter. **1 choke point** `_record`→AuditEntry stamp MỌI outcome branch; cả 2 enqueue site truyền actor. **16 agent-site** thread profile_id/context.agent_id/admin.profile_id; **2 operator-CLI** để "" (lệnh người). Read: `_AUDIT_FIELDS+=actor` → fleet view project field ghi thật. Governance byte-identical (0 đổi guard). Review DONE (0 critical/high). 2324 BE. |
| 2026-07-14 | [v45 — tier-0 routing (no-shell step bỏ Docker)](260714-v45-tier0-routing.md) | ✅ Done | User muốn deep_agent nhanh+bỏ Docker; research+5 phản biện+đo đảo bài toán: đòn bẩy KHÔNG phải bỏ Docker (cold-start 0.4s rẻ; host-exec unsafe cho fleet autonomous+injectable) mà **không spin Docker cho việc không cần shell**. **P1** `needs_shell` field (LLM-set như needs_review, hash có-điều-kiện → all-False hash byte-identical pre-v45, 0 false-stall migrate). **P2** StateBackend scratch cho create_agent (file trong graph-state, strip tool `execute`+fail-loud guard → tuyệt đối no-shell; read-back result["files"] vào reply). **P3** router `resolve_step_runtime`: needs_shell→deep_agent (fail-closed nếu no sandbox); no-shell trên deep_agent-pinned→DROP create_agent (0 Docker); native giữ nguyên; kill-switch→native. Fail-closed 2 chiều (injection ép no-shell chỉ fail, không RCE). **UAT thật**: create_agent ghi /draft_analysis.md compose-early + surface reply, 0 container. Vấp: subprocess+tmpdir=bẫy (leak .env/network), field required phá fixtures→defaulted. Review DONE (0 critical/high). 2312 BE. |
| 2026-07-14 | [v44 — benchmark-hardening wave](260714-v44-benchmark-hardening.md) | ✅ Done | Benchmark HARD-v3 map điểm kém MPM; triage: phần lớn là MOAT lộ ra vì task lệch sân. Wave đúng-identity: **W3 429-backoff** (linear→exp+full-jitter+Retry-After, retry 2→4, total-wait cap 75s kiểm TRƯỚC sleep → không overrun lease 1800s; team run lockstep = tự gây 429 storm, jitter de-sync) · **W1 mem_limit config** per-company mirror seam lease v41 (`_clamp_mem` [256m,4g], default 512m nguyên) · **W2 deep_team cap config** (int riêng `deep_team_max_calls` thay widen bool→union tránh bug int-0-falsy, clamp [1,8], default 3, clause+middleware cùng 1 resolved cap) · **doc §6b** routing decision table (native team=fan-out rộng, deep_team=siloing hẹp, create_agent=no-shell). GIỮ mọi default (moat, không đua tốc độ off-turf); W4 pool WON'T-FIX. Loại 2 non-bug (200-tick=driver, concurrency-2 đã config). Review low: loader type-coercion `isinstance(int)` True cho bool → `_parse_deep_team_max_calls` raise loud. 2290 BE. |
| 2026-07-14 | [v43 — deep-team in-sandbox coordination](260714-v43-deep-team-in-sandbox.md) | ✅ Done | Ảnh benchmark báo "team-of-deep-agents chưa support"; verify code: premise phần lớn SAI (deep_agent đã là team-step + propose split được; raise chỉ ở report). CEO chốt làm **Mức 2**: deep_agent điều phối trợ lý con TRONG sandbox (deepagents `task` tool ON sẵn + moat-clean). **Flag `deep_team` opt-in** → 1 declarative general-purpose subagent (compose-early) + prompt bó ≤3. **Cost-honesty**: UsageMetadataCallbackHandler bắt token cha+con (propagate vào nested subagent.invoke), fold 1 total/step; UAT thật 156k-219k token. **Hard-cap**: TaskCapMiddleware.wrap_tool_call từ chối quá 3 (không fork). Moat: chỉ declarative spec (cấm Compiled/Async), egress vẫn text→gateway. **3 bug vá**: review bắt `_count` không thread-safe (langgraph ToolNode parallel → lock+test 24-thread); latent `gws_context` leak vào build_task (crash mọi deep_agent team-step tiền-v43); UAT lộ 404 flaky (parallel exec race → guard `execute()` degrade như upload/download, 0/2→2/2 pass). 2267 BE. |
| 2026-07-14 | [v42 — deep_agent compose-early contract (step-budget §9)](260714-v42-deep-agent-compose-early-contract.md) | ✅ Done | Benchmark-lại (sau v40 file-write + v41 lease): hết crash + hết chết-lease NHƯNG lộ nút thứ 3 — ~25% run research xong thu thập nhưng đụng trần recursion-loop ĐÚNG lúc compose → reply chỉ "Let me compile the report" + todo, KHÔNG có report. Misallocation budget. **Phương án (a) prompt-contract** (rẻ nhất, report ưu tiên): hằng `_DEEP_AGENT_COMPOSE_CONTRACT` nối vào `system` CHỈ trong `run_deep_agent_work` — "viết nháp report ra /work/*.md SỚM (write_file) + refine, đừng dồn viết ở cuối". **0 code lõi**: recursion_limit giữ nguyên (bounded loop = guardrail); không đụng gateway/sandbox/sanitizer/prompt-chung/tier khác. 3 unit test (fake deepagents→assert contract append đúng `system_prompt`). Vấp: `qwen/qwen-3.7` (comment .env) KHÔNG phải id thật→400, query /models lấy `qwen/qwen3-coder`; exit-0≠PASS (traceback bị nuốt). **UAT thật Docker+qwen3-coder**: report 150 từ đầy đủ NẰM TRONG reply (đúng thứ trước mất); read-back=False OK (viết thẳng reply, mục tiêu đạt cả 2 đường). 2245 BE. |
| 2026-07-14 | [v40 — deep_agent sandbox file-write fix](260714-v40-deep-agent-filewrite-fix.md) | ✅ Done | Từ benchmark 3-harness §10.3: deep_agent crash 3/4 khi ghi file vì `upload_files`/`download_files` là stub `[]` → deepagents middleware assert. Implement thật cả Docker + Fake, 1 response/file, confine `/work`. Docker dùng **exec+base64** (put_archive fail vì rootfs read-only + /work tmpfs). Real-Docker E2E bắt bug thiết kế (tar→exec); self-review bắt **shell-injection** (path vào `sh -c`) → `_SAFE_PATH_RE` chặn metachar, verify `a; touch /work/HACKED` refused. deep_agent trả kết quả text-reply (giữ deliver). 19 test. 2226 BE. |
| 2026-07-13 | [v39 — Google Workspace context + SMTP connection UI](260713-v39-gws-context-smtp-connection.md) | ✅ Done | Agent nhiều context hơn. **P1**: gws READ (Gmail/Calendar/Drive) qua CLI argv CODE-cố-định + `--format json`, tool flag-gated `gws_context` (mặc định TẮT), internal-only, degrade. **P2**: SMTP vào Connections (7 key writable + health check present-only + card) bịt lỗ v38. **P3**: Calendar-create WRITE qua gateway (`("calendar","events","insert")` allowlist + `acl` security marker; chat-ops `create_calendar_event` slot→argv CODE-dựng; Gmail-send bỏ vì đã có email_send). Review Sonnet 0 finding (trace VERIFY HARD trên code). Live E2E bắt bug +agenda in TABLE không JSON → thêm --format json (fake-test không bắt được). 2207 BE. |
| 2026-07-13 | [v38 — harness wave 1: send_message + skill-curator](260713-v38-harness-sendmessage-skillcurator.md) | ✅ Done | TOP-2 gap research harness-v37. **#1 send_message**: facade `{channel,to,text}`→writer per-channel (slack/telegram/email) đã gọi gateway → thừa hưởng Lớp A/B + trust_mode + audit. Surface = chat-ops catalog (LLM điền slot, CODE gọi), KHÔNG là tool LLM-callable trong read-only loop (moat nguyên). **#2 skill-curator**: đếm skill được chọn (per-agent sidecar) + archive skill agent-own quá hạn (90d/30d) sang .archive (không xoá, skip template-role v36); sweep 3h sáng cooldown 24h. Review Sonnet bắt 2 HIGH thật: reply "Đã gửi" cho cả dedup/skip → sửa trung thực theo status; Slack denylist-only → CEO chốt thêm allowlist. 2177 BE. |
| 2026-07-13 | [v37 — UI design-system sync](260713-v37-ui-design-system-sync.md) | ✅ Done | CEO thấy màn Văn phòng font lệch + không thẳng hàng. Audit đo computed-style trên web sống → gốc: (a) cột Kết quả lệch 9px vì box-padding, (b) input+button không-class toàn app dùng 13.3px UA-default (rule input chỉ trong @media mobile). **P1**: box chuyển vào `.office-artifacts-body` → 3 zone-title cùng top 588; task h4→`--fs-h4` (phân cấp size); h2→h3 bỏ 2 h2 chồng. **P2**: input/button về BASE token toàn app (input 16px, button 14.4px); dọn `--fs-md` undefined + `0.85em`×2 + `0.62rem`. Thuần CSS + 2 tag, 0 logic. UAT browser: zones 588/588/588, input 16px 3 trang, 0 overflow. 200 FE. |
| 2026-07-13 | [v36 — storage hygiene + template hybrid](260713-v36-storage-hygiene-template-hybrid.md) | ✅ Done | Backlog mgmt #1+#2. **P1**: GC theo tuổi cho 4 store phình vô hạn (captures 180d/office_room 90d/clarify 90d answered-expired-only/dedup 7d) + `schema_meta` version + integrity audit read-only daily-gated, best-effort trong team-tick. **P2**: template live-skills — agent ghi `template_role`, skill load thẳng từ `profiles/templates/<role>/skills/` lúc chạy (không copy, agent-own override thắng); sửa template = cả đội nhận; agent cũ byte-identical. **P3**: config version-pin — template.yaml có `version`, badge "bản mới" + dialog nâng cấp có duyệt (chỉ áp field user chưa sửa, backup trước ghi, qua save-door). 9-DB split GIỮ nguyên. Review Sonnet bắt HIGH thật: naive `datetime.now()` vs aware UTC row-ts → dedup xoá sớm ~7h; sửa. 2149 BE + 200 FE. |
| 2026-07-13 | [v35 — tool-error resilience + memory consolidation](260713-v35-toolerror-memory-consolidation.md) | ✅ Done | Backlog #1+#2 research. **P1**: `tool_error_guard` bọc read-tool ở `_shim`/`_as_lc_tools` — exception thân tool → "⚠️ tool lỗi/bị từ chối" trả LLM (trước NỔ cả graph create_agent → step failed). Vá tầng my-crew không dùng middleware langchain (pinned ToolNode chỉ trả ToolInvocationError; re-raise GraphBubbleUp). Deep tier tự degrade sẵn. **P2**: `memory/consolidation.py` sweep 3h sáng rút gọn section AGENT-MEMORY (ngưỡng 8000, cooldown 24h) bằng 1 LLM call code-gọi + validate (shrink≥20%/marker-inject/control/fact-count) + archive gốc trước ghi + os.replace; LLM KHÔNG free-write (giữ moat). E2E sống 2 phase. 2128 BE. Reviewer subagent chết giữa (limit) → tự hoàn tất ruff+adversarial. |
| 2026-07-11 | [v20.5 — runtime-tiers + DeepAgent sandbox](260711-v205-runtime-tiers-sandbox.md) | ✅ Done | Guardrail phân tầng (native<tool-calling<deep, egress↔cách-ly nghịch) + DeepAgent cháy thật. **Phase 0**: điều tra red-team → team-step `external_write` (thiết kế v12) chưa nối → nối hook → per-agent ActionGateway (Lớp A/B); opt-in `team_step_egress`. Nền egress. **Caps**: `runtime_loop_limit` per-runtime (config→runtime qua build_task); cost_cap observability-only (cost thật = company task-level). **DeepAgent**: create_deep_agent (deepagents 0.6.12 optional) chạy shell CHỈ trong sandbox `{fake,docker}` — fail-closed positive allowlist reject local/managed/unknown + assert-not-LocalShell (C3); PII gate loại internal-context (H2); teardown reaper (C6). Sandbox = Docker self-hosted (user chọn: KHÔNG dịch vụ ngoài/phí/data bên thứ 3). Wizard chọn runtime + role prefill (fold IdentityStep, tránh renumber F7). **E2E LLM thật**: shell trong fake sandbox tính 42, PII gate chặn 'BÍ MẬT'. 1797 BE+178 FE. **Red-team 3 reviewer/6 Critical** (Security đọc deepagents wheel) bắt nền plan ẢO: deliver→gateway không tồn tại, execute không env param, backend=None không refuse, cost ma, loop không cap, SIGKILL mồ côi → áp hết. Real Docker E2E follow-up (fake = WIRING). Đính chính docs v20 over-claim. |
| 2026-07-11 | [v20 — AgentRuntime + community sockets](260711-v20-agentruntime-community-sockets.md) | ✅ Done | Tách agent-loop thành AgentRuntime seam (Native/ToolCalling/DeepAgent) giữ deliver→gateway. ToolCallingRuntime = tool-calling loop (`create_react_agent` langgraph + langchain-openai, KHÔNG `langchain` full) swaps CHỈ run_work → invariant #1 bằng cấu trúc; positive read-allowlist + classify shim mọi tool (E2E LLM thật: model gọi github.prs → classify thấy) + audience-aware + per-loop cap. DeepAgent optional/experimental (isolate, fail-loud sớm). 3 ổ cắm: skill agentskills.io folder-form (trust theo provenance), pack-MCP spawn gate (default-deny + env scrub), _template-pack + PACK-AUTHORING. 1768 BE+177 FE. **Red-team 4 reviewer/5 Critical** bắt kế hoạch gốc PHÁ moat (bind tool vào loop=egress path 2; denylist deletePage lọt; deepagents shell) → CEO giữ 3 runtime, áp fix thiết kế giữ moat. researcher-pack→template skeleton (team-step đã phục vụ). `agent_runtime:` field riêng (không đụng runtime: infra). |
| 2026-07-11 | [v19 — agent-workspace memory seam](260711-v19-agent-workspace-memory-seam.md) | ✅ Done | Agent-harness vòng 1 (khảo sát hermes/OpenClaw/my-kioku). Memory provider seam `src/memory/` (`resolve_memory_text` thay 6 site đọc `loaded.memory` — gồm qa_answer site thứ 6 red-team bắt), provider `static` byte-identical + `kioku` HOÃN v19.5. Workspace protocol v2: `vault/`+`skills/` per-agent; per-agent skill có guard (wrap `format_internal_content`+scrub name+collision không shadow pack); capability block INTERNAL-only (không system msg — H6). 1730 BE+177 FE xanh. **RED-TEAM cứu nửa plan**: 2 reviewer đối chiếu my-kioku THẬT → 7/16 claim sai (chưa publish npm E404, `--digest`=recency-top-5 không-query, `bun x`=RCE do subprocess kế thừa full `.env`, race vault thiếu busy_timeout) → HOÃN kioku. Vấp: exception phải RuntimeError không ValueError (escape entrypoint catch); stub cũ thiếu memory_config→getattr default. v19.5 nháp + 7 điều kiện tiên quyết. |
| 2026-07-04 | [v6 M15a — watch-task](260704-v6-m15a-watch-task.md) | ✅ Done | Bậc 4 thang trách nhiệm: giao việc SỐNG NHIỀU NGÀY. "Theo dõi PR #45 tới khi merge, nhắc mỗi sáng" → TaskStore per-agent SQLite (open→running→done\|cancelled\|stalled, cap 10, persist restart) + watch check CODE-only (gh state MERGED/CLOSED/deadline 14d → done; LLM KHÔNG quyết "xong rồi", 0 token) + task runner qua pseudo-kind `tasks` (INFRA hold, content-error→stalled sau 3, dedup per-day) + giao qua ops chat M14 (watch_pr confirm/list_tasks readonly/cancel_task). INVARIANT: task KHÔNG nới quyền — mọi post qua gateway đúng phân loại (E2E: reminder external → Lớp B #26 chờ duyệt). 1024 test (20 mới). E2E live: giao PR#1 qua chat LLM thật → check gh thật OPEN→nhắc→Lớp B → check stub MERGED → tự done. Phạm vi M15a; report-task/qa-task + web board → M15b. Bài học: stop condition CODE-only là bất biến an toàn (LLM quyết "PR xong chưa" thì injection trong title lừa done sớm được); "task không nới quyền" phải verify RUNTIME — E2E external→Lớp B chính là bằng chứng đó. |

## Template entry (`YYMMDD-<slug>.md`)

```markdown
# <Tiêu đề mốc>
<ngày> · <trạng thái>

## Làm gì
3-5 gạch: tính năng/kiến trúc đã build (cái verify được).

## Quyết định & vì sao
Bảng: Quyết định | Vì sao | Đánh đổi. Chỉ mốc đáng nhớ.

## Vấp & học được
2-4 gạch: sai gì → rút ra gì. Ngắn.

## Mở / sang sau
1-3 gạch.
```

Sau khi viết entry, thêm 1 dòng vào bảng dòng thời gian trên.
