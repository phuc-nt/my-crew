"""Company Docs library (v7 M19) — a shared, CEO-curated set of flat markdown documents that
agents opt into per-profile and that inject into the INTERNAL compose prompt only.

Distinct from the M18b per-agent "knowledge" form (SOUL/PROJECT editor): this is the company-
wide document library. See `store` for persistence and `inject` for the internal-only render.
"""
